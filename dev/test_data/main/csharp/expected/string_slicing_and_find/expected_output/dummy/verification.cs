/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

using Aas = dummy;  // renamed
using CodeAnalysis = System.Diagnostics.CodeAnalysis;
using Regex = System.Text.RegularExpressions.Regex;

using System.Collections.Generic;  // can't alias
using System.Linq;  // can't alias

namespace dummy
{
    /// <summary>
    /// Verify that the instances of the meta-model satisfy the invariants.
    /// </summary>
    /// <example>
    /// Here is an example how to verify an instance of Something:
    /// <code>
    /// var anInstance = new Aas.Something(
    ///     // ... some constructor arguments ...
    /// );
    /// foreach (var error in Verification.Verify(anInstance))
    /// {
    ///     System.Console.Writeln(
    ///         $"{error.Cause} at: " +
    ///         Reporting.GenerateJsonPath(error.PathSegments));
    /// }
    /// </code>
    /// </example>
    public static class Verification
    {
        /// <summary>
        /// Check the slice up to the position found with <c>find</c>.
        /// </summary>
        public static bool DateBeforeTimeIsLongEnough(
            string text
        )
        {
            long position = (
                (long)text.IndexOf("T", System.StringComparison.Ordinal));
            switch (position)
            {
                case -1:
                    return true;
            }
            return (
                StringHelpers.Slice(text, 0, position).Length == 10);
        }  // public static bool DateBeforeTimeIsLongEnough

        /// <summary>
        /// Check the slice from the position after the one found with <c>find</c>.
        /// </summary>
        public static bool TimeAfterDateIsLongEnough(
            string text
        )
        {
            long position = (
                (long)text.IndexOf("T", System.StringComparison.Ordinal));
            switch (position)
            {
                case -1:
                    return true;
            }
            return (
                StringHelpers.Slice(text, position + 1).Length == 8);
        }  // public static bool TimeAfterDateIsLongEnough

        /// <summary>
        /// Check <c>find</c> with a start and the slice between the found positions.
        /// </summary>
        public static bool MonthIsSeptember(
            string text
        )
        {
            long first = (
                (long)text.IndexOf("-", System.StringComparison.Ordinal));
            switch (first)
            {
                case -1:
                    return true;
            }
            long second = StringHelpers.Find(text, "-", first + 1);
            switch (second)
            {
                case -1:
                    return false;
            }
            return (
                StringHelpers.Slice(text, first + 1, second) == "09");
        }  // public static bool MonthIsSeptember

        /// <summary>
        /// Check the slices and <c>find</c> with negative literal positions.
        /// </summary>
        public static bool SecondsFollowColon(
            string text
        )
        {
            long position = (
                (long)text.IndexOf("T", System.StringComparison.Ordinal));
            switch (position)
            {
                case -1:
                    return true;
            }
            return text.Length < 10
            || (
                StringHelpers.Slice(text, -3, -2) == ":"
                && StringHelpers.Find(text, ":", -3) != -1
                && StringHelpers.Slice(text, 0, -9).Length > 0
            );
        }  // public static bool SecondsFollowColon

        /// <summary>
        /// Check the positions computed at run time which are negative or out of range.
        /// </summary>
        /// <remarks>
        /// The transpiled code needs to follow the Python semantics: a negative position
        /// counts from the end, and the positions out of range are clamped.
        /// </remarks>
        public static bool LastCharacterIsNotZ(
            string text
        )
        {
            long position = (
                (long)text.IndexOf("#", System.StringComparison.Ordinal));
            return StringHelpers.Slice(text, position) != "Z"
            && StringHelpers.Slice(text, -100, 100) == text
            && StringHelpers.Slice(text, 3, 1) == ""
            && StringHelpers.Find(text, "", 100) == -1
            && StringHelpers.Find(text, StringHelpers.Slice(text, position), position) >= 0;
        }  // public static bool LastCharacterIsNotZ

        /// <summary>
        /// Check the slice of a constrained primitive with literal bounds.
        /// </summary>
        public static bool NameStartsWithPrefix(
            string name
        )
        {
            return name.Length < 4
            || StringHelpers.Slice(name, 0, 4) == "name";
        }  // public static bool NameStartsWithPrefix

        /// <summary>
        /// Check <c>find</c> on a constrained primitive and a slice with a literal start.
        /// </summary>
        public static bool NameHasNoSpaceAfterPrefix(
            string name
        )
        {
            return name.Length < 4
            || (long)StringHelpers.Slice(name, 4).IndexOf(" ", System.StringComparison.Ordinal) == -1;
        }  // public static bool NameHasNoSpaceAfterPrefix

        /// <summary>
        /// Hash allowed enum values for efficient validation of enums.
        /// </summary>
        internal static class EnumValueSet
        {

        }  // internal static class EnumValueSet

        /// <summary>
        /// Provide string operations which follow the Python implementation, since
        /// Python is the language of the meta-model specifications.
        /// </summary>
        public static class StringHelpers
        {
            /// <summary>
            /// Resolve <paramref name="position" /> in a string of
            /// <paramref name="length" /> as Python does in slicing.
            /// </summary>
            /// <remarks>
            /// A negative position counts from the end, and the positions out of range
            /// are clamped to the string.
            /// </remarks>
            private static int ResolvePosition(long position, int length)
            {
                if (position < 0)
                {
                    return (int)System.Math.Max(position + length, 0);
                }

                return (int)System.Math.Min(position, length);
            }

            /// <summary>
            /// Slice <paramref name="text" /> from <paramref name="start" /> up to
            /// <paramref name="end" />, exclusive.
            /// </summary>
            /// <remarks>
            /// We follow the Python implementation of slicing, since Python is
            /// the language of the meta-model specifications. Hence, a negative position
            /// counts from the end, the positions out of range are clamped to the string,
            /// and the slice is empty if <paramref name="start" /> is not before
            /// <paramref name="end" />. If <paramref name="end" /> is not given, we slice
            /// up to the end of <paramref name="text" />.
            /// </remarks>
            public static string Slice(string text, long start, long? end = null)
            {
                int theStart = ResolvePosition(start, text.Length);
                int theEnd = end is null
                    ? text.Length
                    : ResolvePosition(end.Value, text.Length);

                if (theStart >= theEnd)
                {
                    return "";
                }

                return text.Substring(theStart, theEnd - theStart);
            }

            /// <summary>
            /// Find the first <paramref name="sub" /> in <paramref name="text" /> from
            /// <paramref name="start" /> on.
            /// </summary>
            /// <remarks>
            /// We follow the Python implementation of <c>str.find</c>, since Python is
            /// the language of the meta-model specifications. Hence, a negative
            /// <paramref name="start" /> counts from the end, and
            /// a <paramref name="start" /> beyond the end of <paramref name="text" />
            /// gives -1.
            /// </remarks>
            /// <returns>
            /// The position of <paramref name="sub" /> in <paramref name="text" />,
            /// or -1 if not found
            /// </returns>
            public static long Find(string text, string sub, long start)
            {
                long theStart = start < 0
                    ? System.Math.Max(start + text.Length, 0)
                    : start;

                if (theStart > text.Length)
                {
                    return -1;
                }

                return text.IndexOf(sub, (int)theStart, System.StringComparison.Ordinal);
            }
        }  // public static class StringHelpers

        [CodeAnalysis.SuppressMessage("ReSharper", "InconsistentNaming")]
        private static readonly Verification.Transformer _transformer = (
            new Verification.Transformer());

        private class Transformer
            : Visitation.AbstractTransformer<IEnumerable<Reporting.Error>>
        {
            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformSomething(
                Aas.ISomething that
            )
            {
                if (!(
                    !(that.Text.Length >= 1)
                    || (StringHelpers.Slice(that.Text, 0, 1) != "X")))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Text must not start with X");
                }

                if (!Verification.NameHasNoSpaceAfterPrefix(that.Name))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Name must contain no space after the prefix");
                }

                if (!Verification.NameStartsWithPrefix(that.Name))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Name must start with the prefix");
                }

                if (!Verification.LastCharacterIsNotZ(that.Text))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Last character must not be Z");
                }

                if (!Verification.SecondsFollowColon(that.Text))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Seconds must follow a colon");
                }

                if (!Verification.MonthIsSeptember(that.Text))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Month must be September");
                }

                if (!Verification.TimeAfterDateIsLongEnough(that.Text))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Time after the date must have 8 characters");
                }

                if (!Verification.DateBeforeTimeIsLongEnough(that.Text))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "Date before the time must have 10 characters");
                }

                foreach (var error in Verification.VerifyNonEmptyString(that.Name))
                {
                    error.PrependSegment(
                        new Reporting.NameSegment(
                            "name"));
                    yield return error;
                }
            }
        }  // private class Transformer

        /// <summary>
        /// Verify the constraints of <paramref name="that" /> recursively.
        /// </summary>
        /// <param name="that">
        /// The instance of the meta-model to be verified
        /// </param>
        public static IEnumerable<Reporting.Error> Verify(Aas.IClass that)
        {
            foreach (var error in _transformer.Transform(that))
            {
                yield return error;
            }
        }

        /// <summary>
        /// Verify the constraints of <paramref name="that" />.
        /// </summary>
        public static IEnumerable<Reporting.Error> VerifyNonEmptyString (
            string that)
        {
            if (!(that.Length > 0))
            {
                yield return new Reporting.Error(
                    "Invariant violated:\n" +
                    "At least one character");
            }
        }
    }  // public static class Verification
}  // namespace dummy

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
