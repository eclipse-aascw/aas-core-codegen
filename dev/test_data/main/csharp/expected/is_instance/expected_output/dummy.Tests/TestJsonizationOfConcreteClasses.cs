/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

using Aas = dummy;  // renamed

using Directory = System.IO.Directory;
using Nodes = System.Text.Json.Nodes;
using Path = System.IO.Path;

using System.Collections.Generic;  // can't alias
using System.Linq;  // can't alias
using NUnit.Framework; // can't alias

namespace dummy.Tests
{
    public class TestJsonizationOfConcreteClasses
    {
        private static void AssertSerializeDeserializeEqualsOriginal(
            Nodes.JsonNode originalNode, Aas.IClass instance, string path)
        {
            Nodes.JsonObject? serialized = null;
            try
            {
                serialized = Aas.Jsonization.Serialize.ToJsonObject(instance);
            }
            catch (System.Exception exception)
            {
                Assert.Fail(
                    "Expected no exception upon serialization of an instance " +
                    $"de-serialized from {path}, but got: {exception}"
                );
            }

            if (serialized == null)
            {
                Assert.Fail(
                    $"Unexpected null serialization of an instance from {path}"
                );
            }
            else
            {
                Aas.Tests.CommonJson.CheckJsonNodesEqual(
                    originalNode,
                    serialized,
                    out Reporting.Error? inequalityError);
                if (inequalityError != null)
                {
                    Assert.Fail(
                        $"The original JSON from {path} is unequal the serialized JSON: " +
                        $"{Reporting.GenerateJsonPath(inequalityError.PathSegments)}: " +
                        inequalityError.Cause
                    );
                }
            }
        }

        private static void AssertEqualsExpectedOrRerecordDeserializationException(
            Aas.Jsonization.Exception? exception,
            string path)
        {
            if (exception == null)
            {
                Assert.Fail(
                    $"Expected a Jsonization exception when de-serializing {path}, but got none."
                );
            }
            else
            {
                string exceptionPath = path + ".exception";
                string got = exception.Message;
                if (Aas.Tests.Common.RecordMode)
                {
                    System.IO.File.WriteAllText(exceptionPath, got);
                }
                else
                {
                    if (!System.IO.File.Exists(exceptionPath))
                    {
                        throw new System.IO.FileNotFoundException(
                            $"The file with the recorded exception does not exist: {exceptionPath}; " +
                            "maybe you want to set the environment " +
                            $"variable {Aas.Tests.Common.RecordModeEnvironmentVariableName}?");
                    }

                    string expected = System.IO.File.ReadAllText(exceptionPath);
                    Assert.AreEqual(
                        expected.Replace("\r\n", "\n"),
                        got.Replace("\r\n", "\n"),
                        $"The expected exception does not match the actual one for the file {path}");
                }
            }
        }

        [Test]
        public void Test_Leaf_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "Leaf"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.LeafFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_Leaf_ok

        [Test]
        public void Test_Leaf_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.LeafFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing Leaf, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_Leaf_deserialization_from_non_object_fail

        [Test]
        public void Test_Leaf_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "Leaf"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of Leaf for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.LeafFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_Leaf_deserialization_fail

        [Test]
        public void Test_Leaf_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "Leaf"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of Leaf for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.LeafFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_Leaf_verification_fail

        [Test]
        public void Test_OrderedContainer_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "OrderedContainer"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.OrderedContainerFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_OrderedContainer_ok

        [Test]
        public void Test_OrderedContainer_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.OrderedContainerFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing OrderedContainer, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_OrderedContainer_deserialization_from_non_object_fail

        [Test]
        public void Test_OrderedContainer_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "OrderedContainer"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of OrderedContainer for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.OrderedContainerFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_OrderedContainer_deserialization_fail

        [Test]
        public void Test_OrderedContainer_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "OrderedContainer"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of OrderedContainer for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.OrderedContainerFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_OrderedContainer_verification_fail

        [Test]
        public void Test_UnorderedContainer_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "UnorderedContainer"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.UnorderedContainerFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_UnorderedContainer_ok

        [Test]
        public void Test_UnorderedContainer_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.UnorderedContainerFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing UnorderedContainer, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_UnorderedContainer_deserialization_from_non_object_fail

        [Test]
        public void Test_UnorderedContainer_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "UnorderedContainer"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of UnorderedContainer for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.UnorderedContainerFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_UnorderedContainer_deserialization_fail

        [Test]
        public void Test_UnorderedContainer_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "UnorderedContainer"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of UnorderedContainer for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.UnorderedContainerFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_UnorderedContainer_verification_fail

        [Test]
        public void Test_GlobalAttribute_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "GlobalAttribute"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.GlobalAttributeFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_GlobalAttribute_ok

        [Test]
        public void Test_GlobalAttribute_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.GlobalAttributeFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing GlobalAttribute, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_GlobalAttribute_deserialization_from_non_object_fail

        [Test]
        public void Test_GlobalAttribute_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "GlobalAttribute"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of GlobalAttribute for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.GlobalAttributeFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_GlobalAttribute_deserialization_fail

        [Test]
        public void Test_GlobalAttribute_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "GlobalAttribute"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of GlobalAttribute for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.GlobalAttributeFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_GlobalAttribute_verification_fail

        [Test]
        public void Test_LocalAttribute_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "LocalAttribute"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.LocalAttributeFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_LocalAttribute_ok

        [Test]
        public void Test_LocalAttribute_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.LocalAttributeFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing LocalAttribute, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_LocalAttribute_deserialization_from_non_object_fail

        [Test]
        public void Test_LocalAttribute_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "LocalAttribute"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of LocalAttribute for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.LocalAttributeFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_LocalAttribute_deserialization_fail

        [Test]
        public void Test_LocalAttribute_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "LocalAttribute"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of LocalAttribute for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.LocalAttributeFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_LocalAttribute_verification_fail

        [Test]
        public void Test_AttributeOperand_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "AttributeOperand"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.AttributeOperandFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_AttributeOperand_ok

        [Test]
        public void Test_AttributeOperand_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.AttributeOperandFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing AttributeOperand, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_AttributeOperand_deserialization_from_non_object_fail

        [Test]
        public void Test_AttributeOperand_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "AttributeOperand"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of AttributeOperand for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.AttributeOperandFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_AttributeOperand_deserialization_fail

        [Test]
        public void Test_AttributeOperand_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "AttributeOperand"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of AttributeOperand for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.AttributeOperandFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_AttributeOperand_verification_fail

        [Test]
        public void Test_StringLiteral_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "StringLiteral"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.StringLiteralFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_StringLiteral_ok

        [Test]
        public void Test_StringLiteral_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.StringLiteralFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing StringLiteral, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_StringLiteral_deserialization_from_non_object_fail

        [Test]
        public void Test_StringLiteral_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "StringLiteral"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of StringLiteral for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.StringLiteralFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_StringLiteral_deserialization_fail

        [Test]
        public void Test_StringLiteral_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "StringLiteral"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of StringLiteral for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.StringLiteralFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_StringLiteral_verification_fail

        [Test]
        public void Test_NumberLiteral_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "NumberLiteral"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.NumberLiteralFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_NumberLiteral_ok

        [Test]
        public void Test_NumberLiteral_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.NumberLiteralFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing NumberLiteral, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_NumberLiteral_deserialization_from_non_object_fail

        [Test]
        public void Test_NumberLiteral_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "NumberLiteral"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of NumberLiteral for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.NumberLiteralFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_NumberLiteral_deserialization_fail

        [Test]
        public void Test_NumberLiteral_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "NumberLiteral"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of NumberLiteral for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.NumberLiteralFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_NumberLiteral_verification_fail

        [Test]
        public void Test_Something_ok()
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    "Something"
                ),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            foreach (var path in paths)
            {
                var node = Aas.Tests.CommonJson.ReadFromFile(path);

                var instance = Aas.Jsonization.Deserialize.SomethingFrom(
                    node);

                var errors = Aas.Verification.Verify(instance).ToList();
                Aas.Tests.Common.AssertNoVerificationErrors(errors, path);

                AssertSerializeDeserializeEqualsOriginal(
                    node, instance, path);
            }
        }  // public void Test_Something_ok

        [Test]
        public void Test_Something_deserialization_from_non_object_fail()
        {
            var node = Nodes.JsonValue.Create("INVALID")
                ?? throw new System.InvalidOperationException(
                    "Unexpected failure of the node creation");

            Aas.Jsonization.Exception? exception = null;
            try
            {
                var _ = Aas.Jsonization.Deserialize.SomethingFrom(
                    node);
            }
            catch (Aas.Jsonization.Exception observedException)
            {
                exception = observedException;
            }

            if (exception == null)
            {
                throw new AssertionException("Expected an exception, but got none");
            }

            if (
                !exception.Message.StartsWith(
                    "Expected a JsonObject representing Something, but got "))
            {
                throw new AssertionException(
                    $"Unexpected exception message: {exception.Message}");
            }
        }  // public void Test_Something_deserialization_from_non_object_fail

        [Test]
        public void Test_Something_deserialization_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Unserializable"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "Something"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of Something for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    Aas.Jsonization.Exception? exception = null;
                    try
                    {
                        var _ = Aas.Jsonization.Deserialize.SomethingFrom(
                            node);
                    }
                    catch (Aas.Jsonization.Exception observedException)
                    {
                        exception = observedException;
                    }

                    AssertEqualsExpectedOrRerecordDeserializationException(
                        exception, path);
                }
            }
        }  // public void Test_Something_deserialization_fail

        [Test]
        public void Test_Something_verification_fail()
        {
            foreach (
                string causeDir in
                Directory.GetDirectories(
                    Path.Combine(
                        Aas.Tests.Common.TestDataDir,
                        "Json",
                        "Unexpected",
                        "Invalid"
                    )
                )
            )
            {
                string clsDir = Path.Combine(
                    causeDir,
                    "Something"
                );

                if (!Directory.Exists(clsDir))
                {
                    // No examples of Something for the failure cause.
                    continue;
                }

                var paths = Directory.GetFiles(
                    clsDir,
                    "*.json",
                    System.IO.SearchOption.AllDirectories).ToList();
                paths.Sort();

                foreach (var path in paths)
                {
                    var node = Aas.Tests.CommonJson.ReadFromFile(path);

                    var instance = Aas.Jsonization.Deserialize.SomethingFrom(
                        node);

                    var errors = Aas.Verification.Verify(instance).ToList();
                    Aas.Tests.Common.AssertEqualsExpectedOrRerecordVerificationErrors(
                        errors, path);
                }
            }
        }  // public void Test_Something_verification_fail

        /// <summary>
        /// Read the first recorded example of the <paramref name="modelType" />.
        /// </summary>
        private static Nodes.JsonNode LoadTheFirstExpected(string modelType)
        {
            var paths = Directory.GetFiles(
                Path.Combine(
                    Aas.Tests.Common.TestDataDir,
                    "Json",
                    "Expected",
                    modelType),
                "*.json",
                System.IO.SearchOption.AllDirectories).ToList();
            paths.Sort();

            Assert.IsNotEmpty(
                paths,
                $"Expected at least one recorded example of {modelType}, but got none");

            return Aas.Tests.CommonJson.ReadFromFile(paths[0]);
        }

        [Test]
        public void TestNumberLiteralNumberSerializationNonFinite()
        {
            foreach (var value in new double[] { System.Double.PositiveInfinity, System.Double.NegativeInfinity, System.Double.NaN })
            {
                var node = LoadTheFirstExpected(
                    "NumberLiteral");

                var instance = Aas.Jsonization.Deserialize.NumberLiteralFrom(
                    node);

                instance.Number = value;

                Aas.Jsonization.SerializationException? exception = null;
                try
                {
                    var _ = Aas.Jsonization.Serialize.ToJsonObject(instance);
                }
                catch (Aas.Jsonization.SerializationException observedException)
                {
                    exception = observedException;
                }

                Assert.IsNotNull(
                    exception,
                    "Expected the serialization to fail at " +
                        "Number" +
                        ", but it succeeded");

                Assert.AreEqual(
                    "Number",
                    exception!.Path);
            }
        }  // public void TestNumberLiteralNumberSerializationNonFinite
    }  // class TestJsonizationOfConcreteClasses
}  // namespace dummy.Tests

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
