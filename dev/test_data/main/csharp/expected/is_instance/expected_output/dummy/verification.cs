/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

using Aas = dummy;  // renamed
using CodeAnalysis = System.Diagnostics.CodeAnalysis;
using Regex = System.Text.RegularExpressions.Regex;

using System.Collections.Generic;  // can't alias
using System.Linq;  // can't alias

namespace dummy
{
    /// <summary>
    /// Verify that the instances of the meta-model satisfy the invariants.
    /// </summary>
    /// <example>
    /// Here is an example how to verify an instance of IElement:
    /// <code>
    /// var anInstance = new Aas.IElement(
    ///     // ... some constructor arguments ...
    /// );
    /// foreach (var error in Verification.Verify(anInstance))
    /// {
    ///     System.Console.Writeln(
    ///         $"{error.Cause} at: " +
    ///         Reporting.GenerateJsonPath(error.PathSegments));
    /// }
    /// </code>
    /// </example>
    public static class Verification
    {
        /// <summary>
        /// Check recursively whether there is a leaf in the tree of <paramref name="element" />.
        /// </summary>
        /// <remarks>
        /// This function tests the narrowing in a conjunction nested in a disjunction,
        /// and the self-recursion.
        /// </remarks>
        public static bool HasLeafInTree(
            IElement element
        )
        {
            return element is Aas.ILeaf
            || (
                element is Aas.IContainer
                && (
                    ((Aas.IContainer)element).Children.Any(
                        child => Verification.HasLeafInTree(child))
                )
            );
        }  // public static bool HasLeafInTree

        /// <summary>
        /// Check recursively that all the leaves in the tree of <paramref name="element" />
        /// have a non-empty text.
        /// </summary>
        /// <remarks>
        /// This function tests the narrowing in a chain of implications, and
        /// the self-recursion.
        /// </remarks>
        public static bool LeavesInTreeAreNotEmpty(
            IElement element
        )
        {
            return (
                !(element is Aas.ILeaf)
                || (StringHelpers.Len(((Aas.ILeaf)element).Text) > 0)
            )
            && (
                !(element is Aas.IContainer)
                || (
                    ((Aas.IContainer)element).Children.All(
                        child => Verification.LeavesInTreeAreNotEmpty(child))
                )
            );
        }  // public static bool LeavesInTreeAreNotEmpty

        /// <summary>
        /// Check that <paramref name="element" /> is either not a leaf or has a short text.
        /// </summary>
        /// <remarks>
        /// This function tests the narrowing in a disjunction of more than two values,
        /// where the first value is a negated <c>isinstance</c>.
        /// </remarks>
        public static bool IsShortLeafOrNoLeaf(
            IElement element
        )
        {
            return (!(element is Aas.ILeaf))
            || StringHelpers.Len(((Aas.ILeaf)element).Text) == 0
            || StringHelpers.Len(((Aas.ILeaf)element).Text) < 16;
        }  // public static bool IsShortLeafOrNoLeaf

        /// <summary>
        /// Check that <paramref name="element" /> is a container.
        /// </summary>
        /// <remarks>
        /// This function tests <c>isinstance</c> over a tuple of classes.
        /// </remarks>
        public static bool IsContainer(
            IElement element
        )
        {
            return (
                (element is Aas.IOrderedContainer || element is Aas.IUnorderedContainer));
        }  // public static bool IsContainer

        /// <summary>
        /// Check that <paramref name="element" /> is an ordered container which is sorted.
        /// </summary>
        /// <remarks>
        /// This function tests the narrowing over two levels of the class hierarchy.
        /// </remarks>
        public static bool IsSortedOrderedContainer(
            IElement element
        )
        {
            return element is Aas.IContainer
            && element is Aas.IOrderedContainer
            && ((Aas.IOrderedContainer)element).IsSorted;
        }  // public static bool IsSortedOrderedContainer

        /// <summary>
        /// Check that <paramref name="container" /> has at least one child.
        /// </summary>
        public static bool ContainerHasChildren(
            IContainer container
        )
        {
            return container.Children.Count > 0;
        }  // public static bool ContainerHasChildren

        /// <summary>
        /// Check that <paramref name="value" /> refers to a global attribute of kind name.
        /// </summary>
        /// <remarks>
        /// This function tests the narrowing of a named union to a class, and then
        /// the narrowing of a property of that class from another named union to a class.
        /// </remarks>
        public static bool IsGlobalAttributeOfKindName(
            Value value
        )
        {
            return value.Underlying is Aas.IAttributeOperand
            && ((Aas.IAttributeOperand)value.Underlying).Attribute.Underlying is Aas.IGlobalAttribute
            && ((Aas.IGlobalAttribute)((Aas.IAttributeOperand)value.Underlying).Attribute.Underlying).Kind == "name";
        }  // public static bool IsGlobalAttributeOfKindName

        /// <summary>
        /// Check that <paramref name="value" /> is a string value.
        /// </summary>
        /// <remarks>
        /// This function tests <c>isinstance</c> over a tuple of classes on a named union.
        /// </remarks>
        public static bool IsStringValue(
            Value value
        )
        {
            return (
                (value.Underlying is Aas.IStringLiteral || value.Underlying is Aas.IAttributeOperand));
        }  // public static bool IsStringValue

        /// <summary>
        /// Hash allowed enum values for efficient validation of enums.
        /// </summary>
        internal static class EnumValueSet
        {

        }  // internal static class EnumValueSet

        /// <summary>
        /// Provide string operations which follow the Python implementation, since
        /// Python is the language of the meta-model specifications.
        /// </summary>
        /// <remarks>
        /// The lengths and the positions count the characters (code points), and not
        /// the UTF-16 code units of the C# strings. Hence, a character beyond the Basic
        /// Multilingual Plane counts as one, though it takes two UTF-16 code units
        /// (a surrogate pair).
        /// </remarks>
        public static class StringHelpers
        {
            /// <summary>
            /// Check whether a surrogate pair starts at <paramref name="offset" />
            /// in <paramref name="text" />.
            /// </summary>
            private static bool IsSurrogatePairAt(string text, int offset)
            {
                return (
                    offset + 1 < text.Length
                    && char.IsHighSurrogate(text[offset])
                    && char.IsLowSurrogate(text[offset + 1])
                );
            }

            /// <summary>
            /// Count the characters of <paramref name="text" /> between the UTF-16
            /// offsets <paramref name="startOffset" /> and <paramref name="endOffset" />.
            /// </summary>
            private static int CountCharacters(
                string text,
                int startOffset,
                int endOffset
            )
            {
                int count = 0;
                int offset = startOffset;
                while (offset < endOffset)
                {
                    offset += IsSurrogatePairAt(text, offset) ? 2 : 1;
                    count++;
                }

                return count;
            }

            /// <summary>
            /// Compute the UTF-16 offset of the character at <paramref name="position" />
            /// in <paramref name="text" />.
            /// </summary>
            private static int OffsetOf(string text, int position)
            {
                int offset = 0;
                for (int i = 0; i < position; i++)
                {
                    offset += IsSurrogatePairAt(text, offset) ? 2 : 1;
                }

                return offset;
            }

            /// <summary>
            /// Resolve <paramref name="position" /> in a string of
            /// <paramref name="length" /> as Python does in slicing.
            /// </summary>
            /// <remarks>
            /// A negative position counts from the end, and the positions out of range
            /// are clamped to the string.
            /// </remarks>
            private static int ResolvePosition(long position, int length)
            {
                if (position < 0)
                {
                    return (int)System.Math.Max(position + length, 0);
                }

                return (int)System.Math.Min(position, length);
            }

            /// <summary>
            /// Count the characters (code points) of <paramref name="text" />.
            /// </summary>
            /// <remarks>
            /// We follow the Python implementation of <c>len</c>, since Python is
            /// the language of the meta-model specifications. Hence, a character beyond
            /// the Basic Multilingual Plane counts as one, unlike in <c>text.Length</c>.
            /// </remarks>
            public static int Len(string text)
            {
                return CountCharacters(text, 0, text.Length);
            }

            /// <summary>
            /// Slice <paramref name="text" /> from <paramref name="start" /> up to
            /// <paramref name="end" />, exclusive.
            /// </summary>
            /// <remarks>
            /// We follow the Python implementation of slicing, since Python is
            /// the language of the meta-model specifications. Hence, the positions count
            /// the characters (code points), a negative position counts from the end,
            /// the positions out of range are clamped to the string, and the slice is
            /// empty if <paramref name="start" /> is not before <paramref name="end" />.
            /// If <paramref name="end" /> is not given, we slice up to the end of
            /// <paramref name="text" />.
            /// </remarks>
            public static string Slice(string text, long start, long? end = null)
            {
                int length = Len(text);
                int theStart = ResolvePosition(start, length);
                int theEnd = end is null
                    ? length
                    : ResolvePosition(end.Value, length);

                if (theStart >= theEnd)
                {
                    return "";
                }

                int startOffset = OffsetOf(text, theStart);
                int endOffset = OffsetOf(text, theEnd);
                return text.Substring(startOffset, endOffset - startOffset);
            }

            /// <summary>
            /// Find the first <paramref name="sub" /> in <paramref name="text" /> from
            /// <paramref name="start" /> on.
            /// </summary>
            /// <remarks>
            /// We follow the Python implementation of <c>str.find</c>, since Python is
            /// the language of the meta-model specifications. Hence, the positions count
            /// the characters (code points), a negative <paramref name="start" /> counts
            /// from the end, and a <paramref name="start" /> beyond the end of
            /// <paramref name="text" /> gives -1. We compare the strings ordinally.
            /// </remarks>
            /// <returns>
            /// The position of <paramref name="sub" /> in <paramref name="text" />,
            /// or -1 if not found
            /// </returns>
            public static long Find(string text, string sub, long start = 0)
            {
                int length = Len(text);
                long theStart = start < 0
                    ? System.Math.Max(start + length, 0)
                    : start;

                if (theStart > length)
                {
                    return -1;
                }

                int startOffset = OffsetOf(text, (int)theStart);
                int offset = text.IndexOf(
                    sub,
                    startOffset,
                    System.StringComparison.Ordinal
                );

                if (offset == -1)
                {
                    return -1;
                }

                return theStart + CountCharacters(text, startOffset, offset);
            }
        }  // public static class StringHelpers

        [CodeAnalysis.SuppressMessage("ReSharper", "InconsistentNaming")]
        private static readonly Verification.Transformer _transformer = (
            new Verification.Transformer());

        private class Transformer
            : Visitation.AbstractTransformer<IEnumerable<Reporting.Error>>
        {
            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformLeaf(
                Aas.ILeaf that
            )
            {
                // No verification has been defined for Leaf.
                yield break;
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformOrderedContainer(
                Aas.IOrderedContainer that
            )
            {
                int indexChildren = 0;
                foreach (var item in that.Children)
                {
                    foreach (var error in Verification.Verify(item))
                    {
                        error.PrependSegment(
                            new Reporting.IndexSegment(
                                indexChildren));
                        error.PrependSegment(
                            new Reporting.NameSegment(
                                "children"));
                        yield return error;
                    }
                    indexChildren++;
                }
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformUnorderedContainer(
                Aas.IUnorderedContainer that
            )
            {
                int indexChildren = 0;
                foreach (var item in that.Children)
                {
                    foreach (var error in Verification.Verify(item))
                    {
                        error.PrependSegment(
                            new Reporting.IndexSegment(
                                indexChildren));
                        error.PrependSegment(
                            new Reporting.NameSegment(
                                "children"));
                        yield return error;
                    }
                    indexChildren++;
                }
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformGlobalAttribute(
                Aas.IGlobalAttribute that
            )
            {
                // No verification has been defined for GlobalAttribute.
                yield break;
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformLocalAttribute(
                Aas.ILocalAttribute that
            )
            {
                // No verification has been defined for LocalAttribute.
                yield break;
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformAttributeOperand(
                Aas.IAttributeOperand that
            )
            {
                foreach (var error in Verification.Verify(that.Attribute))
                {
                    error.PrependSegment(
                        new Reporting.NameSegment(
                            "attribute"));
                    yield return error;
                }
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformStringLiteral(
                Aas.IStringLiteral that
            )
            {
                // No verification has been defined for StringLiteral.
                yield break;
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformNumberLiteral(
                Aas.INumberLiteral that
            )
            {
                // No verification has been defined for NumberLiteral.
                yield break;
            }

            [CodeAnalysis.SuppressMessage("ReSharper", "NegativeEqualityExpression")]
            public override IEnumerable<Reporting.Error> TransformSomething(
                Aas.ISomething that
            )
            {
                if (!Verification.HasLeafInTree(that.Root))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "There must be at least one leaf in the tree.");
                }

                if (!(
                    Verification.IsGlobalAttributeOfKindName(that.Value)))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "The value must be a global attribute of kind name.");
                }

                if (!(
                    that.Values.All(
                        value => !(value.Underlying is Aas.INumberLiteral)
                            || (((Aas.INumberLiteral)value.Underlying).Number >= 0.0))))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "The number literals among the values must be non-negative.");
                }

                if (!(
                    that.Values.Any(
                        value => value.Underlying is Aas.IStringLiteral
                            && ((Aas.IStringLiteral)value.Underlying).Text == "root")))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "There must be at least one string literal ``root`` among " +
                        "the values.");
                }

                if (!(
                    !(that.Root is Aas.IContainer)
                    || Verification.ContainerHasChildren(((Aas.IContainer)that.Root))))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "The root, if a container, must have children.");
                }

                if (!(
                    !(
                        (that.OptionalElement != null)
                        && that.OptionalElement is Aas.ILeaf
                    )
                    || (StringHelpers.Len(((Aas.ILeaf)that.OptionalElement).Text) > 0)))
                {
                    yield return new Reporting.Error(
                        "Invariant violated:\n" +
                        "The optional element, if a leaf, must have a non-empty text.");
                }

                foreach (var error in Verification.Verify(that.Root))
                {
                    error.PrependSegment(
                        new Reporting.NameSegment(
                            "root"));
                    yield return error;
                }

                if (that.OptionalElement != null)
                {
                    foreach (var error in Verification.Verify(that.OptionalElement))
                    {
                        error.PrependSegment(
                            new Reporting.NameSegment(
                                "optionalElement"));
                        yield return error;
                    }
                }

                foreach (var error in Verification.Verify(that.Value))
                {
                    error.PrependSegment(
                        new Reporting.NameSegment(
                            "value"));
                    yield return error;
                }

                int indexValues = 0;
                foreach (var item in that.Values)
                {
                    foreach (var error in Verification.Verify(item))
                    {
                        error.PrependSegment(
                            new Reporting.IndexSegment(
                                indexValues));
                        error.PrependSegment(
                            new Reporting.NameSegment(
                                "values"));
                        yield return error;
                    }
                    indexValues++;
                }
            }
        }  // private class Transformer

        /// <summary>
        /// Verify the constraints of <paramref name="that" /> recursively.
        /// </summary>
        /// <param name="that">
        /// The instance of the meta-model to be verified
        /// </param>
        public static IEnumerable<Reporting.Error> Verify(Aas.IClass that)
        {
            foreach (var error in _transformer.Transform(that))
            {
                yield return error;
            }
        }

        public static IEnumerable<Reporting.Error> Verify(Aas.IUnion that)
        {
            foreach (var error in Verify(that.Underlying))
            {
                yield return error;
            }
        }
    }  // public static class Verification
}  // namespace dummy

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
