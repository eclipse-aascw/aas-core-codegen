// Package enhancing allows for enhancement of model instances with your custom data.
package enhancing

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	aastypes "github.com/dummy-works/dummy/types"
)

type enhanced[E any] interface {
	// Get the enhancement from the enhanced instance.
	getEnhancement() E

	// Set the enhancement of the enhanced instance.
	setEnhancement(E)
}

type enhancedBox[E any] struct {
	instance aastypes.IBox
	enhancement E
}

func (eb *enhancedBox[E]) ModelType(
) aastypes.ModelType {
	return eb.instance.ModelType()
}

func (eb *enhancedBox[E]) DescendOnce(
	action func(aastypes.IClass)bool,
) bool {
	return eb.instance.DescendOnce(action)
}

func (eb *enhancedBox[E]) Descend(
	action func(aastypes.IClass) bool,
) bool {
	return eb.instance.Descend(action)
}

func (eb *enhancedBox[E]) Label(
) string {
	return eb.instance.Label()
}

func (eb *enhancedBox[E]) SetLabel(
	value string,
) {
	eb.instance.SetLabel(value)
}

func (eb *enhancedBox[E]) Color(
) *aastypes.Color {
	return eb.instance.Color()
}

func (eb *enhancedBox[E]) SetColor(
	value *aastypes.Color,
) {
	eb.instance.SetColor(value)
}

func (b *enhancedBox[E]) Describe() string {
	return b.instance.Describe()
}

func (b *enhancedBox[E]) ColorOrDefault() aastypes.Color {
	return b.instance.ColorOrDefault()
}

func (b *enhancedBox[E]) ResolveColor(
	fallback *aastypes.Color,
) aastypes.Color {
	return b.instance.ResolveColor(
		fallback,
	)
}

func (b *enhancedBox[E]) Relabel(
	newLabel string,
) {
	b.instance.Relabel(
		newLabel,
	)
}

func (eb *enhancedBox[E]) getEnhancement(
) E {
	return eb.enhancement
}

func (eb *enhancedBox[E]) setEnhancement(
	value E,
) {
	eb.enhancement = value
}

func wrapBox[E any](
	that aastypes.IBox,
	factory func(aastypes.IClass) (E, bool),
) (result aastypes.IBox) {
	// We assume that we already checked whether `that` has been enhanced
	// in the caller.

	enh, shouldEnhance := factory(that)
	if shouldEnhance {
		result = &enhancedBox[E]{
			instance: that,
			enhancement: enh,
		}
	} else {
		result = that
	}

	return
}

type enhancedBag[E any] struct {
	instance aastypes.IBag
	enhancement E
}

func (eb *enhancedBag[E]) ModelType(
) aastypes.ModelType {
	return eb.instance.ModelType()
}

func (eb *enhancedBag[E]) DescendOnce(
	action func(aastypes.IClass)bool,
) bool {
	return eb.instance.DescendOnce(action)
}

func (eb *enhancedBag[E]) Descend(
	action func(aastypes.IClass) bool,
) bool {
	return eb.instance.Descend(action)
}

func (eb *enhancedBag[E]) Label(
) string {
	return eb.instance.Label()
}

func (eb *enhancedBag[E]) SetLabel(
	value string,
) {
	eb.instance.SetLabel(value)
}

func (eb *enhancedBag[E]) Tags(
) []string {
	return eb.instance.Tags()
}

func (eb *enhancedBag[E]) SetTags(
	value []string,
) {
	eb.instance.SetTags(value)
}

func (b *enhancedBag[E]) Describe() string {
	return b.instance.Describe()
}

func (eb *enhancedBag[E]) getEnhancement(
) E {
	return eb.enhancement
}

func (eb *enhancedBag[E]) setEnhancement(
	value E,
) {
	eb.enhancement = value
}

func wrapBag[E any](
	that aastypes.IBag,
	factory func(aastypes.IClass) (E, bool),
) (result aastypes.IBag) {
	// We assume that we already checked whether `that` has been enhanced
	// in the caller.

	enh, shouldEnhance := factory(that)
	if shouldEnhance {
		result = &enhancedBag[E]{
			instance: that,
			enhancement: enh,
		}
	} else {
		result = that
	}

	return
}

type enhancedContainer[E any] struct {
	instance aastypes.IContainer
	enhancement E
}

func (ec *enhancedContainer[E]) ModelType(
) aastypes.ModelType {
	return ec.instance.ModelType()
}

func (ec *enhancedContainer[E]) DescendOnce(
	action func(aastypes.IClass)bool,
) bool {
	return ec.instance.DescendOnce(action)
}

func (ec *enhancedContainer[E]) Descend(
	action func(aastypes.IClass) bool,
) bool {
	return ec.instance.Descend(action)
}

func (ec *enhancedContainer[E]) Names(
) []string {
	return ec.instance.Names()
}

func (ec *enhancedContainer[E]) SetNames(
	value []string,
) {
	ec.instance.SetNames(value)
}

func (ec *enhancedContainer[E]) Items(
) []aastypes.IItem {
	return ec.instance.Items()
}

func (ec *enhancedContainer[E]) SetItems(
	value []aastypes.IItem,
) {
	ec.instance.SetItems(value)
}

func (ec *enhancedContainer[E]) getEnhancement(
) E {
	return ec.enhancement
}

func (ec *enhancedContainer[E]) setEnhancement(
	value E,
) {
	ec.enhancement = value
}

func wrapContainer[E any](
	that aastypes.IContainer,
	factory func(aastypes.IClass) (E, bool),
) (result aastypes.IContainer) {
	// We assume that we already checked whether `that` has been enhanced
	// in the caller.

	enh, shouldEnhance := factory(that)
	if shouldEnhance {
		result = &enhancedContainer[E]{
			instance: that,
			enhancement: enh,
		}
	} else {
		result = that
	}

	theItems := that.Items()
	for i, v := range theItems {
		// Update in-situ
		theItems[i] = Wrap[E](
			v,
			factory,
		).(aastypes.IItem)
	}

	return
}

// Wrap `that` instance recursively with the enhancement produced by the `factory`.
//
// The factory returns the enhancement, and a boolean "should-enhance". If
// the "should-enhance" is false, `that` instance is not enhance, and we simply
// return it. However, we will still continue to enhance the instances referenced
// by `that` instance recursively.
//
// If `that` instance has been already wrapped, panic.
func Wrap[E any](
	that aastypes.IClass,
	factory func(aastypes.IClass) (E, bool),
) (result aastypes.IClass) {
	_, ok := that.(enhanced[E])
	if ok {
		panic(
			fmt.Sprintf(
				"An instance of %T has been already wrapped: %v",
				that, that,
			),
		)
	}

	switch that.ModelType() {
	case aastypes.ModelTypeBox:
		result = wrapBox[E](
			that.(aastypes.IBox),
			factory,
		)
	case aastypes.ModelTypeBag:
		result = wrapBag[E](
			that.(aastypes.IBag),
			factory,
		)
	case aastypes.ModelTypeContainer:
		result = wrapContainer[E](
			that.(aastypes.IContainer),
			factory,
		)
	default:
		panic(
			fmt.Sprintf(
				"Unexpected model type: %v",
				that.ModelType(),
			),
		)
	}

	return
}

// Retrieve the enhancement from `that` instance.
//
// Return the enhancement, or `ok` false, if `that` instance has not been
// enhanced.
func Unwrap[E any](that aastypes.IClass) (enhancement E, ok bool) {
	var enh enhanced[E]
	enh, ok = that.(enhanced[E])
	if !ok {
		return
	}
	enhancement = enh.getEnhancement()
	return
}

// Retrieve the enhancement from `that` instance.
//
// If `that` instance has not been enhanced yet, panic.
func MustUnwrap[E any](that aastypes.IClass) (enhancement E) {
	var ok bool
	enhancement, ok = Unwrap[E](that)
	if !ok {
		panic(
			fmt.Sprintf(
				"An instance of %T has not been wrapped: %v",
				that, that,
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
