package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	"path"
	aasjsonization "github.com/dummy-works/dummy/jsonization"
	aastypes "github.com/dummy-works/dummy/types"
)

// Load a maximal example of [aastypes.IBox] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBox(
) (result aastypes.IBox) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Box",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BoxFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBox " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBox)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBox at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBox] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBox(
) (result aastypes.IBox) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Box",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BoxFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBox " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBox)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBox at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IBag] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBag(
) (result aastypes.IBag) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Bag",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BagFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBag " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBag)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBag at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBag] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBag(
) (result aastypes.IBag) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Bag",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BagFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBag " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBag)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBag at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalContainer(
) (result aastypes.IContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Container",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalContainer(
) (result aastypes.IContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Container",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
