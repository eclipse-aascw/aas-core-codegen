package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"bytes"
	"fmt"
	aastypes "github.com/dummy-works/dummy/types"
)

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualBox(
	that aastypes.IBox,
	other aastypes.IBox,
) bool {
	thatLabel := that.Label()
	otherLabel := other.Label()
	if thatLabel != otherLabel {
		return false
	}

	thatColor := that.Color()
	otherColor := other.Color()
	if
		(thatColor == nil && otherColor != nil) ||
		(thatColor != nil && otherColor == nil) {
		return false
	}
	if thatColor != nil {
		if *thatColor != *otherColor {
			return false
		}
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualBag(
	that aastypes.IBag,
	other aastypes.IBag,
) bool {
	thatLabel := that.Label()
	otherLabel := other.Label()
	if thatLabel != otherLabel {
		return false
	}

	thatTags := that.Tags()
	otherTags := other.Tags()
	if 
		len(thatTags) !=
		len(otherTags) {
		return false
	}
	for i := range thatTags {
		if thatTags[i] != otherTags[i] {
			return false
		}
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualContainer(
	that aastypes.IContainer,
	other aastypes.IContainer,
) bool {
	thatNames := that.Names()
	otherNames := other.Names()
	if 
		len(thatNames) !=
		len(otherNames) {
		return false
	}
	for i := range thatNames {
		if thatNames[i] != otherNames[i] {
			return false
		}
	}

	thatItems := that.Items()
	otherItems := other.Items()
	if 
		len(thatItems) !=
		len(otherItems) {
		return false
	}
	for i := range thatItems {
		if !DeepEqual(
			thatItems[i],
			otherItems[i],
		) {
			return false
		}
	}

	return true
}

func DeepEqual(
	that aastypes.IClass,
	other aastypes.IClass,
) bool {
	if that.ModelType() != other.ModelType() {
		return false
	}

	switch that.ModelType() {
	case aastypes.ModelTypeBox:
		return deepEqualBox(
			that.(aastypes.IBox),
			other.(aastypes.IBox),
		)
	case aastypes.ModelTypeBag:
		return deepEqualBag(
			that.(aastypes.IBag),
			other.(aastypes.IBag),
		)
	case aastypes.ModelTypeContainer:
		return deepEqualContainer(
			that.(aastypes.IContainer),
			other.(aastypes.IContainer),
		)
	}

	panic(fmt.Sprintf("Unexpected model type: %d", that.ModelType()))
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
