// Package verification allows you to verify model instances.
//
// The main function is [Verify].
//
// Other verification functions (`Verify*`) are left for modularity, in case you want
// to be explicit about the typing in your code. However, in the large majority of
// the cases, you only want to call [Verify].
package verification

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	aascommon "github.com/dummy-works/dummy/common"
	aasreporting "github.com/dummy-works/dummy/reporting"
	aastypes "github.com/dummy-works/dummy/types"
)

// Represent a verification violation.
//
// Implements `error`.
type VerificationError struct{
	Path *aasreporting.Path
	Message string
}

func newVerificationError(message string) *VerificationError {
	return &VerificationError{
		Path: &aasreporting.Path{},
		Message: message,
	}
}

func (ve *VerificationError) Error() string {
	return fmt.Sprintf(
		"%s: %s",
		ve.PathString(),
		ve.Message,
	)
}

// Render the path as a string.
func (ve *VerificationError) PathString() string {
	return aasreporting.ToGolangPath(ve.Path)
}

// Check the for-each with an unconditional early return.
func FirstTextIsNotEmpty(
	texts []string,
) bool {
	for _, text := range texts {
		return aascommon.LenStr(text) > 0
	}
	return true
}

// Check the for-each with an early return in a switch.
func NoNumberIsZero(
	numbers []int64,
) bool {
	for _, number := range numbers {
		switch number {
		case 0:
			return false
		}
	}
	return true
}

// Check the for-range with a variable defined in the body of the loop.
func NoNumberIsMinusOne(
	numbers []int64,
) bool {
	for i := 0; i < len(numbers); i++ {
		number := numbers[i]
		switch number {
		case -1:
			return false
		}
	}
	return true
}

// Check the for-range which does not start at zero.
func NoNumberAfterTheFirstIsOne(
	numbers []int64,
) bool {
	for i := 1; i < len(numbers); i++ {
		switch numbers[i] {
		case 1:
			return false
		}
	}
	return true
}

// Check the for-each which assigns to a variable defined before the loop.
func SumIsSmall(
	numbers []int64,
) bool {
	total := int64(0)
	for _, number := range numbers {
		total = total + number
	}
	return total < 100
}

// Check the nested for-each loops.
func NoItemHasAnEmptyText(
	items []aastypes.IItem,
) bool {
	for _, item := range items {
		texts := item.Texts()
		for _, text := range texts {
			switch text {
			case "":
				return false
			}
		}
	}
	return true
}

// Check the loop variable re-used in a sibling loop.
func IsNeitherThirteenNorUnlucky(
	texts []string,
) bool {
	for _, x := range texts {
		switch x {
		case "thirteen":
			return false
		}
	}
	for _, x := range texts {
		switch x {
		case "unlucky":
			return false
		}
	}
	return true
}

// Check the for-each in a switch branch.
func AlphaHasNoNegativeNumbers(
	kind aastypes.Kind,
	numbers []int64,
) bool {
	switch kind {
	case aastypes.KindAlpha:
		for _, number := range numbers {
			switch number {
			case -2:
				return false
			}
		}
	}
	return true
}

// Verify `that` instance of [aastypes.IItem].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyItem(
	that aastypes.IItem,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	if that.Texts() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: Texts",
			),
		)
		if abort {
			return
		}
	}

	return
}

// Verify `that` instance of [aastypes.ISomething].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifySomething(
	that aastypes.ISomething,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	if !(
		AlphaHasNoNegativeNumbers(
			that.Kind(),
			that.Numbers(),
		)) {
		abort = onError(
			newVerificationError(
				"Alpha has no minus two",),
		)
		if abort {
			return
		}
	}

	if !IsNeitherThirteenNorUnlucky(that.Texts()) {
		abort = onError(
			newVerificationError(
				"Neither thirteen nor unlucky",),
		)
		if abort {
			return
		}
	}

	if !NoItemHasAnEmptyText(that.Items()) {
		abort = onError(
			newVerificationError(
				"No item has an empty text",),
		)
		if abort {
			return
		}
	}

	if !SumIsSmall(that.Numbers()) {
		abort = onError(
			newVerificationError(
				"Sum of numbers is small",),
		)
		if abort {
			return
		}
	}

	if !NoNumberAfterTheFirstIsOne(that.Numbers()) {
		abort = onError(
			newVerificationError(
				"No number after the first is one",),
		)
		if abort {
			return
		}
	}

	if !NoNumberIsMinusOne(that.Numbers()) {
		abort = onError(
			newVerificationError(
				"No number is minus one",),
		)
		if abort {
			return
		}
	}

	if !NoNumberIsZero(that.Numbers()) {
		abort = onError(
			newVerificationError(
				"No number is zero",),
		)
		if abort {
			return
		}
	}

	if !FirstTextIsNotEmpty(that.Texts()) {
		abort = onError(
			newVerificationError(
				"The first text must not be empty",),
		)
		if abort {
			return
		}
	}

	abort = VerifyKind(
		that.Kind(),
		func(err *VerificationError) bool {
			err.Path.PrependName(
				&aasreporting.NameSegment{
					Name: "Kind",
				},
			)
			return onError(err)
		},
	)
	if abort {
		return
	}

	if that.Numbers() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: Numbers",
			),
		)
		if abort {
			return
		}
	}

	if that.Texts() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: Texts",
			),
		)
		if abort {
			return
		}
	}

	if that.Items() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: Items",
			),
		)
		if abort {
			return
		}
	} else {
		for i, v := range that.Items() {
			abort = Verify(
				v,
				func(err *VerificationError) bool {
					err.Path.PrependIndex(
						&aasreporting.IndexSegment{
							Index: i,
						},
					)

					err.Path.PrependName(
						&aasreporting.NameSegment{
							Name: "Items",
						},
					)

					return onError(err)
				},
			)
			if abort {
				return
			}
		}
	}

	return
}

// Verify that `that` is a literal in the valid range
// of Kind.
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyKind(
	that aastypes.Kind,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	if
		that < aastypes.KindAlpha ||
		that > aastypes.KindBeta {
		abort = onError(
			newVerificationError(
				fmt.Sprintf(
					"Invalid literal value for Kind: %v",
					that,
				),
			),
		)
	}

	return
}

// Verify ``that`` instance.
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func Verify(
	that aastypes.IClass,
	onError func(*VerificationError) bool,
) (abort bool) {
	modelType := that.ModelType()
	switch modelType {
	case aastypes.ModelTypeItem:
		abort = VerifyItem(
			that.(aastypes.IItem),
			onError,
		)
	case aastypes.ModelTypeSomething:
		abort = VerifySomething(
			that.(aastypes.ISomething),
			onError,
		)
	default:
		abort = onError(
			newVerificationError(
				fmt.Sprintf(
					"Unexpected model type literal: %v",
					modelType,
				),
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
