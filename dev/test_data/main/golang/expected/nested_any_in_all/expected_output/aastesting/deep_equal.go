package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	aastypes "github.com/dummy-works/dummy/types"
)

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualLangString(
	that aastypes.ILangString,
	other aastypes.ILangString,
) bool {
	thatLanguage := that.Language()
	otherLanguage := other.Language()
	if thatLanguage != otherLanguage {
		return false
	}

	thatText := that.Text()
	otherText := other.Text()
	if thatText != otherText {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualLangStringSet(
	that aastypes.ILangStringSet,
	other aastypes.ILangStringSet,
) bool {
	thatLangStrings := that.LangStrings()
	otherLangStrings := other.LangStrings()
	if 
		len(thatLangStrings) !=
		len(otherLangStrings) {
		return false
	}
	for i := range thatLangStrings {
		if !DeepEqual(
			thatLangStrings[i],
			otherLangStrings[i],
		) {
			return false
		}
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualIecContent(
	that aastypes.IIecContent,
	other aastypes.IIecContent,
) bool {
	thatDefinition := that.Definition()
	otherDefinition := other.Definition()
	if
		(thatDefinition == nil && otherDefinition != nil) ||
		(thatDefinition != nil && otherDefinition == nil) {
		return false
	}
	if thatDefinition != nil {
		if 
			len(thatDefinition) !=
			len(otherDefinition) {
			return false
		}
		for i := range thatDefinition {
			if !DeepEqual(
				thatDefinition[i],
				otherDefinition[i],
			) {
				return false
			}
		}
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualOtherContent(
	that aastypes.IOtherContent,
	other aastypes.IOtherContent,
) bool {
	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSpecification(
	that aastypes.ISpecification,
	other aastypes.ISpecification,
) bool {
	thatContent := that.Content()
	otherContent := other.Content()
	if !DeepEqual(
		thatContent,
		otherContent,
	) {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSomething(
	that aastypes.ISomething,
	other aastypes.ISomething,
) bool {
	thatDefaultLanguage := that.DefaultLanguage()
	otherDefaultLanguage := other.DefaultLanguage()
	if thatDefaultLanguage != otherDefaultLanguage {
		return false
	}

	thatLangStringSets := that.LangStringSets()
	otherLangStringSets := other.LangStringSets()
	if 
		len(thatLangStringSets) !=
		len(otherLangStringSets) {
		return false
	}
	for i := range thatLangStringSets {
		if !DeepEqual(
			thatLangStringSets[i],
			otherLangStringSets[i],
		) {
			return false
		}
	}

	thatSpecifications := that.Specifications()
	otherSpecifications := other.Specifications()
	if
		(thatSpecifications == nil && otherSpecifications != nil) ||
		(thatSpecifications != nil && otherSpecifications == nil) {
		return false
	}
	if thatSpecifications != nil {
		if 
			len(thatSpecifications) !=
			len(otherSpecifications) {
			return false
		}
		for i := range thatSpecifications {
			if !DeepEqual(
				thatSpecifications[i],
				otherSpecifications[i],
			) {
				return false
			}
		}
	}

	return true
}

func DeepEqual(
	that aastypes.IClass,
	other aastypes.IClass,
) bool {
	if that.ModelType() != other.ModelType() {
		return false
	}

	switch that.ModelType() {
	case aastypes.ModelTypeLangString:
		return deepEqualLangString(
			that.(aastypes.ILangString),
			other.(aastypes.ILangString),
		)
	case aastypes.ModelTypeLangStringSet:
		return deepEqualLangStringSet(
			that.(aastypes.ILangStringSet),
			other.(aastypes.ILangStringSet),
		)
	case aastypes.ModelTypeIecContent:
		return deepEqualIecContent(
			that.(aastypes.IIecContent),
			other.(aastypes.IIecContent),
		)
	case aastypes.ModelTypeOtherContent:
		return deepEqualOtherContent(
			that.(aastypes.IOtherContent),
			other.(aastypes.IOtherContent),
		)
	case aastypes.ModelTypeSpecification:
		return deepEqualSpecification(
			that.(aastypes.ISpecification),
			other.(aastypes.ISpecification),
		)
	case aastypes.ModelTypeSomething:
		return deepEqualSomething(
			that.(aastypes.ISomething),
			other.(aastypes.ISomething),
		)
	}

	panic(fmt.Sprintf("Unexpected model type: %d", that.ModelType()))
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
