package types_descend_test

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"path/filepath"
	"testing"
	aastesting "github.com/dummy-works/dummy/aastesting"
)

func TestDescendOnceOnAnInstanceOfLangString(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalLangString()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"LangString",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

func TestDescendOnceOnAnInstanceOfLangStringSet(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalLangStringSet()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"LangStringSet",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

func TestDescendOnceOnAnInstanceOfIecContent(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalIecContent()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"IecContent",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

func TestDescendOnceOnAnInstanceOfOtherContent(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalOtherContent()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"OtherContent",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

func TestDescendOnceOnAnInstanceOfSpecification(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalSpecification()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"Specification",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

func TestDescendOnceOnAnInstanceOfSomething(
	t *testing.T,
) {
	instance := aastesting.MustLoadMaximalSomething()

	expectedPth := filepath.Join(
		aastesting.TestDataDir,
		"DescendOnce",
		"Something",
		"maximal.json.trace",
	)

	onlyOnce := true

	message := compareOrRerecordTrace(
		instance,
		expectedPth,
		onlyOnce,
	)
	if message != nil {
		t.Fatal(*message)
	}
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
