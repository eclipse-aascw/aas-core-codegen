// Package types provides the data structures corresponding to the meta-model.
package types

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	aascommon "github.com/dummy-works/dummy/common"
)

// Represent a value which JSON can carry.
//
// This is, recursively, exactly as JSON itself is defined: a bool, a float64,
// a string, a [JsonArray] of JSON-able values or a [JsonObject] of JSON-able
// values with string keys -- never a nil, and never an infinity or
// a not-a-number, which JSON can not represent at all.
//
// Go has no sum type, so this is an `any`, and what it actually holds is
// checked at run-time -- by the verification, and again by the de/serialization.
type JsonValue = interface{}

// Represent a JSON-able value which is known to be an array.
type JsonArray = []JsonValue

// Represent a JSON-able value which is known to be an object.
type JsonObject = map[string]JsonValue

// Enumerate the model types for faster type switches.
//
// For example, you can use an array of function pointers to
// implement such a switch.
type ModelType int
const (
	ModelTypeSomething ModelType = iota
)

// Represent the most general interface of an AAS model.
type IClass interface {
	// Return the concrete model type at run-time.
	//
	// Use the model type if you want to switch on the concrete model type
	// in efficient manner, as the compiler will most probably implement
	// the switch in form of a jump table.
	ModelType() ModelType

	// Apply the action on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// We do not recurse into the referenced instances.
	//
	// The action is not applied on this instance.
	DescendOnce(action func(IClass) bool) (abort bool)

	// Apply the action recursively on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// The action is not applied on this instance.
	Descend(action func(IClass) bool) (abort bool)
}

type ISomething interface {
	IClass

	Value() JsonValue;

	SetValue(
		value JsonValue,
	);

	Values() JsonArray;

	SetValues(
		value JsonArray,
	);

	Mapping() JsonObject;

	SetMapping(
		value JsonObject,
	);

	MappingWithConstrainedKey() JsonObject;

	SetMappingWithConstrainedKey(
		value JsonObject,
	);

	OptionalValue() JsonValue;

	SetOptionalValue(
		value JsonValue,
	);

	OptionalValues() JsonArray;

	SetOptionalValues(
		value JsonArray,
	);

	OptionalMapping() JsonObject;

	SetOptionalMapping(
		value JsonObject,
	);
}

// Check whether the instance corresponds to [aastypes.ISomething]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSomething(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSomething
	return
}

// Implements ISomething.
type Something struct {
	value JsonValue
	values JsonArray
	mapping JsonObject
	mappingWithConstrainedKey JsonObject
	optionalValue JsonValue
	optionalValues JsonArray
	optionalMapping JsonObject
}

func (s *Something) Value(
) JsonValue {
	return s.value
}

func (s *Something) SetValue(
	value JsonValue,
) {
	s.value = value
}

func (s *Something) Values(
) JsonArray {
	return s.values
}

func (s *Something) SetValues(
	value JsonArray,
) {
	s.values = value
}

func (s *Something) Mapping(
) JsonObject {
	return s.mapping
}

func (s *Something) SetMapping(
	value JsonObject,
) {
	s.mapping = value
}

func (s *Something) MappingWithConstrainedKey(
) JsonObject {
	return s.mappingWithConstrainedKey
}

func (s *Something) SetMappingWithConstrainedKey(
	value JsonObject,
) {
	s.mappingWithConstrainedKey = value
}

func (s *Something) OptionalValue(
) JsonValue {
	return s.optionalValue
}

func (s *Something) SetOptionalValue(
	value JsonValue,
) {
	s.optionalValue = value
}

func (s *Something) OptionalValues(
) JsonArray {
	return s.optionalValues
}

func (s *Something) SetOptionalValues(
	value JsonArray,
) {
	s.optionalValues = value
}

func (s *Something) OptionalMapping(
) JsonObject {
	return s.optionalMapping
}

func (s *Something) SetOptionalMapping(
	value JsonObject,
) {
	s.optionalMapping = value
}

func (s *Something) ModelType(
) ModelType {
	return ModelTypeSomething
}

// Apply the action on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on s.
func (s *Something) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on s.
func (s *Something) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Something with
// the given properties.
func NewSomething(
	value JsonValue,
	values JsonArray,
	mapping JsonObject,
	mappingWithConstrainedKey JsonObject,
) *Something {
	return &Something{
		value: value,
		values: values,
		mapping: mapping,
		mappingWithConstrainedKey: mappingWithConstrainedKey,
		optionalValue: nil,
		optionalValues: nil,
		optionalMapping: nil,
	}
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
