package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	"path"
	aasjsonization "github.com/dummy-works/dummy/jsonization"
	aastypes "github.com/dummy-works/dummy/types"
)

// Load a maximal example of [aastypes.ILeaf] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLeaf(
) (result aastypes.ILeaf) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Leaf",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LeafFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILeaf " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILeaf)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILeaf at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILeaf] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLeaf(
) (result aastypes.ILeaf) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Leaf",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LeafFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILeaf " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILeaf)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILeaf at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IOrderedContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalOrderedContainer(
) (result aastypes.IOrderedContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"OrderedContainer",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OrderedContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOrderedContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOrderedContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOrderedContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IOrderedContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalOrderedContainer(
) (result aastypes.IOrderedContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"OrderedContainer",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OrderedContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOrderedContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOrderedContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOrderedContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IUnorderedContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalUnorderedContainer(
) (result aastypes.IUnorderedContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"UnorderedContainer",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.UnorderedContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IUnorderedContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IUnorderedContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IUnorderedContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IUnorderedContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalUnorderedContainer(
) (result aastypes.IUnorderedContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"UnorderedContainer",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.UnorderedContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IUnorderedContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IUnorderedContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IUnorderedContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IGlobalAttribute] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalGlobalAttribute(
) (result aastypes.IGlobalAttribute) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"GlobalAttribute",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.GlobalAttributeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IGlobalAttribute " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IGlobalAttribute)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IGlobalAttribute at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IGlobalAttribute] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalGlobalAttribute(
) (result aastypes.IGlobalAttribute) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"GlobalAttribute",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.GlobalAttributeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IGlobalAttribute " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IGlobalAttribute)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IGlobalAttribute at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILocalAttribute] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLocalAttribute(
) (result aastypes.ILocalAttribute) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LocalAttribute",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LocalAttributeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILocalAttribute " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILocalAttribute)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILocalAttribute at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILocalAttribute] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLocalAttribute(
) (result aastypes.ILocalAttribute) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LocalAttribute",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LocalAttributeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILocalAttribute " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILocalAttribute)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILocalAttribute at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IAttributeOperand] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalAttributeOperand(
) (result aastypes.IAttributeOperand) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AttributeOperand",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AttributeOperandFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAttributeOperand " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAttributeOperand)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAttributeOperand at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IAttributeOperand] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalAttributeOperand(
) (result aastypes.IAttributeOperand) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AttributeOperand",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AttributeOperandFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAttributeOperand " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAttributeOperand)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAttributeOperand at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IStringLiteral] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalStringLiteral(
) (result aastypes.IStringLiteral) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"StringLiteral",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.StringLiteralFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IStringLiteral " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IStringLiteral)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IStringLiteral at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IStringLiteral] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalStringLiteral(
) (result aastypes.IStringLiteral) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"StringLiteral",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.StringLiteralFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IStringLiteral " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IStringLiteral)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IStringLiteral at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.INumberLiteral] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalNumberLiteral(
) (result aastypes.INumberLiteral) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"NumberLiteral",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.NumberLiteralFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of INumberLiteral " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.INumberLiteral)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of INumberLiteral at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.INumberLiteral] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalNumberLiteral(
) (result aastypes.INumberLiteral) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"NumberLiteral",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.NumberLiteralFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of INumberLiteral " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.INumberLiteral)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of INumberLiteral at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISomething] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSomething(
) (result aastypes.ISomething) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Something",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SomethingFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISomething " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISomething)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISomething at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISomething] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSomething(
) (result aastypes.ISomething) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Something",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SomethingFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISomething " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISomething)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISomething at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
