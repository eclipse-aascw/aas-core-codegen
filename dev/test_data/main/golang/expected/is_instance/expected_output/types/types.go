// Package types provides the data structures corresponding to the meta-model.
package types

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
)

// Enumerate the model types for faster type switches.
//
// For example, you can use an array of function pointers to
// implement such a switch.
type ModelType int
const (
	ModelTypeLeaf ModelType = iota
	ModelTypeOrderedContainer
	ModelTypeUnorderedContainer
	ModelTypeGlobalAttribute
	ModelTypeLocalAttribute
	ModelTypeAttributeOperand
	ModelTypeStringLiteral
	ModelTypeNumberLiteral
	ModelTypeSomething
)

// Represent the most general interface of an AAS model.
type IClass interface {
	// Return the concrete model type at run-time.
	//
	// Use the model type if you want to switch on the concrete model type
	// in efficient manner, as the compiler will most probably implement
	// the switch in form of a jump table.
	ModelType() ModelType

	// Apply the action on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// We do not recurse into the referenced instances.
	//
	// The action is not applied on this instance.
	DescendOnce(action func(IClass) bool) (abort bool)

	// Apply the action recursively on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// The action is not applied on this instance.
	Descend(action func(IClass) bool) (abort bool)
}

type IElement interface {
	IClass

	Identifier() string;

	SetIdentifier(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.IElement]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsElement(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeLeaf:
		ok = true
	case ModelTypeOrderedContainer:
		ok = true
	case ModelTypeUnorderedContainer:
		ok = true
	}
	return
}

type ILeaf interface {
	IElement

	Text() string;

	SetText(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.ILeaf]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsLeaf(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeLeaf
	return
}

// Implements ILeaf.
type Leaf struct {
	identifier string
	text string
}

func (l *Leaf) Identifier(
) string {
	return l.identifier
}

func (l *Leaf) SetIdentifier(
	value string,
) {
	l.identifier = value
}

func (l *Leaf) Text(
) string {
	return l.text
}

func (l *Leaf) SetText(
	value string,
) {
	l.text = value
}

func (l *Leaf) ModelType(
) ModelType {
	return ModelTypeLeaf
}

// Apply the action on the instances referenced from l.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on l.
func (l *Leaf) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from l.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on l.
func (l *Leaf) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Leaf with
// the given properties.
func NewLeaf(
	identifier string,
	text string,
) *Leaf {
	return &Leaf{
		identifier: identifier,
		text: text,
	}
}

type IContainer interface {
	IElement

	Children() []IElement;

	SetChildren(
		value []IElement,
	);
}

// Check whether the instance corresponds to [aastypes.IContainer]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsContainer(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeOrderedContainer:
		ok = true
	case ModelTypeUnorderedContainer:
		ok = true
	}
	return
}

type IOrderedContainer interface {
	IContainer

	IsSorted() bool;

	SetIsSorted(
		value bool,
	);
}

// Check whether the instance corresponds to [aastypes.IOrderedContainer]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsOrderedContainer(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeOrderedContainer
	return
}

// Implements IOrderedContainer.
type OrderedContainer struct {
	identifier string
	children []IElement
	isSorted bool
}

func (oc *OrderedContainer) Identifier(
) string {
	return oc.identifier
}

func (oc *OrderedContainer) SetIdentifier(
	value string,
) {
	oc.identifier = value
}

func (oc *OrderedContainer) Children(
) []IElement {
	return oc.children
}

func (oc *OrderedContainer) SetChildren(
	value []IElement,
) {
	oc.children = value
}

func (oc *OrderedContainer) IsSorted(
) bool {
	return oc.isSorted
}

func (oc *OrderedContainer) SetIsSorted(
	value bool,
) {
	oc.isSorted = value
}

func (oc *OrderedContainer) ModelType(
) ModelType {
	return ModelTypeOrderedContainer
}

// Apply the action on the instances referenced from oc.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on oc.
func (oc *OrderedContainer) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range oc.children {
		abort = action(v);
		if abort {
			return
		}
	}

	return
}

// Apply the action recursively on the instances referenced from oc.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on oc.
func (oc *OrderedContainer) Descend(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range oc.children {
		abort = action(v);
		if abort {
			return
		}

		abort = v.Descend(
			action,
		);
		if abort {
			return
		}
	}

	return
}

// Create a new instance of OrderedContainer with
// the given properties.
func NewOrderedContainer(
	identifier string,
	children []IElement,
	isSorted bool,
) *OrderedContainer {
	return &OrderedContainer{
		identifier: identifier,
		children: children,
		isSorted: isSorted,
	}
}

type IUnorderedContainer interface {
	IContainer
}

// Check whether the instance corresponds to [aastypes.IUnorderedContainer]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsUnorderedContainer(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeUnorderedContainer
	return
}

// Implements IUnorderedContainer.
type UnorderedContainer struct {
	identifier string
	children []IElement
}

func (uc *UnorderedContainer) Identifier(
) string {
	return uc.identifier
}

func (uc *UnorderedContainer) SetIdentifier(
	value string,
) {
	uc.identifier = value
}

func (uc *UnorderedContainer) Children(
) []IElement {
	return uc.children
}

func (uc *UnorderedContainer) SetChildren(
	value []IElement,
) {
	uc.children = value
}

func (uc *UnorderedContainer) ModelType(
) ModelType {
	return ModelTypeUnorderedContainer
}

// Apply the action on the instances referenced from uc.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on uc.
func (uc *UnorderedContainer) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range uc.children {
		abort = action(v);
		if abort {
			return
		}
	}

	return
}

// Apply the action recursively on the instances referenced from uc.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on uc.
func (uc *UnorderedContainer) Descend(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range uc.children {
		abort = action(v);
		if abort {
			return
		}

		abort = v.Descend(
			action,
		);
		if abort {
			return
		}
	}

	return
}

// Create a new instance of UnorderedContainer with
// the given properties.
func NewUnorderedContainer(
	identifier string,
	children []IElement,
) *UnorderedContainer {
	return &UnorderedContainer{
		identifier: identifier,
		children: children,
	}
}

type IGlobalAttribute interface {
	IClass

	Kind() string;

	SetKind(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.IGlobalAttribute]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsGlobalAttribute(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeGlobalAttribute
	return
}

// Implements IGlobalAttribute.
type GlobalAttribute struct {
	kind string
}

func (ga *GlobalAttribute) Kind(
) string {
	return ga.kind
}

func (ga *GlobalAttribute) SetKind(
	value string,
) {
	ga.kind = value
}

func (ga *GlobalAttribute) ModelType(
) ModelType {
	return ModelTypeGlobalAttribute
}

// Apply the action on the instances referenced from ga.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on ga.
func (ga *GlobalAttribute) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from ga.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on ga.
func (ga *GlobalAttribute) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of GlobalAttribute with
// the given properties.
func NewGlobalAttribute(
	kind string,
) *GlobalAttribute {
	return &GlobalAttribute{
		kind: kind,
	}
}

type ILocalAttribute interface {
	IClass

	Name() string;

	SetName(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.ILocalAttribute]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsLocalAttribute(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeLocalAttribute
	return
}

// Implements ILocalAttribute.
type LocalAttribute struct {
	name string
}

func (la *LocalAttribute) Name(
) string {
	return la.name
}

func (la *LocalAttribute) SetName(
	value string,
) {
	la.name = value
}

func (la *LocalAttribute) ModelType(
) ModelType {
	return ModelTypeLocalAttribute
}

// Apply the action on the instances referenced from la.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on la.
func (la *LocalAttribute) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from la.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on la.
func (la *LocalAttribute) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of LocalAttribute with
// the given properties.
func NewLocalAttribute(
	name string,
) *LocalAttribute {
	return &LocalAttribute{
		name: name,
	}
}

type attributeItemValueKind int

const (
	attributeItemValueKindGlobalAttribute attributeItemValueKind = iota
	attributeItemValueKindLocalAttribute
)

// Represent a union of [IGlobalAttribute] and [ILocalAttribute].
type AttributeItem struct {
	valueKind attributeItemValueKind
	asGlobalAttribute IGlobalAttribute
	asLocalAttribute ILocalAttribute
}

// Wrap `that` as an instance of [AttributeItem].
func NewAttributeItemFromGlobalAttribute(that IGlobalAttribute) *AttributeItem {
	return &AttributeItem{
		valueKind: attributeItemValueKindGlobalAttribute,
		asGlobalAttribute: that,
	}
}

// Wrap `that` as an instance of [AttributeItem].
func NewAttributeItemFromLocalAttribute(that ILocalAttribute) *AttributeItem {
	return &AttributeItem{
		valueKind: attributeItemValueKindLocalAttribute,
		asLocalAttribute: that,
	}
}

// Get the underlying instance regardless of the concrete case.
func (a *AttributeItem) Underlying() IClass {
	switch a.valueKind {
	case attributeItemValueKindGlobalAttribute:
		return a.asGlobalAttribute
	case attributeItemValueKindLocalAttribute:
		return a.asLocalAttribute
	default:
		panic(
			fmt.Sprintf(
				"Unexpected value kind: %v",
				a.valueKind,
			),
		)
	}
}

// Wrap `that` as an instance of [AttributeItem] based on its run-time type.
func AttributeItemFromUnderlying(that IClass) *AttributeItem {
	switch that.ModelType() {
	case ModelTypeGlobalAttribute:
		return NewAttributeItemFromGlobalAttribute(
			that.(IGlobalAttribute),
		)
	case ModelTypeLocalAttribute:
		return NewAttributeItemFromLocalAttribute(
			that.(ILocalAttribute),
		)
	default:
		panic(
			fmt.Sprintf(
				"Unexpected run-time type for the union AttributeItem: %T",
				that,
			),
		)
	}
}

// Wrap `that` as an instance of [AttributeItem] based on its run-time type.
//
// This is the method-level counterpart of [AttributeItemFromUnderlying], needed
// so that a generic function constrained to a self-referential union
// interface can re-wrap a transformed value without knowing the concrete
// union type at compile time.
func (a *AttributeItem) WithUnderlying(that IClass) *AttributeItem {
	return AttributeItemFromUnderlying(that)
}

type IAttributeOperand interface {
	IClass

	Attribute() *AttributeItem;

	SetAttribute(
		value *AttributeItem,
	);
}

// Check whether the instance corresponds to [aastypes.IAttributeOperand]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsAttributeOperand(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeAttributeOperand
	return
}

// Implements IAttributeOperand.
type AttributeOperand struct {
	attribute *AttributeItem
}

func (ao *AttributeOperand) Attribute(
) *AttributeItem {
	return ao.attribute
}

func (ao *AttributeOperand) SetAttribute(
	value *AttributeItem,
) {
	ao.attribute = value
}

func (ao *AttributeOperand) ModelType(
) ModelType {
	return ModelTypeAttributeOperand
}

// Apply the action on the instances referenced from ao.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on ao.
func (ao *AttributeOperand) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		ao.attribute.Underlying(),
	)
	if abort {
		return
	}

	return
}

// Apply the action recursively on the instances referenced from ao.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on ao.
func (ao *AttributeOperand) Descend(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		ao.attribute.Underlying(),
	)
	if abort {
		return
	}
	abort = ao.attribute.Underlying().Descend(
		action,
	)
	if abort {
		return
	}

	return
}

// Create a new instance of AttributeOperand with
// the given properties.
func NewAttributeOperand(
	attribute *AttributeItem,
) *AttributeOperand {
	return &AttributeOperand{
		attribute: attribute,
	}
}

type IStringLiteral interface {
	IClass

	Text() string;

	SetText(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.IStringLiteral]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsStringLiteral(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeStringLiteral
	return
}

// Implements IStringLiteral.
type StringLiteral struct {
	text string
}

func (sl *StringLiteral) Text(
) string {
	return sl.text
}

func (sl *StringLiteral) SetText(
	value string,
) {
	sl.text = value
}

func (sl *StringLiteral) ModelType(
) ModelType {
	return ModelTypeStringLiteral
}

// Apply the action on the instances referenced from sl.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on sl.
func (sl *StringLiteral) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from sl.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on sl.
func (sl *StringLiteral) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of StringLiteral with
// the given properties.
func NewStringLiteral(
	text string,
) *StringLiteral {
	return &StringLiteral{
		text: text,
	}
}

type INumberLiteral interface {
	IClass

	Number() float64;

	SetNumber(
		value float64,
	);
}

// Check whether the instance corresponds to [aastypes.INumberLiteral]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsNumberLiteral(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeNumberLiteral
	return
}

// Implements INumberLiteral.
type NumberLiteral struct {
	number float64
}

func (nl *NumberLiteral) Number(
) float64 {
	return nl.number
}

func (nl *NumberLiteral) SetNumber(
	value float64,
) {
	nl.number = value
}

func (nl *NumberLiteral) ModelType(
) ModelType {
	return ModelTypeNumberLiteral
}

// Apply the action on the instances referenced from nl.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on nl.
func (nl *NumberLiteral) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from nl.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on nl.
func (nl *NumberLiteral) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of NumberLiteral with
// the given properties.
func NewNumberLiteral(
	number float64,
) *NumberLiteral {
	return &NumberLiteral{
		number: number,
	}
}

type stringValueValueKind int

const (
	stringValueValueKindStringLiteral stringValueValueKind = iota
	stringValueValueKindAttributeOperand
)

// Represent a union of [IStringLiteral] and [IAttributeOperand].
type StringValue struct {
	valueKind stringValueValueKind
	asStringLiteral IStringLiteral
	asAttributeOperand IAttributeOperand
}

// Wrap `that` as an instance of [StringValue].
func NewStringValueFromStringLiteral(that IStringLiteral) *StringValue {
	return &StringValue{
		valueKind: stringValueValueKindStringLiteral,
		asStringLiteral: that,
	}
}

// Wrap `that` as an instance of [StringValue].
func NewStringValueFromAttributeOperand(that IAttributeOperand) *StringValue {
	return &StringValue{
		valueKind: stringValueValueKindAttributeOperand,
		asAttributeOperand: that,
	}
}

// Get the underlying instance regardless of the concrete case.
func (s *StringValue) Underlying() IClass {
	switch s.valueKind {
	case stringValueValueKindStringLiteral:
		return s.asStringLiteral
	case stringValueValueKindAttributeOperand:
		return s.asAttributeOperand
	default:
		panic(
			fmt.Sprintf(
				"Unexpected value kind: %v",
				s.valueKind,
			),
		)
	}
}

// Wrap `that` as an instance of [StringValue] based on its run-time type.
func StringValueFromUnderlying(that IClass) *StringValue {
	switch that.ModelType() {
	case ModelTypeStringLiteral:
		return NewStringValueFromStringLiteral(
			that.(IStringLiteral),
		)
	case ModelTypeAttributeOperand:
		return NewStringValueFromAttributeOperand(
			that.(IAttributeOperand),
		)
	default:
		panic(
			fmt.Sprintf(
				"Unexpected run-time type for the union StringValue: %T",
				that,
			),
		)
	}
}

// Wrap `that` as an instance of [StringValue] based on its run-time type.
//
// This is the method-level counterpart of [StringValueFromUnderlying], needed
// so that a generic function constrained to a self-referential union
// interface can re-wrap a transformed value without knowing the concrete
// union type at compile time.
func (s *StringValue) WithUnderlying(that IClass) *StringValue {
	return StringValueFromUnderlying(that)
}

type valueValueKind int

const (
	valueValueKindStringLiteral valueValueKind = iota
	valueValueKindAttributeOperand
	valueValueKindNumberLiteral
)

// Represent a union of [IStringLiteral], [IAttributeOperand] and [INumberLiteral].
type Value struct {
	valueKind valueValueKind
	asStringLiteral IStringLiteral
	asAttributeOperand IAttributeOperand
	asNumberLiteral INumberLiteral
}

// Wrap `that` as an instance of [Value].
func NewValueFromStringLiteral(that IStringLiteral) *Value {
	return &Value{
		valueKind: valueValueKindStringLiteral,
		asStringLiteral: that,
	}
}

// Wrap `that` as an instance of [Value].
func NewValueFromAttributeOperand(that IAttributeOperand) *Value {
	return &Value{
		valueKind: valueValueKindAttributeOperand,
		asAttributeOperand: that,
	}
}

// Wrap `that` as an instance of [Value].
func NewValueFromNumberLiteral(that INumberLiteral) *Value {
	return &Value{
		valueKind: valueValueKindNumberLiteral,
		asNumberLiteral: that,
	}
}

// Get the underlying instance regardless of the concrete case.
func (v *Value) Underlying() IClass {
	switch v.valueKind {
	case valueValueKindStringLiteral:
		return v.asStringLiteral
	case valueValueKindAttributeOperand:
		return v.asAttributeOperand
	case valueValueKindNumberLiteral:
		return v.asNumberLiteral
	default:
		panic(
			fmt.Sprintf(
				"Unexpected value kind: %v",
				v.valueKind,
			),
		)
	}
}

// Wrap `that` as an instance of [Value] based on its run-time type.
func ValueFromUnderlying(that IClass) *Value {
	switch that.ModelType() {
	case ModelTypeStringLiteral:
		return NewValueFromStringLiteral(
			that.(IStringLiteral),
		)
	case ModelTypeAttributeOperand:
		return NewValueFromAttributeOperand(
			that.(IAttributeOperand),
		)
	case ModelTypeNumberLiteral:
		return NewValueFromNumberLiteral(
			that.(INumberLiteral),
		)
	default:
		panic(
			fmt.Sprintf(
				"Unexpected run-time type for the union Value: %T",
				that,
			),
		)
	}
}

// Wrap `that` as an instance of [Value] based on its run-time type.
//
// This is the method-level counterpart of [ValueFromUnderlying], needed
// so that a generic function constrained to a self-referential union
// interface can re-wrap a transformed value without knowing the concrete
// union type at compile time.
func (v *Value) WithUnderlying(that IClass) *Value {
	return ValueFromUnderlying(that)
}

type ISomething interface {
	IClass

	Root() IElement;

	SetRoot(
		value IElement,
	);

	OptionalElement() IElement;

	SetOptionalElement(
		value IElement,
	);

	Value() *Value;

	SetValue(
		value *Value,
	);

	Values() []*Value;

	SetValues(
		value []*Value,
	);
}

// Check whether the instance corresponds to [aastypes.ISomething]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSomething(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSomething
	return
}

// Implements ISomething.
type Something struct {
	root IElement
	optionalElement IElement
	value *Value
	values []*Value
}

func (s *Something) Root(
) IElement {
	return s.root
}

func (s *Something) SetRoot(
	value IElement,
) {
	s.root = value
}

func (s *Something) OptionalElement(
) IElement {
	return s.optionalElement
}

func (s *Something) SetOptionalElement(
	value IElement,
) {
	s.optionalElement = value
}

func (s *Something) Value(
) *Value {
	return s.value
}

func (s *Something) SetValue(
	value *Value,
) {
	s.value = value
}

func (s *Something) Values(
) []*Value {
	return s.values
}

func (s *Something) SetValues(
	value []*Value,
) {
	s.values = value
}

func (s *Something) ModelType(
) ModelType {
	return ModelTypeSomething
}

// Apply the action on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on s.
func (s *Something) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		s.root,
	)
	if abort {
		return
	}

	if s.optionalElement != nil {
		abort = action(
			s.optionalElement,
		)
		if abort {
			return
		}
	}

	abort = action(
		s.value.Underlying(),
	)
	if abort {
		return
	}

	for _, v := range s.values {
		abort = action(v.Underlying());
		if abort {
			return
		}
	}

	return
}

// Apply the action recursively on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on s.
func (s *Something) Descend(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		s.root,
	)
	if abort {
		return
	}
	abort = s.root.Descend(
		action,
	)
	if abort {
		return
	}

	if s.optionalElement != nil {
		abort = action(
			s.optionalElement,
		)
		if abort {
			return
		}
		abort = s.optionalElement.Descend(
			action,
		)
		if abort {
			return
		}
	}

	abort = action(
		s.value.Underlying(),
	)
	if abort {
		return
	}
	abort = s.value.Underlying().Descend(
		action,
	)
	if abort {
		return
	}

	for _, v := range s.values {
		abort = action(v.Underlying());
		if abort {
			return
		}

		abort = v.Underlying().Descend(
			action,
		);
		if abort {
			return
		}
	}

	return
}

// Create a new instance of Something with
// the given properties.
func NewSomething(
	root IElement,
	value *Value,
	values []*Value,
) *Something {
	return &Something{
		root: root,
		value: value,
		values: values,
		optionalElement: nil,
	}
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
