"""Test the verification of instances."""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


# pylint: disable=missing-docstring


import json
import unittest


import dummy.jsonization as aas_jsonization
import dummy.verification as aas_verification


import tests.common


class TestVerificationOfValidInstances(unittest.TestCase):
    def test_leaf(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Leaf'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.leaf_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_ordered_container(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'OrderedContainer'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.ordered_container_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_unordered_container(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'UnorderedContainer'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.unordered_container_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_global_attribute(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'GlobalAttribute'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.global_attribute_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_local_attribute(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'LocalAttribute'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.local_attribute_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_attribute_operand(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'AttributeOperand'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.attribute_operand_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_string_literal(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'StringLiteral'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.string_literal_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_number_literal(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'NumberLiteral'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.number_literal_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_something(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Something'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.something_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )


class TestVerificationOfInvalidInstances(unittest.TestCase):
    def test_leaf(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Leaf').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.leaf_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_ordered_container(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'OrderedContainer').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.ordered_container_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_unordered_container(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'UnorderedContainer').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.unordered_container_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_global_attribute(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'GlobalAttribute').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.global_attribute_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_local_attribute(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'LocalAttribute').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.local_attribute_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_attribute_operand(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'AttributeOperand').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.attribute_operand_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_string_literal(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'StringLiteral').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.string_literal_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_number_literal(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'NumberLiteral').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.number_literal_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_something(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Something').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.something_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)


if __name__ == "__main__":
    unittest.main()


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
