"""
Verify that the instances of the meta-model satisfy the invariants.

Here is an example how to verify an instance of :py:class:`dummy.types.Something`:

.. code-block::

    import dummy.types as aas_types
    import dummy.verification as aas_verification

    an_instance = aas_types.Something(
        # ... some constructor arguments ...
    )

    for error in aas_verification.verify(an_instance):
        print(f"{error.cause} at: {error.path}")
"""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


import collections.abc
import math
import re
import struct
import sys
from typing import (
    Any,
    Callable,
    Iterable,
    Iterator,
    List,
    Mapping,
    Optional,
    Pattern,
    Sequence,
    Set,
    Union
)

if sys.version_info >= (3, 8):
    from typing import Final
else:
    from typing_extensions import Final

from dummy import (
    constants as aas_constants,
    reporting as aas_reporting,
    types as aas_types,
)


PropertySegment = aas_reporting.PropertySegment
IndexSegment = aas_reporting.IndexSegment
KeySegment = aas_reporting.KeySegment
Segment = aas_reporting.Segment
Path = aas_reporting.Path

Error = aas_reporting.Error


class _Transformer(
        aas_types.AbstractTransformer[
            Iterator[Error]
        ]
):
    # noinspection PyMethodMayBeStatic
    def transform_something(
            self,
            that: aas_types.Something
    ) -> Iterator[Error]:
        for error in verify_constrainedbool(that.some_bool):
            error.path._prepend(
                PropertySegment(
                    that,
                    'some_bool'
                )
            )
            yield error

        for error in verify_positiveint(that.some_int):
            error.path._prepend(
                PropertySegment(
                    that,
                    'some_int'
                )
            )
            yield error

        for error in verify_positivefloat(that.some_float):
            error.path._prepend(
                PropertySegment(
                    that,
                    'some_float'
                )
            )
            yield error

        for error in verify_nonemptystring(that.some_string):
            error.path._prepend(
                PropertySegment(
                    that,
                    'some_string'
                )
            )
            yield error

        for error in verify_nonemptybytes(that.some_bytes):
            error.path._prepend(
                PropertySegment(
                    that,
                    'some_bytes'
                )
            )
            yield error


_TRANSFORMER = _Transformer()


def verify(
        that: aas_types.Class
) -> Iterator[Error]:
    """
    Verify the constraints of :paramref:`that` recursively.

    :param that: instance whose constraints we want to verify
    :yield: constraint violations
    """
    yield from _TRANSFORMER.transform(that)


def verify_constrainedbool(
        that: bool
) -> Iterator[Error]:
    """Verify the constraints of :paramref:`that`."""
    if not that:
        yield Error(
            'Always true'
        )


def verify_positiveint(
        that: int
) -> Iterator[Error]:
    """Verify the constraints of :paramref:`that`."""
    if not (that > 0):
        yield Error(
            'Larger than zero'
        )


def verify_positivefloat(
        that: float
) -> Iterator[Error]:
    """Verify the constraints of :paramref:`that`."""
    if not (that > 0.0):
        yield Error(
            'Larger than zero'
        )


def verify_nonemptystring(
        that: str
) -> Iterator[Error]:
    """Verify the constraints of :paramref:`that`."""
    if not (len(that) > 0):
        yield Error(
            'At least one character'
        )


def verify_nonemptybytes(
        that: bytes
) -> Iterator[Error]:
    """Verify the constraints of :paramref:`that`."""
    if not (len(that) > 0):
        yield Error(
            'At least one byte'
        )


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
