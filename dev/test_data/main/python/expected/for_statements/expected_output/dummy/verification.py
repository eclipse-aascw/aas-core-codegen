"""
Verify that the instances of the meta-model satisfy the invariants.

Here is an example how to verify an instance of :py:class:`dummy.types.Item`:

.. code-block::

    import dummy.types as aas_types
    import dummy.verification as aas_verification

    an_instance = aas_types.Item(
        # ... some constructor arguments ...
    )

    for error in aas_verification.verify(an_instance):
        print(f"{error.cause} at: {error.path}")
"""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


import collections.abc
import math
import re
import struct
import sys
from typing import (
    Any,
    Callable,
    Iterable,
    Iterator,
    List,
    Mapping,
    Optional,
    Pattern,
    Sequence,
    Set,
    Union
)

if sys.version_info >= (3, 8):
    from typing import Final
else:
    from typing_extensions import Final

from dummy import (
    constants as aas_constants,
    reporting as aas_reporting,
    types as aas_types,
)


PropertySegment = aas_reporting.PropertySegment
IndexSegment = aas_reporting.IndexSegment
KeySegment = aas_reporting.KeySegment
Segment = aas_reporting.Segment
Path = aas_reporting.Path

Error = aas_reporting.Error


def first_text_is_not_empty(
    texts: List[str]
) -> bool:
    """Check the for-each with an unconditional early return."""
    # pylint: disable=all
    for text in texts:
        return len(text) > 0
    return True


def no_number_is_zero(
    numbers: List[int]
) -> bool:
    """Check the for-each with an early return in a switch."""
    # pylint: disable=all
    for number in numbers:
        if number == 0:
            return False
    return True


def no_number_is_minus_one(
    numbers: List[int]
) -> bool:
    """
    Check the for-range with a variable defined in the body of the loop.
    """
    # pylint: disable=all
    for i in range(0, len(numbers)):
        number = numbers[i]
        if number == -1:
            return False
    return True


def no_number_after_the_first_is_one(
    numbers: List[int]
) -> bool:
    """Check the for-range which does not start at zero."""
    # pylint: disable=all
    for i in range(1, len(numbers)):
        if numbers[i] == 1:
            return False
    return True


def sum_is_small(
    numbers: List[int]
) -> bool:
    """
    Check the for-each which assigns to a variable defined before the loop.
    """
    # pylint: disable=all
    total = 0
    for number in numbers:
        total = total + number
    return total < 100


def no_item_has_an_empty_text(
    items: List[aas_types.Item]
) -> bool:
    """Check the nested for-each loops."""
    # pylint: disable=all
    for item in items:
        texts = item.texts
        for text in texts:
            if text == '':
                return False
    return True


def is_neither_thirteen_nor_unlucky(
    texts: List[str]
) -> bool:
    """Check the loop variable re-used in a sibling loop."""
    # pylint: disable=all
    for x in texts:
        if x == 'thirteen':
            return False
    for x in texts:
        if x == 'unlucky':
            return False
    return True


def alpha_has_no_negative_numbers(
    kind: aas_types.Kind,
    numbers: List[int]
) -> bool:
    """Check the for-each in a switch branch."""
    # pylint: disable=all
    if kind == aas_types.Kind.ALPHA:
        for number in numbers:
            if number == -2:
                return False
    return True


class _Transformer(
        aas_types.AbstractTransformer[
            Iterator[Error]
        ]
):
    # noinspection PyMethodMayBeStatic
    def transform_item(
            self,
            that: aas_types.Item
    ) -> Iterator[Error]:
        # No verification has been defined for Item.
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    # noinspection PyMethodMayBeStatic
    def transform_something(
            self,
            that: aas_types.Something
    ) -> Iterator[Error]:
        if not (
            alpha_has_no_negative_numbers(
                that.kind,
                that.numbers
            )
        ):
            yield Error(
                'Alpha has no minus two'
            )

        if not is_neither_thirteen_nor_unlucky(that.texts):
            yield Error(
                'Neither thirteen nor unlucky'
            )

        if not no_item_has_an_empty_text(that.items):
            yield Error(
                'No item has an empty text'
            )

        if not sum_is_small(that.numbers):
            yield Error(
                'Sum of numbers is small'
            )

        if not no_number_after_the_first_is_one(that.numbers):
            yield Error(
                'No number after the first is one'
            )

        if not no_number_is_minus_one(that.numbers):
            yield Error(
                'No number is minus one'
            )

        if not no_number_is_zero(that.numbers):
            yield Error(
                'No number is zero'
            )

        if not first_text_is_not_empty(that.texts):
            yield Error(
                'The first text must not be empty'
            )

        for i, yet_another_item in enumerate(that.items):
            for error in self.transform(yet_another_item):
                error.path._prepend(
                    IndexSegment(
                        that.items,
                        i
                    )
                )
                error.path._prepend(
                    PropertySegment(
                        that,
                        'items'
                    )
                )
                yield error


_TRANSFORMER = _Transformer()


def verify(
        that: aas_types.Class
) -> Iterator[Error]:
    """
    Verify the constraints of :paramref:`that` recursively.

    :param that: instance whose constraints we want to verify
    :yield: constraint violations
    """
    yield from _TRANSFORMER.transform(that)


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
