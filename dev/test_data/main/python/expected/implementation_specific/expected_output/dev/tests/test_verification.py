"""Test the verification of instances."""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


# pylint: disable=missing-docstring


import json
import unittest


import dummy.jsonization as aas_jsonization
import dummy.verification as aas_verification


import tests.common


class TestVerificationOfValidInstances(unittest.TestCase):
    def test_box(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Box'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.box_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_bag(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Bag'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.bag_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_container(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Container'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.container_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )


class TestVerificationOfInvalidInstances(unittest.TestCase):
    def test_box(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Box').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.box_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_bag(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Bag').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.bag_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_container(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Container').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.container_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)


if __name__ == "__main__":
    unittest.main()


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
