# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


import abc
import enum
from typing import (
    Generic,
    Iterator,
    Optional,
    TypeVar,
    List,
    Tuple,
    Union
)


T = TypeVar("T")
ContextT = TypeVar("ContextT")


class Class(abc.ABC):
    """Represent the most general class of an AAS model."""
    @abc.abstractmethod
    def descend_once(self) -> Iterator["Class"]:
        """Iterate over all the instances referenced from this one."""
        raise NotImplementedError()

    @abc.abstractmethod
    def descend(self) -> Iterator["Class"]:
        """Iterate recursively over all the instances referenced from this one."""
        raise NotImplementedError()

    @abc.abstractmethod
    def accept(
            self,
            visitor: "AbstractVisitor"
    ) -> None:
        """
        Dispatch the :paramref:`visitor` on this instance.

        :param visitor: to be dispatched
        """
        raise NotImplementedError()

    @abc.abstractmethod
    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """
        Dispatch the :paramref:`visitor` on this instance with :paramref:`context`.

        :param visitor: to be dispatched
        :param context: of the visitation
        """
        raise NotImplementedError()

    @abc.abstractmethod
    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance.

        :param transformer: to be dispatched
        :return: transformed self
        """
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance with :paramref:`context`.

        :param transformer: to be dispatched
        :return: transformed self
        """
        raise NotImplementedError()


# pylint: disable=redefined-builtin


class StructuralFirst(Class):
    # pylint: disable=missing-class-docstring

    unique_to_first: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_structural_first(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_structural_first_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_structural_first(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_structural_first_with_context(
            self, context)

    def __init__(self, unique_to_first: str) -> None:
        """Initialize with the given values."""
        self.unique_to_first = unique_to_first


class StructuralSecond(Class):
    # pylint: disable=missing-class-docstring

    unique_to_second: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_structural_second(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_structural_second_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_structural_second(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_structural_second_with_context(
            self, context)

    def __init__(self, unique_to_second: str) -> None:
        """Initialize with the given values."""
        self.unique_to_second = unique_to_second


#: Represent a union of classes.
StructuralUnion = Union['StructuralFirst', 'StructuralSecond']


class MixedAbstractMember(Class):
    # pylint: disable=missing-class-docstring
    pass


class MixedAbstractDescendantOne(MixedAbstractMember):
    # pylint: disable=missing-class-docstring

    unique_to_abstract_descendant_one: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_mixed_abstract_descendant_one(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_mixed_abstract_descendant_one_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_mixed_abstract_descendant_one(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_mixed_abstract_descendant_one_with_context(
            self, context)

    def __init__(self, unique_to_abstract_descendant_one: str) -> None:
        """Initialize with the given values."""
        self.unique_to_abstract_descendant_one = unique_to_abstract_descendant_one


class MixedAbstractDescendantTwo(MixedAbstractMember):
    # pylint: disable=missing-class-docstring

    unique_to_abstract_descendant_two: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_mixed_abstract_descendant_two(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_mixed_abstract_descendant_two_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_mixed_abstract_descendant_two(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_mixed_abstract_descendant_two_with_context(
            self, context)

    def __init__(self, unique_to_abstract_descendant_two: str) -> None:
        """Initialize with the given values."""
        self.unique_to_abstract_descendant_two = unique_to_abstract_descendant_two


class MixedConcreteWithDescendants(Class):
    # pylint: disable=missing-class-docstring

    some_base_property: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_mixed_concrete_with_descendants(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_mixed_concrete_with_descendants_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_mixed_concrete_with_descendants(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_mixed_concrete_with_descendants_with_context(
            self, context)

    def __init__(self, some_base_property: str) -> None:
        """Initialize with the given values."""
        self.some_base_property = some_base_property


class MixedConcreteWithDescendantsChild(MixedConcreteWithDescendants):
    # pylint: disable=missing-class-docstring

    some_child_property: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_mixed_concrete_with_descendants_child(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_mixed_concrete_with_descendants_child_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_mixed_concrete_with_descendants_child(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_mixed_concrete_with_descendants_child_with_context(
            self, context)

    def __init__(
            self,
            some_base_property: str,
            some_child_property: str
    ) -> None:
        """Initialize with the given values."""
        MixedConcreteWithDescendants.__init__(
            self,
            some_base_property
        )
        self.some_child_property = some_child_property


class MixedConcreteLeaf(Class):
    # pylint: disable=missing-class-docstring

    unique_to_concrete_leaf: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_mixed_concrete_leaf(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_mixed_concrete_leaf_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_mixed_concrete_leaf(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_mixed_concrete_leaf_with_context(
            self, context)

    def __init__(self, unique_to_concrete_leaf: str) -> None:
        """Initialize with the given values."""
        self.unique_to_concrete_leaf = unique_to_concrete_leaf


#: Represent a union of classes.
MixedUnion = Union[
    'MixedAbstractDescendantOne',
    'MixedAbstractDescendantTwo',
    'MixedConcreteWithDescendantsChild',
    'MixedConcreteWithDescendants',
    'MixedConcreteLeaf',
]


class ModelTypedFirst(Class):
    # pylint: disable=missing-class-docstring

    some_property: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_model_typed_first(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_model_typed_first_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_model_typed_first(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_model_typed_first_with_context(
            self, context)

    def __init__(self, some_property: str) -> None:
        """Initialize with the given values."""
        self.some_property = some_property


class ModelTypedSecond(Class):
    # pylint: disable=missing-class-docstring

    some_property: str

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        # No descendable properties
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_model_typed_second(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_model_typed_second_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_model_typed_second(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_model_typed_second_with_context(
            self, context)

    def __init__(self, some_property: str) -> None:
        """Initialize with the given values."""
        self.some_property = some_property


#: Represent a union of classes.
ModelTypedUnion = Union['ModelTypedFirst', 'ModelTypedSecond']


class Something(Class):
    # pylint: disable=missing-class-docstring

    structural_property: 'StructuralUnion'

    mixed_property: 'MixedUnion'

    model_typed_property: 'ModelTypedUnion'

    list_structural_property: List['StructuralUnion']

    list_mixed_property: List['MixedUnion']

    list_model_typed_property: List['ModelTypedUnion']

    tuple_property: Tuple['StructuralUnion', 'MixedUnion', 'ModelTypedUnion']

    optional_structural_property: Optional['StructuralUnion']

    optional_mixed_property: Optional['MixedUnion']

    optional_model_typed_property: Optional['ModelTypedUnion']

    def descend_once(self) -> Iterator[Class]:
        """
        Iterate over the instances referenced from this instance.

        We do not recurse into the referenced instance.

        :yield: instances directly referenced from this instance
        """
        yield self.structural_property

        yield self.mixed_property

        yield self.model_typed_property

        yield from self.list_structural_property

        yield from self.list_mixed_property

        yield from self.list_model_typed_property

        yield self.tuple_property[0]

        yield self.tuple_property[1]

        yield self.tuple_property[2]

        if self.optional_structural_property is not None:
            yield self.optional_structural_property

        if self.optional_mixed_property is not None:
            yield self.optional_mixed_property

        if self.optional_model_typed_property is not None:
            yield self.optional_model_typed_property

    def descend(self) -> Iterator[Class]:
        """
        Iterate recursively over the instances referenced from this one.

        :yield: instances recursively referenced from this instance
        """
        yield self.structural_property

        yield from self.structural_property.descend()

        yield self.mixed_property

        yield from self.mixed_property.descend()

        yield self.model_typed_property

        yield from self.model_typed_property.descend()

        for an_item in self.list_structural_property:
            yield an_item

            yield from an_item.descend()

        for another_item in self.list_mixed_property:
            yield another_item

            yield from another_item.descend()

        for yet_another_item in self.list_model_typed_property:
            yield yet_another_item

            yield from yet_another_item.descend()

        yield self.tuple_property[0]

        yield from self.tuple_property[0].descend()

        yield self.tuple_property[1]

        yield from self.tuple_property[1].descend()

        yield self.tuple_property[2]

        yield from self.tuple_property[2].descend()

        if self.optional_structural_property is not None:
            yield self.optional_structural_property

            yield from self.optional_structural_property.descend()

        if self.optional_mixed_property is not None:
            yield self.optional_mixed_property

            yield from self.optional_mixed_property.descend()

        if self.optional_model_typed_property is not None:
            yield self.optional_model_typed_property

            yield from self.optional_model_typed_property.descend()

    def accept(self, visitor: "AbstractVisitor") -> None:
        """Dispatch the :paramref:`visitor` on this instance."""
        visitor.visit_something(self)

    def accept_with_context(
            self,
            visitor: "AbstractVisitorWithContext[ContextT]",
            context: ContextT
    ) -> None:
        """Dispatch the :paramref:`visitor` on this instance in :paramref:`context`."""
        visitor.visit_something_with_context(self, context)

    def transform(
            self,
            transformer: "AbstractTransformer[T]"
    ) -> T:
        """Dispatch the :paramref:`transformer` on this instance."""
        return transformer.transform_something(self)

    def transform_with_context(
            self,
            transformer: "AbstractTransformerWithContext[ContextT, T]",
            context: ContextT
    ) -> T:
        """
        Dispatch the :paramref:`transformer` on this instance in :paramref:`context`.
        """
        return transformer.transform_something_with_context(
            self, context)

    def __init__(
            self,
            structural_property: 'StructuralUnion',
            mixed_property: 'MixedUnion',
            model_typed_property: 'ModelTypedUnion',
            list_structural_property: List['StructuralUnion'],
            list_mixed_property: List['MixedUnion'],
            list_model_typed_property: List['ModelTypedUnion'],
            tuple_property: Tuple['StructuralUnion', 'MixedUnion', 'ModelTypedUnion'],
            optional_structural_property: Optional['StructuralUnion'] = None,
            optional_mixed_property: Optional['MixedUnion'] = None,
            optional_model_typed_property: Optional['ModelTypedUnion'] = None
    ) -> None:
        """Initialize with the given values."""
        self.structural_property = structural_property
        self.mixed_property = mixed_property
        self.model_typed_property = model_typed_property
        self.list_structural_property = list_structural_property
        self.list_mixed_property = list_mixed_property
        self.list_model_typed_property = list_model_typed_property
        self.tuple_property = tuple_property
        self.optional_structural_property = optional_structural_property
        self.optional_mixed_property = optional_mixed_property
        self.optional_model_typed_property = optional_model_typed_property


class AbstractVisitor:
    """Visit the instances of the model."""
    def visit(
            self,
            that: Class
    ) -> None:
        """Double-dispatch on :paramref:`that`."""
        that.accept(self)

    @abc.abstractmethod
    def visit_structural_first(
            self,
            that: StructuralFirst
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_structural_second(
            self,
            that: StructuralSecond
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_abstract_descendant_one(
            self,
            that: MixedAbstractDescendantOne
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_abstract_descendant_two(
            self,
            that: MixedAbstractDescendantTwo
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_with_descendants(
            self,
            that: MixedConcreteWithDescendants
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_with_descendants_child(
            self,
            that: MixedConcreteWithDescendantsChild
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_leaf(
            self,
            that: MixedConcreteLeaf
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_model_typed_first(
            self,
            that: ModelTypedFirst
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_model_typed_second(
            self,
            that: ModelTypedSecond
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_something(
            self,
            that: Something
    ) -> None:
        """Visit :paramref:`that`."""
        raise NotImplementedError()


class AbstractVisitorWithContext(Generic[ContextT]):
    """Visit the instances of the model with context."""
    def visit_with_context(
            self,
            that: Class,
            context: ContextT
    ) -> None:
        """Double-dispatch on :paramref:`that`."""
        that.accept_with_context(self, context)

    @abc.abstractmethod
    def visit_structural_first_with_context(
            self,
            that: StructuralFirst,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_structural_second_with_context(
            self,
            that: StructuralSecond,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_abstract_descendant_one_with_context(
            self,
            that: MixedAbstractDescendantOne,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_abstract_descendant_two_with_context(
            self,
            that: MixedAbstractDescendantTwo,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_with_descendants_with_context(
            self,
            that: MixedConcreteWithDescendants,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_with_descendants_child_with_context(
            self,
            that: MixedConcreteWithDescendantsChild,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_mixed_concrete_leaf_with_context(
            self,
            that: MixedConcreteLeaf,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_model_typed_first_with_context(
            self,
            that: ModelTypedFirst,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_model_typed_second_with_context(
            self,
            that: ModelTypedSecond,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def visit_something_with_context(
            self,
            that: Something,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()


class PassThroughVisitor(AbstractVisitor):
    """
    Visit the instances of the model without action.

    This visitor is not meant to be directly used. Instead, you usually
    inherit from it, and implement only the relevant visit methods.
    """
    def visit(
            self,
            that: Class
    ) -> None:
        """Double-dispatch on :paramref:`that`."""
        that.accept(self)

    def visit_structural_first(
            self,
            that: StructuralFirst
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_structural_second(
            self,
            that: StructuralSecond
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_mixed_abstract_descendant_one(
            self,
            that: MixedAbstractDescendantOne
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_mixed_abstract_descendant_two(
            self,
            that: MixedAbstractDescendantTwo
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_mixed_concrete_with_descendants(
            self,
            that: MixedConcreteWithDescendants
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_mixed_concrete_with_descendants_child(
            self,
            that: MixedConcreteWithDescendantsChild
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_mixed_concrete_leaf(
            self,
            that: MixedConcreteLeaf
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_model_typed_first(
            self,
            that: ModelTypedFirst
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_model_typed_second(
            self,
            that: ModelTypedSecond
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)

    def visit_something(
            self,
            that: Something
    ) -> None:
        """Visit :paramref:`that`."""
        for another in that.descend_once():
            self.visit(another)


class PassThroughVisitorWithContext(
        AbstractVisitorWithContext[ContextT]
):
    """
    Visit the instances of the model without action and in context.

    This visitor is not meant to be directly used. Instead, you usually
    inherit from it, and implement only the relevant visit methods.
    """
    def visit_with_context(
            self,
            that: Class,
            context: ContextT
    ) -> None:
        """Double-dispatch on :paramref:`that`."""
        that.accept_with_context(self, context)

    def visit_structural_first_with_context(
            self,
            that: StructuralFirst,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_structural_second_with_context(
            self,
            that: StructuralSecond,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_mixed_abstract_descendant_one_with_context(
            self,
            that: MixedAbstractDescendantOne,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_mixed_abstract_descendant_two_with_context(
            self,
            that: MixedAbstractDescendantTwo,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_mixed_concrete_with_descendants_with_context(
            self,
            that: MixedConcreteWithDescendants,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_mixed_concrete_with_descendants_child_with_context(
            self,
            that: MixedConcreteWithDescendantsChild,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_mixed_concrete_leaf_with_context(
            self,
            that: MixedConcreteLeaf,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_model_typed_first_with_context(
            self,
            that: ModelTypedFirst,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_model_typed_second_with_context(
            self,
            that: ModelTypedSecond,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)

    def visit_something_with_context(
            self,
            that: Something,
            context: ContextT
    ) -> None:
        """Visit :paramref:`that` in :paramref:`context`."""
        for another in that.descend_once():
            self.visit_with_context(another, context)


class AbstractTransformer(Generic[T]):
    """Transform the instances of the model."""
    def transform(
            self,
            that: Class
    ) -> T:
        """Double-dispatch on :paramref:`that`."""
        return that.transform(self)

    @abc.abstractmethod
    def transform_structural_first(
            self,
            that: StructuralFirst
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_structural_second(
            self,
            that: StructuralSecond
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_abstract_descendant_one(
            self,
            that: MixedAbstractDescendantOne
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_abstract_descendant_two(
            self,
            that: MixedAbstractDescendantTwo
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_with_descendants(
            self,
            that: MixedConcreteWithDescendants
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_with_descendants_child(
            self,
            that: MixedConcreteWithDescendantsChild
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_leaf(
            self,
            that: MixedConcreteLeaf
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_model_typed_first(
            self,
            that: ModelTypedFirst
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_model_typed_second(
            self,
            that: ModelTypedSecond
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_something(
            self,
            that: Something
    ) -> T:
        """Transform :paramref:`that`."""
        raise NotImplementedError()


class AbstractTransformerWithContext(
        Generic[ContextT, T]
):
    """Transform the instances of the model in context."""
    def transform_with_context(
            self,
            that: Class,
            context: ContextT
    ) -> T:
        """Double-dispatch on :paramref:`that`."""
        return that.transform_with_context(self, context)

    @abc.abstractmethod
    def transform_structural_first_with_context(
            self,
            that: StructuralFirst,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_structural_second_with_context(
            self,
            that: StructuralSecond,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_abstract_descendant_one_with_context(
            self,
            that: MixedAbstractDescendantOne,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_abstract_descendant_two_with_context(
            self,
            that: MixedAbstractDescendantTwo,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_with_descendants_with_context(
            self,
            that: MixedConcreteWithDescendants,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_with_descendants_child_with_context(
            self,
            that: MixedConcreteWithDescendantsChild,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_mixed_concrete_leaf_with_context(
            self,
            that: MixedConcreteLeaf,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_model_typed_first_with_context(
            self,
            that: ModelTypedFirst,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_model_typed_second_with_context(
            self,
            that: ModelTypedSecond,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()

    @abc.abstractmethod
    def transform_something_with_context(
            self,
            that: Something,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        raise NotImplementedError()


class TransformerWithDefault(AbstractTransformer[T]):
    """
    Transform the instances of the model.

    If you do not override the transformation methods, they simply
    return :py:attr:`.default`.
    """
    #: Default value which is returned if no override of the transformation
    default: T

    def __init__(self, default: T) -> None:
        """Initialize with the given :paramref:`default` value."""
        self.default = default

    def transform(
            self,
            that: Class
    ) -> T:
        """Double-dispatch on :paramref:`that`."""
        return that.transform(self)

    def transform_structural_first(
            self,
            that: StructuralFirst
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_structural_second(
            self,
            that: StructuralSecond
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_mixed_abstract_descendant_one(
            self,
            that: MixedAbstractDescendantOne
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_mixed_abstract_descendant_two(
            self,
            that: MixedAbstractDescendantTwo
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_mixed_concrete_with_descendants(
            self,
            that: MixedConcreteWithDescendants
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_mixed_concrete_with_descendants_child(
            self,
            that: MixedConcreteWithDescendantsChild
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_mixed_concrete_leaf(
            self,
            that: MixedConcreteLeaf
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_model_typed_first(
            self,
            that: ModelTypedFirst
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_model_typed_second(
            self,
            that: ModelTypedSecond
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default

    def transform_something(
            self,
            that: Something
    ) -> T:
        """Transform :paramref:`that`."""
        return self.default


class TransformerWithDefaultAndContext(
        AbstractTransformerWithContext[ContextT, T]
):
    """
    Transform the instances of the model in context.

    If you do not override the transformation methods, they simply
    return :py:attr:`.default`.
    """
    #: Default value which is returned if no override of the transformation
    default: T

    def __init__(self, default: T) -> None:
        """Initialize with the given :paramref:`default` value."""
        self.default = default

    def transform_with_context(
            self,
            that: Class,
            context: ContextT
    ) -> T:
        """Double-dispatch on :paramref:`that`."""
        return that.transform_with_context(self, context)

    def transform_structural_first_with_context(
            self,
            that: StructuralFirst,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_structural_second_with_context(
            self,
            that: StructuralSecond,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_mixed_abstract_descendant_one_with_context(
            self,
            that: MixedAbstractDescendantOne,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_mixed_abstract_descendant_two_with_context(
            self,
            that: MixedAbstractDescendantTwo,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_mixed_concrete_with_descendants_with_context(
            self,
            that: MixedConcreteWithDescendants,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_mixed_concrete_with_descendants_child_with_context(
            self,
            that: MixedConcreteWithDescendantsChild,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_mixed_concrete_leaf_with_context(
            self,
            that: MixedConcreteLeaf,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_model_typed_first_with_context(
            self,
            that: ModelTypedFirst,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_model_typed_second_with_context(
            self,
            that: ModelTypedSecond,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default

    def transform_something_with_context(
            self,
            that: Something,
            context: ContextT
    ) -> T:
        """Transform :paramref:`that` in :paramref:`context`."""
        return self.default


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
