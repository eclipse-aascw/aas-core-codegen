"""
Verify that the instances of the meta-model satisfy the invariants.

Here is an example how to verify an instance of :py:class:`dummy.types.LangString`:

.. code-block::

    import dummy.types as aas_types
    import dummy.verification as aas_verification

    an_instance = aas_types.LangString(
        # ... some constructor arguments ...
    )

    for error in aas_verification.verify(an_instance):
        print(f"{error.cause} at: {error.path}")
"""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


import collections.abc
import math
import re
import struct
import sys
from typing import (
    Any,
    Callable,
    Iterable,
    Iterator,
    List,
    Mapping,
    Optional,
    Pattern,
    Sequence,
    Set,
    Union
)

if sys.version_info >= (3, 8):
    from typing import Final
else:
    from typing_extensions import Final

from dummy import (
    constants as aas_constants,
    reporting as aas_reporting,
    types as aas_types,
)


PropertySegment = aas_reporting.PropertySegment
IndexSegment = aas_reporting.IndexSegment
KeySegment = aas_reporting.KeySegment
Segment = aas_reporting.Segment
Path = aas_reporting.Path

Error = aas_reporting.Error


def is_english(
    language: str
) -> bool:
    """Check that :paramref:`language` denotes English."""
    # pylint: disable=all
    return language == 'en'


def lang_string_sets_have_english(
    lang_string_sets: List[aas_types.LangStringSet]
) -> bool:
    """
    Check that every set in :paramref:`lang_string_sets` has at least one
    string in English.

    This function tests a plain ``any`` nested in ``all``.
    """
    # pylint: disable=all
    return (
        all(
            any(
                is_english(lang_string.language)
                for lang_string in lang_string_set.lang_strings
            )
            for lang_string_set in lang_string_sets
        ))


def iec_contents_have_definition_in_english(
    specifications: List[aas_types.Specification]
) -> bool:
    """
    Check that the :py:attr:`.types.IecContent.definition` is defined at least in English
    for all the specifications whose content is an :py:class:`.types.IecContent`.

    This function tests an ``any`` nested in ``all``, where the nested ``any``
    iterates over an optional property of a value narrowed by ``isinstance``.
    """
    # pylint: disable=all
    return (
        all(
            not isinstance(specification.content, aas_types.IecContent)
            or (
                (
                    (specification.content.definition is not None)
                    and (
                        any(
                            is_english(lang_string.language)
                            for lang_string in specification.content.definition
                        )
                    )
                )
            )
            for specification in specifications
        ))


class _Transformer(
        aas_types.AbstractTransformer[
            Iterator[Error]
        ]
):
    # noinspection PyMethodMayBeStatic
    def transform_lang_string(
            self,
            that: aas_types.LangString
    ) -> Iterator[Error]:
        # No verification has been defined for LangString.
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    # noinspection PyMethodMayBeStatic
    def transform_lang_string_set(
            self,
            that: aas_types.LangStringSet
    ) -> Iterator[Error]:
        for i, an_item in enumerate(that.lang_strings):
            for error in self.transform(an_item):
                error.path._prepend(
                    IndexSegment(
                        that.lang_strings,
                        i
                    )
                )
                error.path._prepend(
                    PropertySegment(
                        that,
                        'lang_strings'
                    )
                )
                yield error

    # noinspection PyMethodMayBeStatic
    def transform_iec_content(
            self,
            that: aas_types.IecContent
    ) -> Iterator[Error]:
        if that.definition is not None:
            for i, an_item in enumerate(that.definition):
                for error in self.transform(an_item):
                    error.path._prepend(
                        IndexSegment(
                            that.definition,
                            i
                        )
                    )
                    error.path._prepend(
                        PropertySegment(
                            that,
                            'definition'
                        )
                    )
                    yield error

    # noinspection PyMethodMayBeStatic
    def transform_other_content(
            self,
            that: aas_types.OtherContent
    ) -> Iterator[Error]:
        # No verification has been defined for OtherContent.
        return
        # For this uncommon return-yield construction, see:
        # https://stackoverflow.com/questions/13243766/how-to-define-an-empty-generator-function
        # noinspection PyUnreachableCode
        yield

    # noinspection PyMethodMayBeStatic
    def transform_specification(
            self,
            that: aas_types.Specification
    ) -> Iterator[Error]:
        for error in self.transform(that.content):
            error.path._prepend(
                PropertySegment(
                    that,
                    'content'
                )
            )
            yield error

    # noinspection PyMethodMayBeStatic
    def transform_something(
            self,
            that: aas_types.Something
    ) -> Iterator[Error]:
        if not (
            not (that.specifications is not None)
            or iec_contents_have_definition_in_english(
                that.specifications
            )
        ):
            yield Error(
                'The IEC contents must have a definition at least in English.'
            )

        if not (
            all(
                any(
                    lang_string.language == that.default_language
                    for lang_string in lang_string_set.lang_strings
                )
                for lang_string_set in that.lang_string_sets
            )
        ):
            yield Error(
                'Every language string set must have at least one string in ' +
                'the default language.'
            )

        if not (
            lang_string_sets_have_english(that.lang_string_sets)
        ):
            yield Error(
                'Every language string set must have at least one string in ' +
                'English.'
            )

        for i, an_item in enumerate(that.lang_string_sets):
            for error in self.transform(an_item):
                error.path._prepend(
                    IndexSegment(
                        that.lang_string_sets,
                        i
                    )
                )
                error.path._prepend(
                    PropertySegment(
                        that,
                        'lang_string_sets'
                    )
                )
                yield error

        if that.specifications is not None:
            for i, another_item in enumerate(that.specifications):
                for error in self.transform(another_item):
                    error.path._prepend(
                        IndexSegment(
                            that.specifications,
                            i
                        )
                    )
                    error.path._prepend(
                        PropertySegment(
                            that,
                            'specifications'
                        )
                    )
                    yield error


_TRANSFORMER = _Transformer()


def verify(
        that: aas_types.Class
) -> Iterator[Error]:
    """
    Verify the constraints of :paramref:`that` recursively.

    :param that: instance whose constraints we want to verify
    :yield: constraint violations
    """
    yield from _TRANSFORMER.transform(that)


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
