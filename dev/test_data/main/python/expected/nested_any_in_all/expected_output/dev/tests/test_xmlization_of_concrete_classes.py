"""Test XML de/serialization of concrete classes."""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


# pylint: disable=missing-docstring


import io
import pathlib
import tempfile
import math
import unittest
import xml.etree.ElementTree as ET


import dummy.types as aas_types
import dummy.xmlization as aas_xmlization


import tests.common
import tests.common_xmlization


class TestRoundTrips(unittest.TestCase):
    def test_lang_string(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'langString'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.lang_string_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .lang_string_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .lang_string_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .lang_string_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion

    def test_lang_string_set(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'langStringSet'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.lang_string_set_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .lang_string_set_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .lang_string_set_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .lang_string_set_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion

    def test_iec_content(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'iecContent'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.iec_content_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .iec_content_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .iec_content_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .iec_content_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion

    def test_other_content(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'otherContent'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.other_content_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .other_content_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .other_content_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .other_content_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion

    def test_specification(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'specification'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.specification_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .specification_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .specification_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .specification_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion

    def test_something(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Xml"
                / "Expected"
                / 'something'
            ).glob("**/*.xml")
        ):
            text = path.read_text(encoding="utf-8")
            et_concrete = ET.fromstring(text)
            tests.common_xmlization.remove_redundant_whitespace(et_concrete)

            # region From iterparse
            iterator = ET.iterparse(source=io.StringIO(text), events=["start", "end"])
            got_from_iterparse = (
                aas_xmlization.something_from_iterparse(iterator)
            )

            et_from_iterparse = ET.fromstring(aas_xmlization.to_str(got_from_iterparse))
            tests.common_xmlization.remove_redundant_whitespace(et_from_iterparse)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_iterparse)
            # endregion

            # region From stream
            got_from_stream = (
                aas_xmlization
                .something_from_stream(
                    io.StringIO(text)
                )
            )
            et_from_stream = ET.fromstring(aas_xmlization.to_str(got_from_stream))
            tests.common_xmlization.remove_redundant_whitespace(et_from_stream)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_stream)
            # endregion

            # region From file
            with tempfile.TemporaryDirectory() as tmp_dir:
                path = pathlib.Path(tmp_dir) / "something.xml"
                path.write_text(text, encoding="utf-8")

                got_from_file = (
                    aas_xmlization
                    .something_from_file(path)
                )
            et_from_file = ET.fromstring(aas_xmlization.to_str(got_from_file))
            tests.common_xmlization.remove_redundant_whitespace(et_from_file)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_file)
            # endregion

            # region From string
            got_from_str = (
                aas_xmlization
                .something_from_str(text)
            )
            et_from_str = ET.fromstring(aas_xmlization.to_str(got_from_str))
            tests.common_xmlization.remove_redundant_whitespace(et_from_str)
            tests.common_xmlization.assert_elements_equal(et_concrete, et_from_str)
            # endregion


class TestDuplicateProperty(unittest.TestCase):
    """Test that a property given more than once is refused."""

    def test_duplicate_property(self) -> None:
        path = (
            tests.common.TEST_DATA_DIR
            / "Xml"
            / "Expected"
            / 'langString'
            / "minimal.xml"
        )

        text = path.read_text(encoding="utf-8")

        start = text.find('<language>')
        if start >= 0:
            end = text.find('</language>', start)
            duplicated = text[start:end + 11]
        else:
            # The element is written self-closing in the example, an empty list
            # being the usual reason. We write that very element out ourselves.
            duplicated = '<language/>'

        insertion_index = text.rfind('</langString>')

        self.assertGreaterEqual(
            insertion_index,
            0,
            "Expected the recorded example to contain the closing tag "
            f"</langString>, but it does not: {path}",
        )

        broken_text = (
            text[:insertion_index] + duplicated + text[insertion_index:]
        )

        with self.assertRaises(aas_xmlization.DeserializationException):
            aas_xmlization.lang_string_from_str(broken_text)


if __name__ == "__main__":
    unittest.main()


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
