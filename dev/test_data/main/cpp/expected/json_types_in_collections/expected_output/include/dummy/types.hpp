#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <nlohmann/json.hpp>

#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kSomething = 0
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::vector<nlohmann::json>& values() const = 0;

  virtual std::vector<nlohmann::json>& mutable_values() = 0;

  virtual void set_values(
    std::vector<nlohmann::json> value
  ) = 0;

  virtual const std::tuple<
    std::wstring,
    nlohmann::json,
    nlohmann::json,
    nlohmann::json
  >& tuple_with_json() const = 0;

  virtual std::tuple<
    std::wstring,
    nlohmann::json,
    nlohmann::json,
    nlohmann::json
  >& mutable_tuple_with_json() = 0;

  virtual void set_tuple_with_json(
    std::tuple<
      std::wstring,
      nlohmann::json,
      nlohmann::json,
      nlohmann::json
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Something
    : public ISomething {
 public:
  Something(
    std::vector<nlohmann::json> values,
    std::tuple<
      std::wstring,
      nlohmann::json,
      nlohmann::json,
      nlohmann::json
    > tuple_with_json
  );

  ModelType model_type() const override;

  // region Get and set values_

  const std::vector<nlohmann::json>& values() const override;

  std::vector<nlohmann::json>& mutable_values() override;

  void set_values(
    std::vector<nlohmann::json> value
  ) override;

  // endregion

  // region Get and set tuple_with_json_

  const std::tuple<
    std::wstring,
    nlohmann::json,
    nlohmann::json,
    nlohmann::json
  >& tuple_with_json() const override;

  std::tuple<
    std::wstring,
    nlohmann::json,
    nlohmann::json,
    nlohmann::json
  >& mutable_tuple_with_json() override;

  void set_tuple_with_json(
    std::tuple<
      std::wstring,
      nlohmann::json,
      nlohmann::json,
      nlohmann::json
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::vector<nlohmann::json> values_;

  std::tuple<
    std::wstring,
    nlohmann::json,
    nlohmann::json,
    nlohmann::json
  > tuple_with_json_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
