#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <nlohmann/json.hpp>

#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kItem = 0,
  kSomething = 1
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IItem;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

/// \brief Represent something which can be enhanced.
class IItem
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IItem() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const common::optional<
    std::vector<
      std::shared_ptr<IItem>
    >
  >& optional_items() const = 0;

  virtual common::optional<
    std::vector<
      std::shared_ptr<IItem>
    >
  >& mutable_optional_items() = 0;

  virtual void set_optional_items(
    common::optional<
      std::vector<
        std::shared_ptr<IItem>
      >
    > value
  ) = 0;

  virtual const common::optional<
    std::vector<std::wstring>
  >& optional_texts() const = 0;

  virtual common::optional<
    std::vector<std::wstring>
  >& mutable_optional_texts() = 0;

  virtual void set_optional_texts(
    common::optional<
      std::vector<std::wstring>
    > value
  ) = 0;

  virtual const common::optional<
    std::vector<nlohmann::json>
  >& optional_values() const = 0;

  virtual common::optional<
    std::vector<nlohmann::json>
  >& mutable_optional_values() = 0;

  virtual void set_optional_values(
    common::optional<
      std::vector<nlohmann::json>
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Item
    : public IItem {
 public:
  explicit Item(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~Item() override = default;

 private:
  std::wstring text_;
};

class Something
    : public ISomething {
 public:
  Something(
    common::optional<
      std::vector<
        std::shared_ptr<IItem>
      >
    > optional_items = common::nullopt,
    common::optional<
      std::vector<std::wstring>
    > optional_texts = common::nullopt,
    common::optional<
      std::vector<nlohmann::json>
    > optional_values = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set optional_items_

  const common::optional<
    std::vector<
      std::shared_ptr<IItem>
    >
  >& optional_items() const override;

  common::optional<
    std::vector<
      std::shared_ptr<IItem>
    >
  >& mutable_optional_items() override;

  void set_optional_items(
    common::optional<
      std::vector<
        std::shared_ptr<IItem>
      >
    > value
  ) override;

  // endregion

  // region Get and set optional_texts_

  const common::optional<
    std::vector<std::wstring>
  >& optional_texts() const override;

  common::optional<
    std::vector<std::wstring>
  >& mutable_optional_texts() override;

  void set_optional_texts(
    common::optional<
      std::vector<std::wstring>
    > value
  ) override;

  // endregion

  // region Get and set optional_values_

  const common::optional<
    std::vector<nlohmann::json>
  >& optional_values() const override;

  common::optional<
    std::vector<nlohmann::json>
  >& mutable_optional_values() override;

  void set_optional_values(
    common::optional<
      std::vector<nlohmann::json>
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  common::optional<
    std::vector<
      std::shared_ptr<IItem>
    >
  > optional_items_;

  common::optional<
    std::vector<std::wstring>
  > optional_texts_;

  common::optional<
    std::vector<nlohmann::json>
  > optional_values_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IItem
 */
bool IsItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
