// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

/**
* Load minimal and maximal examples from an XML file.
*/

#ifndef TEST_COMMON_EXAMPLES_GUARD_
#define TEST_COMMON_EXAMPLES_GUARD_

#include <dummy/types.hpp>

namespace test {
namespace common {
namespace examples {

/**
 * Load a minimal example of ILeaf by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ILeaf
> LoadMinLeaf();

/**
 * Load a maximal example of ILeaf by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ILeaf
> LoadMaxLeaf();

/**
 * Load a minimal example of IOrderedContainer by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IOrderedContainer
> LoadMinOrderedContainer();

/**
 * Load a maximal example of IOrderedContainer by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IOrderedContainer
> LoadMaxOrderedContainer();

/**
 * Load a minimal example of IUnorderedContainer by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IUnorderedContainer
> LoadMinUnorderedContainer();

/**
 * Load a maximal example of IUnorderedContainer by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IUnorderedContainer
> LoadMaxUnorderedContainer();

/**
 * Load a minimal example of IGlobalAttribute by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IGlobalAttribute
> LoadMinGlobalAttribute();

/**
 * Load a maximal example of IGlobalAttribute by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IGlobalAttribute
> LoadMaxGlobalAttribute();

/**
 * Load a minimal example of ILocalAttribute by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ILocalAttribute
> LoadMinLocalAttribute();

/**
 * Load a maximal example of ILocalAttribute by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ILocalAttribute
> LoadMaxLocalAttribute();

/**
 * Load a minimal example of IAttributeOperand by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IAttributeOperand
> LoadMinAttributeOperand();

/**
 * Load a maximal example of IAttributeOperand by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IAttributeOperand
> LoadMaxAttributeOperand();

/**
 * Load a minimal example of IStringLiteral by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IStringLiteral
> LoadMinStringLiteral();

/**
 * Load a maximal example of IStringLiteral by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::IStringLiteral
> LoadMaxStringLiteral();

/**
 * Load a minimal example of INumberLiteral by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::INumberLiteral
> LoadMinNumberLiteral();

/**
 * Load a maximal example of INumberLiteral by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::INumberLiteral
> LoadMaxNumberLiteral();

/**
 * Load a minimal example of ISomething by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ISomething
> LoadMinSomething();

/**
 * Load a maximal example of ISomething by
 * de-serializing it from an XML file.
 */
std::shared_ptr<
  dummy::types::ISomething
> LoadMaxSomething();

}  // namespace examples
}  // namespace common
}  // namespace test

#endif // TEST_COMMON_EXAMPLES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
