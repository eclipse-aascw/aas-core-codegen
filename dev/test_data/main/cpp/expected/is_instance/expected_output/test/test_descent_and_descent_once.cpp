// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "./common.hpp"
#include "./common_examples.hpp"
#include "./common_xmlization.hpp"

#include <dummy/iteration.hpp>
#include <dummy/stringification.hpp>
#include <dummy/types.hpp>

#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>

#include <deque>

namespace aas = dummy;

const std::filesystem::path& DetermineXmlDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "Xml";
  }

  return *result;
}

const std::filesystem::path& DetermineDescentDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "Descent";
  }

  return *result;
}

const std::filesystem::path& DetermineDescentOnceDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "DescentOnce";
  }

  return *result;
}

void AssertOrRerecordDescent(
  const std::filesystem::path& xml_path,
  const std::filesystem::path& trace_path
) {
  std::shared_ptr<
    aas::types::IClass
  > instance(
    test::common::xmlization::MustDeserializeFile(xml_path)
  );

  std::deque<std::wstring> parts;

  for (
    const std::shared_ptr<aas::types::IClass>& something
    : aas::iteration::Descent(instance)
  ) {
    parts.emplace_back(test::common::TraceMark(*something));
    parts.push_back(L"\n");
  }

  test::common::AssertContentEqualsExpectedOrRecord(
    aas::common::WstringToUtf8(
      test::common::JoinWstrings(parts, L"")
    ),
    trace_path
  );
}

void AssertOrRerecordDescentOnce(
  const std::shared_ptr<aas::types::IClass>& instance,
  const std::filesystem::path& trace_path
) {
  std::deque<std::wstring> parts;

  for (
    const std::shared_ptr<aas::types::IClass>& something
    : aas::iteration::DescentOnce(instance)
  ) {
    parts.emplace_back(test::common::TraceMark(*something));
    parts.push_back(L"\n");
  }

  test::common::AssertContentEqualsExpectedOrRecord(
    aas::common::WstringToUtf8(
      test::common::JoinWstrings(parts, L"")
    ),
    trace_path
  );
}

TEST_CASE("Test Descent over an Leaf") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "leaf",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an Leaf") {
  const std::shared_ptr<
    aas::types::ILeaf
  > instance(
    test::common::examples::LoadMaxLeaf()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxLeaf.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an OrderedContainer") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "orderedContainer",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an OrderedContainer") {
  const std::shared_ptr<
    aas::types::IOrderedContainer
  > instance(
    test::common::examples::LoadMaxOrderedContainer()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxOrderedContainer.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an UnorderedContainer") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "unorderedContainer",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an UnorderedContainer") {
  const std::shared_ptr<
    aas::types::IUnorderedContainer
  > instance(
    test::common::examples::LoadMaxUnorderedContainer()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxUnorderedContainer.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an GlobalAttribute") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "globalAttribute",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an GlobalAttribute") {
  const std::shared_ptr<
    aas::types::IGlobalAttribute
  > instance(
    test::common::examples::LoadMaxGlobalAttribute()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxGlobalAttribute.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an LocalAttribute") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "localAttribute",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an LocalAttribute") {
  const std::shared_ptr<
    aas::types::ILocalAttribute
  > instance(
    test::common::examples::LoadMaxLocalAttribute()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxLocalAttribute.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an AttributeOperand") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "attributeOperand",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an AttributeOperand") {
  const std::shared_ptr<
    aas::types::IAttributeOperand
  > instance(
    test::common::examples::LoadMaxAttributeOperand()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxAttributeOperand.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an StringLiteral") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "stringLiteral",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an StringLiteral") {
  const std::shared_ptr<
    aas::types::IStringLiteral
  > instance(
    test::common::examples::LoadMaxStringLiteral()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxStringLiteral.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an NumberLiteral") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "numberLiteral",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an NumberLiteral") {
  const std::shared_ptr<
    aas::types::INumberLiteral
  > instance(
    test::common::examples::LoadMaxNumberLiteral()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxNumberLiteral.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an Something") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "something",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an Something") {
  const std::shared_ptr<
    aas::types::ISomething
  > instance(
    test::common::examples::LoadMaxSomething()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxSomething.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
