// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/stringification.hpp"
#include "dummy/wstringification.hpp"
#include "dummy/xmlization.hpp"

#include "xml_common.hpp"

#pragma warning(push, 0)
#include <bitset>
#include <cmath>
#include <cstdint>
#include <cstdlib>
#include <deque>
#include <memory>
#include <limits>
#include <unordered_map>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {
namespace xmlization {

const std::string kNamespace(  // NOLINT(cert-err58-cpp)
  xml_common::kNamespace
);

// region De-serialization

// region DeserializationError

DeserializationError::DeserializationError(
  std::wstring a_cause
) :
  cause(a_cause) {
  // Intentionally empty.
}

DeserializationError::DeserializationError(
  std::wstring a_cause,
  xml_path::Path a_path
) :
  cause(a_cause),
  path(a_path) {
  // Intentionally empty.
}

// endregion DeserializationError

// region Forward declarations of de-serialization functions

// NOTE (mristin):
// We make forward declarations of de-serialization functions so that they can be
// called in any order.

std::pair<
  common::optional<
    std::shared_ptr<types::IElement>
  >,
  common::optional<DeserializationError>
> ElementFromElement(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::ILeaf>
  >,
  common::optional<DeserializationError>
> LeafFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ILeaf>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> LeafFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IContainer>
  >,
  common::optional<DeserializationError>
> ContainerFromElement(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IOrderedContainer>
  >,
  common::optional<DeserializationError>
> OrderedContainerFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IOrderedContainer>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> OrderedContainerFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IUnorderedContainer>
  >,
  common::optional<DeserializationError>
> UnorderedContainerFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IUnorderedContainer>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> UnorderedContainerFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IGlobalAttribute>
  >,
  common::optional<DeserializationError>
> GlobalAttributeFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IGlobalAttribute>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> GlobalAttributeFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::ILocalAttribute>
  >,
  common::optional<DeserializationError>
> LocalAttributeFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ILocalAttribute>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> LocalAttributeFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IAttributeOperand>
  >,
  common::optional<DeserializationError>
> AttributeOperandFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IAttributeOperand>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> AttributeOperandFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::IStringLiteral>
  >,
  common::optional<DeserializationError>
> StringLiteralFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IStringLiteral>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> StringLiteralFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::INumberLiteral>
  >,
  common::optional<DeserializationError>
> NumberLiteralFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::INumberLiteral>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> NumberLiteralFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<
    std::shared_ptr<types::ISomething>
  >,
  common::optional<DeserializationError>
> SomethingFromElement(
  xml_common::ReaderMergingText& reader
);

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ISomething>::value
  >::type* = nullptr
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> SomethingFromSequence(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<types::AttributeItem>,
  common::optional<DeserializationError>
> AttributeItemFromElement(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<types::StringValue>,
  common::optional<DeserializationError>
> StringValueFromElement(
  xml_common::ReaderMergingText& reader
);

std::pair<
  common::optional<types::Value>,
  common::optional<DeserializationError>
> ValueFromElement(
  xml_common::ReaderMergingText& reader
);

// endregion Forward declarations of de-serialization functions

/**
 * Map XML class names to model types.
 */
const std::unordered_map<
  std::string,
  types::ModelType
> kElementNameToModelType = {
  {
    "leaf",
    types::ModelType::kLeaf
  },
  {
    "orderedContainer",
    types::ModelType::kOrderedContainer
  },
  {
    "unorderedContainer",
    types::ModelType::kUnorderedContainer
  },
  {
    "globalAttribute",
    types::ModelType::kGlobalAttribute
  },
  {
    "localAttribute",
    types::ModelType::kLocalAttribute
  },
  {
    "attributeOperand",
    types::ModelType::kAttributeOperand
  },
  {
    "stringLiteral",
    types::ModelType::kStringLiteral
  },
  {
    "numberLiteral",
    types::ModelType::kNumberLiteral
  },
  {
    "something",
    types::ModelType::kSomething
  }
};

common::optional<types::ModelType> ModelTypeFromElementName(
  const std::string& element_name
) {
  auto it = kElementNameToModelType.find(element_name);
  if (it == kElementNameToModelType.end()) {
    return common::nullopt;
  }

  return it->second;
}

template <
  typename T
>
std::pair<
  common::optional<T>,
  common::optional<DeserializationError>
> InstanceAndNoDeserializationError(
  T&& instance
) {
  return std::make_pair<
    common::optional<T>,
    common::optional<DeserializationError>
  >(
    std::move(instance),
    common::nullopt
  );
}

template <
  typename T
> std::pair<
  common::optional<T>,
  common::optional<DeserializationError>
> NoInstanceAndDeserializationErrorWithCause(
  std::wstring cause
) {
  return std::make_pair<
    common::optional<T>,
    common::optional<DeserializationError>
  >(
    common::nullopt,
    common::make_optional<DeserializationError>(
      std::move(cause)
    )
  );
}

DeserializationError DuplicatePropertyError(
  const std::string& name
) {
  return DeserializationError(
    common::Concat(
      L"Property ",
      common::Utf8ToWstring(name),
      L" occurred more than once"
    )
  );
}

DeserializationError DeserializationErrorFromReader(
  xml_common::ReaderMergingText& reader
) {
  if (reader.node().kind() != xml_common::NodeKind::Error) {
    throw std::logic_error(
      common::Concat(
        "Expected an error node at the reader cursor, but got ",
        xml_common::NodeToHumanReadableString(reader.node())
      )
    );
  }

  const auto error_node(
    static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
      const xml_common::ErrorNode&
    >(reader.node())
  );

  return DeserializationError(
    common::Utf8ToWstring(
      error_node.cause
    )
  );
}

template <
  typename T
>
std::pair<
  common::optional<T>,
  common::optional<DeserializationError>
> NoInstanceAndDeserializationErrorFromReader(
  xml_common::ReaderMergingText& reader
) {
  if (reader.node().kind() != xml_common::NodeKind::Error) {
    throw std::logic_error(
      common::Concat(
        "Expected an error node at the reader cursor, but got ",
        xml_common::NodeToHumanReadableString(reader.node())
      )
    );
  }

  DeserializationError error = DeserializationErrorFromReader(
    reader
  );

  return std::make_pair(
    common::nullopt,
    common::make_optional<DeserializationError>(
      std::move(error)
    )
  );
}

template <
  typename T
>
std::pair<
  common::optional<T>,
  common::optional<DeserializationError>
> NoInstanceAndDeserializationError(
  DeserializationError error
) {
  return std::make_pair(
    common::nullopt,
    common::make_optional<DeserializationError>(
      std::move(error)
    )
  );
}

void PrependElementSegmentToDeserializationError(
  const std::string& name,
  DeserializationError& deserialization_error
) {
  deserialization_error.path.segments.emplace_front(
    common::make_unique<xml_path::ElementSegment>(
      common::Utf8ToWstring(name)
    )
  );
}

common::optional<DeserializationError> CheckReaderAtEof(
  const xml_common::ReaderMergingText& reader
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in CheckReaderAtEof. "
      "CheckReaderAtEof expects no reader error at entry."
    );
  }
  #endif

  if (reader.node().kind() != xml_common::NodeKind::Eof) {
    return common::make_optional<DeserializationError>(
      common::Concat(
        L"Expected end-of-input, but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );
  }

  return common::nullopt;
}

/**
 * \brief Skip the beginning-of-file (BoF) and read a node.
 *
 * Do nothing if the cursor points to a non-BoF node.
 *
 * Return an error if the reader produced an error.
 */
common::optional<DeserializationError> SkipBof(
  xml_common::ReaderMergingText& reader
) {
  if (reader.node().kind() != xml_common::NodeKind::Bof) {
    return common::nullopt;
  }

  reader.Read();
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return DeserializationErrorFromReader(reader);
  }

  return common::nullopt;
}

/**
 * Return `true` if all characters are whitespace in the UTF-8-encoded text.
 */
bool IsWhitespace(const std::string& utf8_text) {
  for (const char character : utf8_text) {
    switch (character) {
      // NOTE (mristin):
      // The characters are ordered by their ASCII codes so that
      // we allow compilers to optimize.

      // NOTE (mristin):
      // Text nodes contain text in UTF-8 which is compatible with ASCII.
      // In particular, all characters above ASCII (>127) are encoded with
      // all the leading bits set. Hence, it is safe to check for whitespace
      // characters in an UTF-8-encoded string using one-byte characters.
      //
      // See: https://stackoverflow.com/questions/15965811/why-utf8-is-compatible-with-ascii

      case '\t':
      case '\n':
      case '\r':
      case ' ':
        // Pass
        break;
      default:
        return false;
    }
  }

  return true;
}

/**
 * \brief Skip all whitespace text nodes.
 *
 * Do nothing if the cursor points to a non-text node.
 *
 * The whitespace includes space, tab, carriage return and newline.
 *
 * Return an error if the reader produced an error.
 */
common::optional<DeserializationError> SkipWhitespace(
  xml_common::ReaderMergingText& reader
) {
  while (reader.node().kind() == xml_common::NodeKind::Text) {
    const xml_common::TextNode& text_node(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::TextNode&
      >(
        reader.node()
      )
    );

    if (!IsWhitespace(text_node.text)) {
      break;
    }

    reader.Read();
  }

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return DeserializationErrorFromReader(reader);
  }

  return common::nullopt;
}

/**
 * \brief Create the exception to be thrown on an unexpected property literal.
 *
 * Every ``switch`` over the properties of a class covers all the literals of
 * its enumeration, so we can only get here if the value has been corrupted.
 * We report that as a logic error, and not as a de-serialization error, since
 * it does not originate in the input.
 *
 * \param enum_name name of the property enumeration, for the message
 * \param property the unexpected literal
 * \return the exception to be thrown
 */
template <typename EnumT>
std::logic_error UnexpectedPropertyLiteralError(
  const char* enum_name,
  EnumT property
) {
  return std::logic_error(
    common::Concat(
      "Unexpected properties literal of ",
      enum_name,
      ": ",
      std::to_string(
        static_cast<std::uint32_t>(property)
      )
    )
  );
}

/**
 * \brief Read the properties of an instance as a sequence of XML elements.
 *
 * The cursor is expected to point at the content of the element which opens
 * the instance. On success, the cursor points at the stop element closing it,
 * which the caller is expected to consume.
 *
 * This function factors out everything which the property loop of a class does
 * not say about the class it belongs to: skipping the whitespace, recognizing
 * the end of the sequence, reading and consuming the start element, looking
 * the property up, refusing a duplicate, marking the property on the error path
 * and consuming the stop element. The class itself supplies only \p on_property,
 * which dispatches on the property and assigns the local variables that its
 * constructor is finally called with.
 *
 * NOTE (mristin):
 * We take \p map_of_properties in, instead of letting \p on_property work on
 * the XML name of the property, because we want the class to dispatch with
 * a hard-wired ``switch`` whose branches assign the local variables of
 * the caller.
 *
 * A ``switch`` needs an integral constant, and C++ can not switch on a string,
 * so the name has to be translated into a literal of the property enumeration
 * first. We do that here rather than in the class so that the translation, and
 * the error reported when the name matches no property at all, are written
 * once instead of once per class.
 *
 * The alternative -- mapping the name directly to the code which reads
 * the property -- would cost a type-erased, capturing callable per property,
 * built anew on every single read, since the code has to assign the caller's
 * variables. The ``switch`` costs a jump table.
 *
 * \tparam kPropertyCount number of the properties of the class
 * \param reader to read from
 * \param map_of_properties maps the XML name of a property to its literal
 * \param interface_name name of the interface, for the messages
 * \param on_property reads the content of the recognized property
 * \return the error, if the reading failed
 */
template <std::size_t kPropertyCount, typename EnumT, typename OnPropertyT>
common::optional<DeserializationError> ReadProperties(
  xml_common::ReaderMergingText& reader,
  const std::unordered_map<std::string, EnumT>& map_of_properties,
  const wchar_t* interface_name,
  const OnPropertyT& on_property
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in ReadProperties. "
      "ReadProperties expects no reader error at entry."
    );
  }
  #endif

  common::optional<DeserializationError> error;

  error = SkipBof(reader);
  if (error.has_value()) {
    return error;
  }

  // NOTE (mristin):
  // Whether a property has already occurred is a bookkeeping of this loop, and
  // not of the property, so we track it here, indexed by the property literal,
  // instead of asking every single target variable at every single case of
  // the ``switch``.
  //
  // The bit set is sized to the class, so a class may have arbitrarily many
  // properties, and it lives on the stack, so nothing is allocated for it on
  // a read.
  std::bitset<kPropertyCount> seen;

  while (true) {
    error = SkipWhitespace(reader);
    if (error.has_value()) {
      return error;
    }

    if (reader.node().kind() == xml_common::NodeKind::Stop) {
      // NOTE (mristin):
      // We reached a closing element of an instance, so we know that
      // the sequence ended.
      return common::nullopt;
    } else if (reader.node().kind() != xml_common::NodeKind::Start) {
      return DeserializationError(
        common::Concat(
          L"Expected a start element opening a property of ",
          interface_name,
          L", but got ",
          xml_common::NodeToHumanReadableWstring(reader.node())
        )
      );
    }

    const std::string name(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::StartNode&
      >(reader.node()).name
    );

    // NOTE (mristin):
    // We consume the start element.
    reader.Read();

    if (reader.node().kind() == xml_common::NodeKind::Error) {
      error = DeserializationErrorFromReader(reader);

      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }

    auto it = map_of_properties.find(name);
    if (it == map_of_properties.end()) {
      return DeserializationError(
        common::Concat(
          L"Expected a start element opening a property of ",
          interface_name,
          L", but got a start element "
          L"which does not correspond to any of its properties: <",
          common::Utf8ToWstring(name),
          L">"
        )
      );
    }

    const EnumT property(it->second);

    // NOTE (mristin):
    // The literals of a property enumeration run from zero without a gap, and
    // the map maps only to them, so the index is always in the range of
    // the bit set. We check that only in debug builds, and index unchecked
    // otherwise.
    const std::size_t index(
      static_cast<std::size_t>(property)
    );

    #ifdef DEBUG
    if (index >= kPropertyCount) {
      throw std::logic_error(
        common::Concat(
          "Unexpected property index in ReadProperties: ",
          std::to_string(index),
          ", but there are only ",
          std::to_string(kPropertyCount),
          " properties"
        )
      );
    }
    #endif

    // NOTE (mristin):
    // An engaged bit can only have been set by an earlier turn of this loop, so
    // it tells us that the property comes a second time. The check precedes
    // the read, so the duplicate is refused without its content ever being
    // looked at.
    if (seen[index]) {
      error = DuplicatePropertyError(name);

      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }

    seen[index] = true;

    error = on_property(property);
    if (error.has_value()) {
      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }

    error = SkipWhitespace(reader);
    if (error.has_value()) {
      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }

    if (!xml_common::IsStopNodeWithName(reader.node(), name)) {
      error = DeserializationError(
        common::Concat(
          L"Expected a stop element </",
          common::Utf8ToWstring(name),
          L"> closing the property of ",
          interface_name,
          L", but got ",
          xml_common::NodeToHumanReadableWstring(reader.node())
        )
      );

      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }

    // NOTE (mristin):
    // We consume the stop element.
    reader.Read();

    if (reader.node().kind() == xml_common::NodeKind::Error) {
      error = DeserializationErrorFromReader(reader);

      PrependElementSegmentToDeserializationError(
        name,
        *error
      );

      return error;
    }
  }
}

template <typename ValueT, typename DispatchT>
std::pair<
  common::optional<ValueT>,
  common::optional<DeserializationError>
> DeserializeFromElement(
  xml_common::ReaderMergingText& reader,
  const wchar_t* value_name,
  const DispatchT& dispatch
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeFromElement. "
      "DeserializeFromElement expects no reader error at entry."
    );
  }
  #endif

  common::optional<DeserializationError> error;

  error = SkipBof(reader);
  if (error.has_value()) {
    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  error = SkipWhitespace(reader);
  if (error.has_value()) {
    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  if (reader.node().kind() != xml_common::NodeKind::Start) {
    return NoInstanceAndDeserializationErrorWithCause<ValueT>(
      common::Concat(
        L"Expected a start element opening an instance of ",
        value_name,
        L", but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );
  }

  const std::string name(
    static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
      const xml_common::StartNode&
    >(reader.node()).name
  );

  common::optional<types::ModelType> model_type(
    ModelTypeFromElementName(name)
  );
  if (!model_type.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<ValueT>(
      common::Concat(
        L"Unexpected start element as its name does not correspond "
        L"to any model type: ",
        common::Utf8ToWstring(name)
      )
    );
  }

  // NOTE (mristin):
  // We consume the start element.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    auto no_instance_and_error = NoInstanceAndDeserializationErrorFromReader<
      ValueT
    >(
      reader
    );

    PrependElementSegmentToDeserializationError(
      name,
      *(no_instance_and_error.second)
    );

    return no_instance_and_error;
  }

  common::optional<ValueT> instance;
  std::tie(
    instance,
    error
  ) = dispatch(reader, *model_type, name);

  if (error.has_value()) {
    PrependElementSegmentToDeserializationError(
      name,
      *error
    );

    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  error = SkipWhitespace(reader);
  if (error.has_value()) {
    PrependElementSegmentToDeserializationError(
      name,
      *error
    );

    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  if (!xml_common::IsStopNodeWithName(reader.node(), name)) {
    error = DeserializationError(
      common::Concat(
        L"Expected a stop element </",
        common::Utf8ToWstring(name),
        L"> closing an instance of ",
        value_name,
        L", but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );

    PrependElementSegmentToDeserializationError(
      name,
      *error
    );

    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  // NOTE (mristin):
  // We consume the stop element.
  reader.Read();
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    error = DeserializationErrorFromReader(reader);

    PrependElementSegmentToDeserializationError(
      name,
      *error
    );

    return NoInstanceAndDeserializationError<ValueT>(std::move(*error));
  }

  return InstanceAndNoDeserializationError(
    std::move(*instance)
  );
}

template <typename ValueT>
std::pair<
  common::optional<ValueT>,
  common::optional<DeserializationError>
> DeserializeSoleFromElement(
  xml_common::ReaderMergingText& reader,
  const wchar_t* value_name,
  types::ModelType expected_model_type,
  std::pair<
    common::optional<ValueT>,
    common::optional<DeserializationError>
  > (*from_sequence)(xml_common::ReaderMergingText&)
) {
  return DeserializeFromElement<ValueT>(
    reader,
    value_name,
    [value_name, expected_model_type, from_sequence](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<ValueT>,
      common::optional<DeserializationError>
    > {
      if (a_model_type != expected_model_type) {
        return NoInstanceAndDeserializationErrorWithCause<ValueT>(
          common::Concat(
            L"Impossible to de-serialize an instance of ",
            value_name,
            L" from <",
            common::Utf8ToWstring(a_name),
            L">"
          )
        );
      }

      return from_sequence(a_reader);
    }
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IClass>
  >,
  common::optional<DeserializationError>
> ClassFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<
    std::shared_ptr<types::IClass>
  >(
    reader,
    L"IClass",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<std::shared_ptr<types::IClass> >,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kLeaf:
          return LeafFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kOrderedContainer:
          return OrderedContainerFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kUnorderedContainer:
          return UnorderedContainerFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kGlobalAttribute:
          return GlobalAttributeFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kLocalAttribute:
          return LocalAttributeFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kAttributeOperand:
          return AttributeOperandFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kStringLiteral:
          return StringLiteralFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kNumberLiteral:
          return NumberLiteralFromSequence<
            types::IClass
          >(a_reader);
        case types::ModelType::kSomething:
          return SomethingFromSequence<
            types::IClass
          >(a_reader);
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            std::shared_ptr<types::IClass>
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of IClass from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

/**
 * \brief Wrap a de-serialized pointer as a named union's variant alternative.
 *
 * Every implementer of a named union is de-serialized through its own
 * *FromSequence function, and the resulting
 * pair<optional<shared_ptr<T>>, ...> then needs to be wrapped into the
 * union's common::variant alternative of the implementer's most specific
 * root. As the roots may overlap, the alternative is picked explicitly by
 * its index \p Index.
 *
 * \param result the result of a de-serialization call for one implementer
 * \return the result wrapped as a variant, or the propagated error
 */
template <typename VariantT, std::size_t Index, typename T>
std::pair<
  common::optional<VariantT>,
  common::optional<DeserializationError>
> WrapDeserializedAsVariant(
  std::pair<
    common::optional<std::shared_ptr<T> >,
    common::optional<DeserializationError>
  > result
) {
  if (result.first.has_value()) {
    return std::make_pair<
      common::optional<VariantT>,
      common::optional<DeserializationError>
    >(
      VariantT(
        common::in_place_index_t<Index>(),
        std::move(*result.first)
      ),
      common::nullopt
    );
  }

  return std::make_pair<
    common::optional<VariantT>,
    common::optional<DeserializationError>
  >(
    common::nullopt,
    std::move(result.second)
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IElement>
  >,
  common::optional<DeserializationError>
> ElementFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<
    std::shared_ptr<types::IElement>
  >(
    reader,
    L"IElement",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<std::shared_ptr<types::IElement> >,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kLeaf:
          return LeafFromSequence<
            types::IElement
          >(a_reader);
        case types::ModelType::kOrderedContainer:
          return OrderedContainerFromSequence<
            types::IElement
          >(a_reader);
        case types::ModelType::kUnorderedContainer:
          return UnorderedContainerFromSequence<
            types::IElement
          >(a_reader);
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            std::shared_ptr<types::IElement>
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of IElement from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::ILeaf>
  >,
  common::optional<DeserializationError>
> LeafFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::ILeaf>
  >(
    reader,
    L"ILeaf",
    types::ModelType::kLeaf,
    LeafFromSequence<types::ILeaf>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IContainer>
  >,
  common::optional<DeserializationError>
> ContainerFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<
    std::shared_ptr<types::IContainer>
  >(
    reader,
    L"IContainer",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<std::shared_ptr<types::IContainer> >,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kOrderedContainer:
          return OrderedContainerFromSequence<
            types::IContainer
          >(a_reader);
        case types::ModelType::kUnorderedContainer:
          return UnorderedContainerFromSequence<
            types::IContainer
          >(a_reader);
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            std::shared_ptr<types::IContainer>
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of IContainer from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IOrderedContainer>
  >,
  common::optional<DeserializationError>
> OrderedContainerFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::IOrderedContainer>
  >(
    reader,
    L"IOrderedContainer",
    types::ModelType::kOrderedContainer,
    OrderedContainerFromSequence<types::IOrderedContainer>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IUnorderedContainer>
  >,
  common::optional<DeserializationError>
> UnorderedContainerFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::IUnorderedContainer>
  >(
    reader,
    L"IUnorderedContainer",
    types::ModelType::kUnorderedContainer,
    UnorderedContainerFromSequence<types::IUnorderedContainer>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IGlobalAttribute>
  >,
  common::optional<DeserializationError>
> GlobalAttributeFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::IGlobalAttribute>
  >(
    reader,
    L"IGlobalAttribute",
    types::ModelType::kGlobalAttribute,
    GlobalAttributeFromSequence<types::IGlobalAttribute>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::ILocalAttribute>
  >,
  common::optional<DeserializationError>
> LocalAttributeFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::ILocalAttribute>
  >(
    reader,
    L"ILocalAttribute",
    types::ModelType::kLocalAttribute,
    LocalAttributeFromSequence<types::ILocalAttribute>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IAttributeOperand>
  >,
  common::optional<DeserializationError>
> AttributeOperandFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::IAttributeOperand>
  >(
    reader,
    L"IAttributeOperand",
    types::ModelType::kAttributeOperand,
    AttributeOperandFromSequence<types::IAttributeOperand>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::IStringLiteral>
  >,
  common::optional<DeserializationError>
> StringLiteralFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::IStringLiteral>
  >(
    reader,
    L"IStringLiteral",
    types::ModelType::kStringLiteral,
    StringLiteralFromSequence<types::IStringLiteral>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::INumberLiteral>
  >,
  common::optional<DeserializationError>
> NumberLiteralFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::INumberLiteral>
  >(
    reader,
    L"INumberLiteral",
    types::ModelType::kNumberLiteral,
    NumberLiteralFromSequence<types::INumberLiteral>
  );
}

std::pair<
  common::optional<
    std::shared_ptr<types::ISomething>
  >,
  common::optional<DeserializationError>
> SomethingFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeSoleFromElement<
    std::shared_ptr<types::ISomething>
  >(
    reader,
    L"ISomething",
    types::ModelType::kSomething,
    SomethingFromSequence<types::ISomething>
  );
}

std::pair<
  common::optional<types::AttributeItem>,
  common::optional<DeserializationError>
> AttributeItemFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<types::AttributeItem>(
    reader,
    L"AttributeItem",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<types::AttributeItem>,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kGlobalAttribute: {
          return WrapDeserializedAsVariant<
            types::AttributeItem,
            0
          >(
            GlobalAttributeFromSequence<
              types::IGlobalAttribute
            >(a_reader)
          );
        }
        case types::ModelType::kLocalAttribute: {
          return WrapDeserializedAsVariant<
            types::AttributeItem,
            1
          >(
            LocalAttributeFromSequence<
              types::ILocalAttribute
            >(a_reader)
          );
        }
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            types::AttributeItem
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of AttributeItem from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

std::pair<
  common::optional<types::StringValue>,
  common::optional<DeserializationError>
> StringValueFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<types::StringValue>(
    reader,
    L"StringValue",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<types::StringValue>,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kStringLiteral: {
          return WrapDeserializedAsVariant<
            types::StringValue,
            0
          >(
            StringLiteralFromSequence<
              types::IStringLiteral
            >(a_reader)
          );
        }
        case types::ModelType::kAttributeOperand: {
          return WrapDeserializedAsVariant<
            types::StringValue,
            1
          >(
            AttributeOperandFromSequence<
              types::IAttributeOperand
            >(a_reader)
          );
        }
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            types::StringValue
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of StringValue from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

std::pair<
  common::optional<types::Value>,
  common::optional<DeserializationError>
> ValueFromElement(
  xml_common::ReaderMergingText& reader
) {
  return DeserializeFromElement<types::Value>(
    reader,
    L"Value",
    [](
      xml_common::ReaderMergingText& a_reader,
      types::ModelType a_model_type,
      const std::string& a_name
    ) -> std::pair<
      common::optional<types::Value>,
      common::optional<DeserializationError>
    > {
      switch (a_model_type) {
        case types::ModelType::kStringLiteral: {
          return WrapDeserializedAsVariant<
            types::Value,
            0
          >(
            StringLiteralFromSequence<
              types::IStringLiteral
            >(a_reader)
          );
        }
        case types::ModelType::kAttributeOperand: {
          return WrapDeserializedAsVariant<
            types::Value,
            1
          >(
            AttributeOperandFromSequence<
              types::IAttributeOperand
            >(a_reader)
          );
        }
        case types::ModelType::kNumberLiteral: {
          return WrapDeserializedAsVariant<
            types::Value,
            2
          >(
            NumberLiteralFromSequence<
              types::INumberLiteral
            >(a_reader)
          );
        }
        default:
          return NoInstanceAndDeserializationErrorWithCause<
            types::Value
          >(
            common::Concat(
              L"Impossible to de-serialize an instance "
              L"of Value from <",
              common::Utf8ToWstring(a_name),
              L">"
            )
          );
      }
    }
  );
}

// region De-serialize primitives

const std::unordered_map<
  std::string,
  bool
> kTextToBool = {
  {"true", true},
  {"false", false},
  {"1", true},
  {"0", false}
};

std::pair<
  common::optional<bool>,
  common::optional<DeserializationError>
> DeserializeBool(
  xml_common::ReaderMergingText& reader
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeBool. "
      "DeserializeBool expects no error node."
    );
  }
  #endif

  if (reader.node().kind() != xml_common::NodeKind::Text) {
    return NoInstanceAndDeserializationErrorWithCause<bool>(
      common::Concat(
        L"Expected to parse an xs:boolean from XML text, but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );
  }

  const std::string text(
    xml_common::CollapseWhitespace(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::TextNode&
      >(reader.node()).text
    )
  );

  auto it = kTextToBool.find(text);
  if (it == kTextToBool.end()) {
    return NoInstanceAndDeserializationErrorWithCause<bool>(
      common::Concat(
        L"Expected to parse an xs:boolean from text, "
        L"but got an invalid value: ",
        common::Utf8ToWstring(text)
      )
    );
  }

  // NOTE (mristin):
  // We consume the text node.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return NoInstanceAndDeserializationErrorFromReader<bool>(reader);
  }

  return std::make_pair(
    it->second,
    common::nullopt
  );
}

std::pair<
  common::optional<int64_t>,
  common::optional<DeserializationError>
> DeserializeInt64(
  xml_common::ReaderMergingText& reader
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeInt64. "
      "DeserializeInt64 expects no error node."
    );
  }
  #endif

  if (reader.node().kind() != xml_common::NodeKind::Text) {
    return NoInstanceAndDeserializationErrorWithCause<int64_t>(
      common::Concat(
        L"Expected to parse an xs:long from XML text, but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );
  }

  const std::string text(
    xml_common::CollapseWhitespace(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::TextNode&
      >(reader.node()).text
    )
  );

  // NOTE (mristin):
  // The lexical form has to be checked before the text is parsed. The std::sto*
  // family reads far more than XSD admits: it stops at the first character it
  // can not use and answers with what it has read so far, so "0x10" comes out
  // as 0, "1abc" as 1, and "5.0" as 5.
  if (!xml_common::MatchesXsLongNumeral(text)) {
    return NoInstanceAndDeserializationErrorWithCause<int64_t>(
      common::Concat(
        L"Expected to parse an xs:long from text, "
        L"but got an invalid value: ",
        common::Utf8ToWstring(text)
      )
    );
  }

  common::optional<int64_t> deserialized;

  static_assert(
    sizeof(int) == 8
    || sizeof(long) == 8
    || sizeof(long long) == 8,
    "Neither int nor long nor long long are 8 bytes long, "
    "so we do not know how to parse an xs:long."
  );

  try {
    // NOTE (mristin):
    // We remove the warning C4101 in MSVC with constants.
    // See: https://stackoverflow.com/questions/25573996/c4127-conditional-expression-is-constant
    const bool sizeof_int_is_8 = sizeof(int) == 8;
    const bool sizeof_long_is_8 = sizeof(long) == 8;
    const bool sizeof_long_long_is_8 = sizeof(long long) == 8;

    if (sizeof_int_is_8) {
      deserialized = std::stoi(text);
    } else if (sizeof_long_is_8) {
      deserialized = std::stol(text);
    } else if (sizeof_long_long_is_8) {
      deserialized = std::stoll(text);
    }
  } catch (std::invalid_argument&) {
    return NoInstanceAndDeserializationErrorWithCause<int64_t>(
      common::Concat(
        L"Expected to parse an xs:long from text, "
        L"but got an invalid value: ",
        common::Utf8ToWstring(text)
      )
    );
  } catch (std::out_of_range&) {
    return NoInstanceAndDeserializationErrorWithCause<int64_t>(
      common::Concat(
        L"Expected to parse an xs:long from text, "
        L"but got a value out of the xs:long range: ",
        common::Utf8ToWstring(text)
      )
    );
  }

  if (!deserialized.has_value()) {
    throw std::logic_error(
      "Neither int nor long nor long long are 8 bytes long, "
      "but this should have been caught earlier in the static assert"
    );
  }

  // NOTE (mristin):
  // We consume the text node.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return NoInstanceAndDeserializationErrorFromReader<int64_t>(reader);
  }

  return std::make_pair(
    deserialized,
    common::nullopt
  );
}

std::pair<
  common::optional<double>,
  common::optional<DeserializationError>
> DeserializeDouble(
  xml_common::ReaderMergingText& reader
) {
  static_assert(
    sizeof(double) == 8,
    "DeserializeDouble expects double to be 8 bytes, "
    "but the size of the double is not 8 bytes"
  );

  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeDouble. "
      "DeserializeDouble expects no error node."
    );
  }
  #endif

  if (reader.node().kind() != xml_common::NodeKind::Text) {
    return NoInstanceAndDeserializationErrorWithCause<double>(
      common::Concat(
        L"Expected to parse an xs:double from XML text, but got ",
        xml_common::NodeToHumanReadableWstring(reader.node())
      )
    );
  }

  const std::string text(
    xml_common::CollapseWhitespace(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::TextNode&
      >(reader.node()).text
    )
  );

  // NOTE (mristin):
  // XSD names the three special values, and it is case-sensitive about it.
  //
  // See: https://www.w3.org/TR/xmlschema11-2/#double
  double deserialized;

  // NOTE (mristin):
  // "+INF" is read although it is written as "INF": XSD 1.1 admits it, its
  // production being (\+|-)?INF, and being liberal in what we accept costs
  // nothing here.
  if (text == "INF" || text == "+INF") {
    deserialized = std::numeric_limits<double>::infinity();
  } else if (text == "-INF") {
    deserialized = -std::numeric_limits<double>::infinity();
  } else if (text == "NaN") {
    deserialized = std::numeric_limits<double>::quiet_NaN();
  } else {
    // NOTE (mristin):
    // The lexical form has to be checked before the text is parsed.
    // std::stod reads far more than XSD admits: a hexadecimal significand,
    // so "0x10" comes out as 16; a trailing remainder, so "1.0abc" comes
    // out as 1; and the spellings "inf", "infinity", "nan" and "NAN".
    if (!xml_common::MatchesXsDoubleNumeral(text)) {
      return NoInstanceAndDeserializationErrorWithCause<double>(
        common::Concat(
          L"Expected to parse an xs:double from text, "
          L"but got an invalid value: ",
          common::Utf8ToWstring(text)
        )
      );
    }

    // NOTE (mristin):
    // std::stod refuses a literal whose value does not fit a double, by
    // throwing std::out_of_range, and it does so at both ends. XSD asks for
    // neither refusal: a literal too large rounds to an infinity, which is
    // in the value space of xs:double, and one too small rounds to zero.
    // std::strtod gives exactly that, reporting the range through errno,
    // which we deliberately leave alone.
    //
    // The numeral has already been matched, so strtod consumes all of it.
    deserialized = std::strtod(text.c_str(), nullptr);
  }

  // NOTE (mristin):
  // We consume the text node.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return NoInstanceAndDeserializationErrorFromReader<double>(reader);
  }

  return std::make_pair(
    deserialized,
    common::nullopt
  );
}

std::pair<
  common::optional<std::wstring>,
  common::optional<DeserializationError>
> DeserializeWstring(
  xml_common::ReaderMergingText& reader
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeWstring. "
      "DeserializeWstring expects no error node."
    );
  }
  #endif

  switch (reader.node().kind()) {
    case xml_common::NodeKind::Stop:
      // Encountering a stop node means that the string is empty.
      return std::make_pair(std::wstring(), common::nullopt);
    case xml_common::NodeKind::Text:
      // We pass and continue decoding the text.
      break;
    default:
      return NoInstanceAndDeserializationErrorWithCause<std::wstring>(
        common::Concat(
          L"Expected to parse an xs:string from XML text, but got ",
            xml_common::NodeToHumanReadableWstring(reader.node())
          )
        );
  }

  const std::string& text(
    static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
      const xml_common::TextNode&
    >(reader.node()).text
  );

  std::wstring deserialized = common::Utf8ToWstring(text);

  // NOTE (mristin):
  // We consume the text node.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return NoInstanceAndDeserializationErrorFromReader<std::wstring>(reader);
  }

  return std::make_pair(std::move(deserialized), common::nullopt);
}

std::pair<
  common::optional<std::vector<std::uint8_t> >,
  common::optional<DeserializationError>
> DeserializeByteArray(
  xml_common::ReaderMergingText& reader
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeByteArray. "
      "DeserializeByteArray expects no error node."
    );
  }
  #endif

  switch (reader.node().kind()) {
    case xml_common::NodeKind::Stop:
      // Encountering a stop node means empty byte array.
      return std::make_pair(
        std::vector<std::uint8_t>(),
        common::nullopt
      );
    case xml_common::NodeKind::Text:
      // We pass and continue decoding the byte array.
      break;
    default:
      return NoInstanceAndDeserializationErrorWithCause<
        std::vector<std::uint8_t>
      >(
        common::Concat(
          L"Expected to parse an xs:base64Binary from XML text, but got ",
            xml_common::NodeToHumanReadableWstring(reader.node())
          )
        );
  }

  const std::string text(
    xml_common::RemoveWhitespace(
      static_cast<  // NOLINT(cppcoreguidelines-pro-type-static-cast-downcast)
        const xml_common::TextNode&
      >(reader.node()).text
    )
  );

  if (!xml_common::MatchesXsBase64Binary(text)) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::vector<std::uint8_t>
    >(
      common::Concat(
        L"Expected a text as base64-encoded bytes, but got: ",
        common::Utf8ToWstring(text)
      )
    );
  }

  common::expected<
    std::vector<std::uint8_t>,
    std::string
  > deserialized = stringification::Base64Decode(text);

  if (!deserialized.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::vector<std::uint8_t>
    >(
      common::Concat(
        L"Expected to parse an xs:base64Binary from text, "
        L"but the value was invalid: ",
        common::Utf8ToWstring(deserialized.error())
      )
    );
  }

  // NOTE (mristin):
  // We consume the text node.
  reader.Read();

  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return NoInstanceAndDeserializationErrorFromReader<
      std::vector<std::uint8_t>
    >(reader);
  }

  return std::make_pair(
    std::move(*deserialized),
    common::nullopt
  );
}

// endregion De-serialize primitives

template <typename T, typename DeserializeT>
std::pair<
  common::optional<std::vector<T> >,
  common::optional<DeserializationError>
> DeserializeList(
  xml_common::ReaderMergingText& reader,
  const DeserializeT& deserialize_item
) {
  #ifdef DEBUG
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    throw std::logic_error(
      "Unexpected unhandled XML error in DeserializeWstring. "
      "DeserializeWstring expects no error node."
    );
  }
  #endif

  common::optional<DeserializationError> error;

  error = SkipWhitespace(reader);
  if (error.has_value()) {
    return std::make_pair(
      common::nullopt,
      std::move(error)
    );
  }

  // If we encounter the stop element then we reached the end of the list. If this is
  // the first node we encounter then the list is empty, *i.e.*, contains no items.
  if (reader.node().kind() == xml_common::NodeKind::Stop) {
    return std::make_pair(
      std::vector<T>(),
      common::nullopt
    );
  } else {
    // NOTE (mristin):
    // We use std::deque here as it is a buffered list, while a std::list
    // would incur a memory allocation on each push. We do not want to use
    // std::vector as the number of elements in a list can be arbitrarily large
    // leading potentially to out-of-memory errors since std::vector's double
    // their size for amortized time complexity of O(1) for insertions.

    std::deque<T> items;

    size_t i = 0;

    while (true) {
      common::optional<T> item;

      std::tie(
        item,
        error
      ) = deserialize_item(reader);

      if (error.has_value()) {
        error->path.segments.emplace_front(
          common::make_unique<xml_path::IndexSegment>(i)
        );
        break;
      }

      error = SkipWhitespace(reader);
      if (error.has_value()) {
        break;
      }

      items.emplace_back(*item);

      if (reader.node().kind() == xml_common::NodeKind::Stop) {
        break;
      }

      ++i;
    }

    if (!error.has_value()) {
      auto result = std::vector<T>();
      result.reserve(items.size());

      for (auto& item : items) {
        result.emplace_back(
          std::move(item)
        );
    }

      return std::make_pair(
        std::move(result),
        common::nullopt
      );
    } else {
      return std::make_pair(
        common::nullopt,
        std::move(error)
      );
    }
  }
}

/**
 * \brief Assign the value read to \p target, or return the error of the read.
 *
 * We deliberately take the *result* of a read instead of the reader and
 * the function which reads. The item readers of a tuple vary both in number
 * and in type, so no signature taking the reader could serve every read;
 * taking the result lets this single function serve all of them.
 *
 * \param target variable to be assigned the value read
 * \param read result of the read
 * \return the error, if the read failed
 */
template <typename T>
common::optional<DeserializationError> ReadInto(
  common::optional<T>& target,
  std::pair<
    common::optional<T>,
    common::optional<DeserializationError>
  >&& read
) {
  if (read.second.has_value()) {
    return std::move(read.second);
  }

  target = std::move(read.first);

  return common::nullopt;
}

namespace properties {

enum class OfLeaf : std::uint32_t {
  kIdentifier = 0,
  kText = 1
};  // enum class OfLeaf

enum class OfOrderedContainer : std::uint32_t {
  kIdentifier = 0,
  kChildren = 1,
  kIsSorted = 2
};  // enum class OfOrderedContainer

enum class OfUnorderedContainer : std::uint32_t {
  kIdentifier = 0,
  kChildren = 1
};  // enum class OfUnorderedContainer

enum class OfGlobalAttribute : std::uint32_t {
  kKind = 0
};  // enum class OfGlobalAttribute

enum class OfLocalAttribute : std::uint32_t {
  kName = 0
};  // enum class OfLocalAttribute

enum class OfAttributeOperand : std::uint32_t {
  kAttribute = 0
};  // enum class OfAttributeOperand

enum class OfStringLiteral : std::uint32_t {
  kText = 0
};  // enum class OfStringLiteral

enum class OfNumberLiteral : std::uint32_t {
  kNumber = 0
};  // enum class OfNumberLiteral

enum class OfSomething : std::uint32_t {
  kRoot = 0,
  kOptionalElement = 1,
  kValue = 2,
  kValues = 3
};  // enum class OfSomething

const std::size_t kPropertyCountOfLeaf = 2;

const std::unordered_map<
  std::string,
  OfLeaf
> kMapOfLeaf = {
  {
    "identifier",
    OfLeaf::kIdentifier
  },
  {
    "text",
    OfLeaf::kText
  }
};

const std::size_t kPropertyCountOfOrderedContainer = 3;

const std::unordered_map<
  std::string,
  OfOrderedContainer
> kMapOfOrderedContainer = {
  {
    "identifier",
    OfOrderedContainer::kIdentifier
  },
  {
    "children",
    OfOrderedContainer::kChildren
  },
  {
    "isSorted",
    OfOrderedContainer::kIsSorted
  }
};

const std::size_t kPropertyCountOfUnorderedContainer = 2;

const std::unordered_map<
  std::string,
  OfUnorderedContainer
> kMapOfUnorderedContainer = {
  {
    "identifier",
    OfUnorderedContainer::kIdentifier
  },
  {
    "children",
    OfUnorderedContainer::kChildren
  }
};

const std::size_t kPropertyCountOfGlobalAttribute = 1;

const std::unordered_map<
  std::string,
  OfGlobalAttribute
> kMapOfGlobalAttribute = {
  {
    "kind",
    OfGlobalAttribute::kKind
  }
};

const std::size_t kPropertyCountOfLocalAttribute = 1;

const std::unordered_map<
  std::string,
  OfLocalAttribute
> kMapOfLocalAttribute = {
  {
    "name",
    OfLocalAttribute::kName
  }
};

const std::size_t kPropertyCountOfAttributeOperand = 1;

const std::unordered_map<
  std::string,
  OfAttributeOperand
> kMapOfAttributeOperand = {
  {
    "attribute",
    OfAttributeOperand::kAttribute
  }
};

const std::size_t kPropertyCountOfStringLiteral = 1;

const std::unordered_map<
  std::string,
  OfStringLiteral
> kMapOfStringLiteral = {
  {
    "text",
    OfStringLiteral::kText
  }
};

const std::size_t kPropertyCountOfNumberLiteral = 1;

const std::unordered_map<
  std::string,
  OfNumberLiteral
> kMapOfNumberLiteral = {
  {
    "number",
    OfNumberLiteral::kNumber
  }
};

const std::size_t kPropertyCountOfSomething = 4;

const std::unordered_map<
  std::string,
  OfSomething
> kMapOfSomething = {
  {
    "root",
    OfSomething::kRoot
  },
  {
    "optionalElement",
    OfSomething::kOptionalElement
  },
  {
    "value",
    OfSomething::kValue
  },
  {
    "values",
    OfSomething::kValues
  }
};

}  // namespace properties

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ILeaf>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> LeafFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_identifier;

  common::optional<std::wstring> the_text;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfLeaf
    >(
      reader,
      properties::kMapOfLeaf,
      L"ILeaf",
      [&](
        properties::OfLeaf property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfLeaf::kIdentifier:
            return ReadInto(
              the_identifier,
              DeserializeWstring(reader)
            );
          case properties::OfLeaf::kText:
            return ReadInto(
              the_text,
              DeserializeWstring(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfLeaf",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_identifier.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property identifier is missing"
    );
  }

  if (!the_text.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property text is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::Leaf(
        std::move(*the_identifier),
        std::move(*the_text)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IOrderedContainer>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> OrderedContainerFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_identifier;

  common::optional<
    std::vector<
      std::shared_ptr<types::IElement>
    >
  > the_children;

  common::optional<bool> the_is_sorted;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfOrderedContainer
    >(
      reader,
      properties::kMapOfOrderedContainer,
      L"IOrderedContainer",
      [&](
        properties::OfOrderedContainer property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfOrderedContainer::kIdentifier:
            return ReadInto(
              the_identifier,
              DeserializeWstring(reader)
            );
          case properties::OfOrderedContainer::kChildren:
            return ReadInto(
              the_children,
              DeserializeList<
                std::shared_ptr<types::IElement>
              >(
                reader,
                ElementFromElement
              )
            );
          case properties::OfOrderedContainer::kIsSorted:
            return ReadInto(
              the_is_sorted,
              DeserializeBool(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfOrderedContainer",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_identifier.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property identifier is missing"
    );
  }

  if (!the_children.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property children is missing"
    );
  }

  if (!the_is_sorted.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property isSorted is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::OrderedContainer(
        std::move(*the_identifier),
        std::move(*the_children),
        std::move(*the_is_sorted)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IUnorderedContainer>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> UnorderedContainerFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_identifier;

  common::optional<
    std::vector<
      std::shared_ptr<types::IElement>
    >
  > the_children;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfUnorderedContainer
    >(
      reader,
      properties::kMapOfUnorderedContainer,
      L"IUnorderedContainer",
      [&](
        properties::OfUnorderedContainer property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfUnorderedContainer::kIdentifier:
            return ReadInto(
              the_identifier,
              DeserializeWstring(reader)
            );
          case properties::OfUnorderedContainer::kChildren:
            return ReadInto(
              the_children,
              DeserializeList<
                std::shared_ptr<types::IElement>
              >(
                reader,
                ElementFromElement
              )
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfUnorderedContainer",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_identifier.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property identifier is missing"
    );
  }

  if (!the_children.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property children is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::UnorderedContainer(
        std::move(*the_identifier),
        std::move(*the_children)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IGlobalAttribute>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> GlobalAttributeFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_kind;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfGlobalAttribute
    >(
      reader,
      properties::kMapOfGlobalAttribute,
      L"IGlobalAttribute",
      [&](
        properties::OfGlobalAttribute property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfGlobalAttribute::kKind:
            return ReadInto(
              the_kind,
              DeserializeWstring(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfGlobalAttribute",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_kind.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property kind is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::GlobalAttribute(
        std::move(*the_kind)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ILocalAttribute>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> LocalAttributeFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_name;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfLocalAttribute
    >(
      reader,
      properties::kMapOfLocalAttribute,
      L"ILocalAttribute",
      [&](
        properties::OfLocalAttribute property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfLocalAttribute::kName:
            return ReadInto(
              the_name,
              DeserializeWstring(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfLocalAttribute",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_name.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property name is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::LocalAttribute(
        std::move(*the_name)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IAttributeOperand>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> AttributeOperandFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<types::AttributeItem> the_attribute;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfAttributeOperand
    >(
      reader,
      properties::kMapOfAttributeOperand,
      L"IAttributeOperand",
      [&](
        properties::OfAttributeOperand property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfAttributeOperand::kAttribute:
            return ReadInto(
              the_attribute,
              AttributeItemFromElement(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfAttributeOperand",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_attribute.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property attribute is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::AttributeOperand(
        std::move(*the_attribute)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::IStringLiteral>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> StringLiteralFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::wstring> the_text;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfStringLiteral
    >(
      reader,
      properties::kMapOfStringLiteral,
      L"IStringLiteral",
      [&](
        properties::OfStringLiteral property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfStringLiteral::kText:
            return ReadInto(
              the_text,
              DeserializeWstring(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfStringLiteral",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_text.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property text is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::StringLiteral(
        std::move(*the_text)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::INumberLiteral>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> NumberLiteralFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<double> the_number;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfNumberLiteral
    >(
      reader,
      properties::kMapOfNumberLiteral,
      L"INumberLiteral",
      [&](
        properties::OfNumberLiteral property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfNumberLiteral::kNumber:
            return ReadInto(
              the_number,
              DeserializeDouble(reader)
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfNumberLiteral",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_number.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property number is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::NumberLiteral(
        std::move(*the_number)
      )
    ),
    common::nullopt
  );
}

template <
  typename T,
  typename std::enable_if<
    std::is_base_of<T, types::ISomething>::value
  >::type*
>
std::pair<
  common::optional<std::shared_ptr<T> >,
  common::optional<DeserializationError>
> SomethingFromSequence(
  xml_common::ReaderMergingText& reader
) {
  // region Initialization

  common::optional<std::shared_ptr<types::IElement> > the_root;

  common::optional<
    std::shared_ptr<types::IElement>
  > the_optional_element;

  common::optional<types::Value> the_value;

  common::optional<std::vector<types::Value> > the_values;

  // endregion Initialization

  common::optional<DeserializationError> error(
    ReadProperties<
      properties::kPropertyCountOfSomething
    >(
      reader,
      properties::kMapOfSomething,
      L"ISomething",
      [&](
        properties::OfSomething property
      ) -> common::optional<DeserializationError> {
        switch (property) {
          case properties::OfSomething::kRoot:
            return ReadInto(
              the_root,
              ElementFromElement(reader)
            );
          case properties::OfSomething::kOptionalElement:
            return ReadInto(
              the_optional_element,
              ElementFromElement(reader)
            );
          case properties::OfSomething::kValue:
            return ReadInto(
              the_value,
              ValueFromElement(reader)
            );
          case properties::OfSomething::kValues:
            return ReadInto(
              the_values,
              DeserializeList<
                types::Value
              >(
                reader,
                ValueFromElement
              )
            );
          default:
            throw UnexpectedPropertyLiteralError(
              "properties::OfSomething",
              property
            );
        }
      }
    )
  );

  if (error.has_value()) {
    return NoInstanceAndDeserializationError<
      std::shared_ptr<T>
    >(
      std::move(*error)
    );
  }

  // region Check required properties

  if (!the_root.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property root is missing"
    );
  }

  if (!the_value.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property value is missing"
    );
  }

  if (!the_values.has_value()) {
    return NoInstanceAndDeserializationErrorWithCause<
      std::shared_ptr<T>
    >(
      L"The required property values is missing"
    );
  }

  // endregion Check required properties

  return std::make_pair(
    common::make_optional<
      std::shared_ptr<T>
    >(
      // NOTE (mristin):
      // We deliberately do not use std::make_shared here to avoid an unnecessary
      // upcast.
      new types::Something(
        std::move(*the_root),
        std::move(*the_value),
        std::move(*the_values),
        std::move(the_optional_element)
      )
    ),
    common::nullopt
  );
}

template <typename ValueT, typename FromElementT>
common::expected<
  ValueT,
  DeserializationError
> DeserializeFrom(
  std::istream& is,
  const ReadingOptions& options,
  const FromElementT& from_element
) {
  xml_common::ReaderMergingText reader(
    is,
    options.additional_attributes,
    options.buffer_size
  );

  reader.Initialize();
  if (reader.node().kind() == xml_common::NodeKind::Error) {
    return common::make_unexpected(
      DeserializationErrorFromReader(reader)
    );
  }

  common::optional<ValueT> instance;
  common::optional<DeserializationError> error;

  std::tie(
    instance,
    error
  ) = from_element(reader);

  if (error.has_value()) {
    return common::make_unexpected(
      std::move(*error)
    );
  }

  error = SkipWhitespace(reader);
  if (error.has_value()) {
    return common::make_unexpected(
      std::move(*error)
    );
  }

  error = CheckReaderAtEof(reader);
  if (error.has_value()) {
    return common::make_unexpected(
      std::move(*error)
    );
  }

  return std::move(*instance);
}

common::expected<
  std::shared_ptr<types::IClass>,
  DeserializationError
> From(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IClass>
  >(
    is,
    options,
    ClassFromElement
  );
}

common::expected<
  std::shared_ptr<types::IElement>,
  DeserializationError
> ElementFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IElement>
  >(
    is,
    options,
    ElementFromElement
  );
}

common::expected<
  std::shared_ptr<types::ILeaf>,
  DeserializationError
> LeafFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::ILeaf>
  >(
    is,
    options,
    LeafFromElement
  );
}

common::expected<
  std::shared_ptr<types::IContainer>,
  DeserializationError
> ContainerFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IContainer>
  >(
    is,
    options,
    ContainerFromElement
  );
}

common::expected<
  std::shared_ptr<types::IOrderedContainer>,
  DeserializationError
> OrderedContainerFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IOrderedContainer>
  >(
    is,
    options,
    OrderedContainerFromElement
  );
}

common::expected<
  std::shared_ptr<types::IUnorderedContainer>,
  DeserializationError
> UnorderedContainerFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IUnorderedContainer>
  >(
    is,
    options,
    UnorderedContainerFromElement
  );
}

common::expected<
  std::shared_ptr<types::IGlobalAttribute>,
  DeserializationError
> GlobalAttributeFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IGlobalAttribute>
  >(
    is,
    options,
    GlobalAttributeFromElement
  );
}

common::expected<
  std::shared_ptr<types::ILocalAttribute>,
  DeserializationError
> LocalAttributeFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::ILocalAttribute>
  >(
    is,
    options,
    LocalAttributeFromElement
  );
}

common::expected<
  std::shared_ptr<types::IAttributeOperand>,
  DeserializationError
> AttributeOperandFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IAttributeOperand>
  >(
    is,
    options,
    AttributeOperandFromElement
  );
}

common::expected<
  std::shared_ptr<types::IStringLiteral>,
  DeserializationError
> StringLiteralFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::IStringLiteral>
  >(
    is,
    options,
    StringLiteralFromElement
  );
}

common::expected<
  std::shared_ptr<types::INumberLiteral>,
  DeserializationError
> NumberLiteralFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::INumberLiteral>
  >(
    is,
    options,
    NumberLiteralFromElement
  );
}

common::expected<
  std::shared_ptr<types::ISomething>,
  DeserializationError
> SomethingFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    std::shared_ptr<types::ISomething>
  >(
    is,
    options,
    SomethingFromElement
  );
}

common::expected<
  types::AttributeItem,
  DeserializationError
> AttributeItemFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    types::AttributeItem
  >(
    is,
    options,
    AttributeItemFromElement
  );
}

common::expected<
  types::StringValue,
  DeserializationError
> StringValueFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    types::StringValue
  >(
    is,
    options,
    StringValueFromElement
  );
}

common::expected<
  types::Value,
  DeserializationError
> ValueFrom(
  std::istream& is,
  const ReadingOptions& options
) {
  return DeserializeFrom<
    types::Value
  >(
    is,
    options,
    ValueFromElement
  );
}

// endregion De-serialization

// region Serialization

// region SerializationException

std::string RenderSerializationErrorMessage(
  const std::wstring& cause,
  const iteration::Path& path
) {
  return common::WstringToUtf8(
    common::Concat(
      L"Serialization failed at ",
      path.ToWstring(),
      L": ",
      cause
    )
  );
}

SerializationException::SerializationException(
  std::wstring cause
) :
  cause_(std::move(cause)),
  path_(),
  msg_(RenderSerializationErrorMessage(cause, path_)) {
  // Intentionally empty.
}

SerializationException::SerializationException(
  std::wstring cause,
  iteration::Path path
) :
  cause_(std::move(cause)),
  path_(std::move(path)),
  msg_(RenderSerializationErrorMessage(cause, path)) {
  // Intentionally empty.
}

const char* SerializationException::what() const noexcept {
  return msg_.c_str();
}

const std::wstring& SerializationException::cause() const noexcept {
  return cause_;
}

const iteration::Path& SerializationException::path() const noexcept {
  return path_;
}

// endregion SerializationException

common::optional<xml_common::SerializationError> WriteBool(
  bool value,
  xml_common::SelfClosingWriter& writer
) {
  writer.SerializeBool(value);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> WriteInt64(
  int64_t value,
  xml_common::SelfClosingWriter& writer
) {
  writer.SerializeInt64(value);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> WriteDouble(
  double value,
  xml_common::SelfClosingWriter& writer
) {
  writer.SerializeDouble(value);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> WriteWstring(
  const std::wstring& value,
  xml_common::SelfClosingWriter& writer
) {
  writer.SerializeWstring(value);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> WriteByteArray(
  const std::vector<std::uint8_t>& value,
  xml_common::SelfClosingWriter& writer
) {
  writer.SerializeByteArray(value);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

/**
 * \brief Write \p value as an XML element named \p name.
 *
 * This is the only place where an element is framed. The element of a class,
 * the `<v>` of a list item and the positional `<v1>`, `<v2>`, <i>etc.</i> of
 * a tuple item differ only in the name and in the content, so all of them
 * come through here.
 *
 * \param name of the XML element
 * \param value to be written between the tags
 * \param writer to write to
 * \param write_content writes \p value between the tags
 * \return an error, if any
 */
template <typename T, typename WriteT>
common::optional<xml_common::SerializationError> WriteElement(
  const char* name,
  const T& value,
  xml_common::SelfClosingWriter& writer,
  const WriteT& write_content
) {
  writer.StartElement(name);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  common::optional<xml_common::SerializationError> error(
    write_content(value, writer)
  );
  if (error.has_value()) {
    return error;
  }

  writer.StopElement(name);
  if (writer.error().has_value()) {
    return writer.move_error();
  }

  return common::nullopt;
}

/**
 * \brief Write \p value as the XML element of \p property.
 *
 * This is \ref WriteElement plus the one thing which a property knows and
 * nothing beneath it does -- which property of the instance it is -- so that
 * the path of the error is built as the stack unwinds.
 *
 * \param name of the XML element
 * \param value of the property
 * \param writer to write to
 * \param property which the element stands for, for the path of the error
 * \param write_content writes \p value between the tags
 * \return an error, if any
 */
template <typename T, typename WriteT>
common::optional<xml_common::SerializationError> WriteProperty(
  const char* name,
  const T& value,
  xml_common::SelfClosingWriter& writer,
  iteration::Property property,
  const WriteT& write_content
) {
  common::optional<xml_common::SerializationError> error(
    WriteElement(name, value, writer, write_content)
  );

  if (error.has_value()) {
    error->path.segments.emplace_front(
      common::make_unique<iteration::PropertySegment>(property)
    );
  }

  return error;
}

/**
 * \brief Write the instance behind \p value as the XML element of
 * \p property.
 *
 * See the overload which takes the value itself for what is written and
 * for the path of the error.
 */
template <typename T, typename WriteT>
common::optional<xml_common::SerializationError> WriteProperty(
  const char* name,
  const std::shared_ptr<T>& value,
  xml_common::SelfClosingWriter& writer,
  iteration::Property property,
  const WriteT& write_content
) {
  return WriteProperty(name, *value, writer, property, write_content);
}

/**
 * \brief Write \p value as the XML element of \p property, or nothing at
 * all if the property has not been given.
 *
 * See the overload which takes the value itself for what is written and
 * for the path of the error.
 */
template <typename T, typename WriteT>
common::optional<xml_common::SerializationError> WriteProperty(
  const char* name,
  const common::optional<T>& value,
  xml_common::SelfClosingWriter& writer,
  iteration::Property property,
  const WriteT& write_content
) {
  if (!value.has_value()) {
    return common::nullopt;
  }

  return WriteProperty(name, *value, writer, property, write_content);
}

/**
 * \brief Write \p list as the XML element of \p property, every item as
 * an XML element of its own.
 *
 * An instance is self-describing -- the name of its XML element is its model
 * type -- so an item needs no positional tag here. A value encoded as text
 * does need one, which is why a list of values is written by a function of
 * its own instead of by this one with the name of the item passed in.
 *
 * \param name of the XML element
 * \param list of the instances
 * \param writer to write to
 * \param property which the element stands for, for the path of the error
 * \param write_item writes an item as an XML element of its own
 * \return an error, if any
 */
template <typename T, typename WriteItemT>
common::optional<xml_common::SerializationError> WriteListOfInstancesProperty(
  const char* name,
  const std::vector<T>& list,
  xml_common::SelfClosingWriter& writer,
  iteration::Property property,
  const WriteItemT& write_item
) {
  return WriteProperty(
    name,
    list,
    writer,
    property,
    [&write_item](
      const std::vector<T>& a_list,
      xml_common::SelfClosingWriter& a_writer
    ) -> common::optional<xml_common::SerializationError> {
      for (size_t i = 0; i < a_list.size(); ++i) {
        common::optional<xml_common::SerializationError> error(
          write_item(a_list[i], a_writer)
        );

        if (error.has_value()) {
          error->path.segments.emplace_front(
            common::make_unique<iteration::IndexSegment>(i)
          );

          return error;
        }
      }

      return common::nullopt;
    }
  );
}

/**
 * \brief Write \p list as the XML element of \p property, or nothing at all
 * if the property has not been given.
 *
 * See the overload which takes the list itself for what is written and
 * for the path of the error.
 */
template <typename T, typename WriteItemT>
common::optional<xml_common::SerializationError> WriteListOfInstancesProperty(
  const char* name,
  const common::optional<std::vector<T> >& list,
  xml_common::SelfClosingWriter& writer,
  iteration::Property property,
  const WriteItemT& write_item
) {
  if (!list.has_value()) {
    return common::nullopt;
  }

  return WriteListOfInstancesProperty(
    name,
    *list,
    writer,
    property,
    write_item
  );
}

/**
 * \brief Serialize \p that instance by dispatching to the appropriate concrete
 * serialization function.
 *
 * \param that instance to be serialized
 * \param writer to be write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeElementAsElement(
  const types::IElement& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeElementAsElement(const types::IElement&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeElementPtrAsElement(
  const std::shared_ptr<types::IElement>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeLeafAsSequence(
  const types::ILeaf& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<leaf>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeLeafAsElement(
  const types::ILeaf& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeLeafAsElement(const types::ILeaf&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeLeafPtrAsElement(
  const std::shared_ptr<types::ILeaf>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance by dispatching to the appropriate concrete
 * serialization function.
 *
 * \param that instance to be serialized
 * \param writer to be write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeContainerAsElement(
  const types::IContainer& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeContainerAsElement(const types::IContainer&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeContainerPtrAsElement(
  const std::shared_ptr<types::IContainer>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeOrderedContainerAsSequence(
  const types::IOrderedContainer& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<orderedContainer>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeOrderedContainerAsElement(
  const types::IOrderedContainer& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeOrderedContainerAsElement(const types::IOrderedContainer&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeOrderedContainerPtrAsElement(
  const std::shared_ptr<types::IOrderedContainer>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeUnorderedContainerAsSequence(
  const types::IUnorderedContainer& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<unorderedContainer>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeUnorderedContainerAsElement(
  const types::IUnorderedContainer& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeUnorderedContainerAsElement(const types::IUnorderedContainer&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeUnorderedContainerPtrAsElement(
  const std::shared_ptr<types::IUnorderedContainer>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeGlobalAttributeAsSequence(
  const types::IGlobalAttribute& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<globalAttribute>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeGlobalAttributeAsElement(
  const types::IGlobalAttribute& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeGlobalAttributeAsElement(const types::IGlobalAttribute&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeGlobalAttributePtrAsElement(
  const std::shared_ptr<types::IGlobalAttribute>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeLocalAttributeAsSequence(
  const types::ILocalAttribute& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<localAttribute>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeLocalAttributeAsElement(
  const types::ILocalAttribute& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeLocalAttributeAsElement(const types::ILocalAttribute&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeLocalAttributePtrAsElement(
  const std::shared_ptr<types::ILocalAttribute>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeAttributeOperandAsSequence(
  const types::IAttributeOperand& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<attributeOperand>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeAttributeOperandAsElement(
  const types::IAttributeOperand& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeAttributeOperandAsElement(const types::IAttributeOperand&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeAttributeOperandPtrAsElement(
  const std::shared_ptr<types::IAttributeOperand>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeStringLiteralAsSequence(
  const types::IStringLiteral& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<stringLiteral>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeStringLiteralAsElement(
  const types::IStringLiteral& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeStringLiteralAsElement(const types::IStringLiteral&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeStringLiteralPtrAsElement(
  const std::shared_ptr<types::IStringLiteral>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeNumberLiteralAsSequence(
  const types::INumberLiteral& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<numberLiteral>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeNumberLiteralAsElement(
  const types::INumberLiteral& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeNumberLiteralAsElement(const types::INumberLiteral&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeNumberLiteralPtrAsElement(
  const std::shared_ptr<types::INumberLiteral>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeSomethingAsSequence(
  const types::ISomething& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * Serialize \p that instance to an XML element
 * `<something>`.
 *
 * \param that instance to be serialized
 * \return an error, if any
 */
common::optional<xml_common::SerializationError> SerializeSomethingAsElement(
  const types::ISomething& that,
  xml_common::SelfClosingWriter& writer
);

/** @copybrief SerializeSomethingAsElement(const types::ISomething&, xml_common::SelfClosingWriter& */
common::optional<xml_common::SerializationError> SerializeSomethingPtrAsElement(
  const std::shared_ptr<types::ISomething>& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance by dispatching to the appropriate concrete
 * serialization function.
 *
 * \param that instance to be serialized
 * \param writer to be write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeAttributeItemAsElement(
  const types::AttributeItem& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance by dispatching to the appropriate concrete
 * serialization function.
 *
 * \param that instance to be serialized
 * \param writer to be write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeStringValueAsElement(
  const types::StringValue& that,
  xml_common::SelfClosingWriter& writer
);

/**
 * \brief Serialize \p that instance by dispatching to the appropriate concrete
 * serialization function.
 *
 * \param that instance to be serialized
 * \param writer to be write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeValueAsElement(
  const types::Value& that,
  xml_common::SelfClosingWriter& writer
);

common::optional<xml_common::SerializationError> SerializeElementAsElement(
  const types::IElement& that,
  xml_common::SelfClosingWriter& writer
) {
  // NOTE (mristin):
  // The dynamic casts are necessary due to virtual inheritance. Otherwise,
  // we would have used static casts.

  switch (that.model_type()) {
    case types::ModelType::kLeaf:
      return SerializeLeafAsElement(
        dynamic_cast<
          const types::ILeaf&
        >(that),
        writer
      );
    case types::ModelType::kOrderedContainer:
      return SerializeOrderedContainerAsElement(
        dynamic_cast<
          const types::IOrderedContainer&
        >(that),
        writer
      );
    case types::ModelType::kUnorderedContainer:
      return SerializeUnorderedContainerAsElement(
        dynamic_cast<
          const types::IUnorderedContainer&
        >(that),
        writer
      );
    default:
      throw std::invalid_argument(
        common::Concat(
          "Invalid model type: ",
          stringification::to_string(that.model_type())
        )
      );
  };
}

common::optional<xml_common::SerializationError> SerializeElementPtrAsElement(
  const std::shared_ptr<types::IElement>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeElementAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeLeafAsSequence(
  const types::ILeaf& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "identifier",
    that.identifier(),
    writer,
    iteration::Property::kIdentifier,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteProperty(
    "text",
    that.text(),
    writer,
    iteration::Property::kText,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeLeafAsElement(
  const types::ILeaf& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "leaf",
    that,
    writer,
    SerializeLeafAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeLeafPtrAsElement(
  const std::shared_ptr<types::ILeaf>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeLeafAsElement(*that, writer);
}

common::optional<xml_common::SerializationError> SerializeContainerAsElement(
  const types::IContainer& that,
  xml_common::SelfClosingWriter& writer
) {
  // NOTE (mristin):
  // The dynamic casts are necessary due to virtual inheritance. Otherwise,
  // we would have used static casts.

  switch (that.model_type()) {
    case types::ModelType::kOrderedContainer:
      return SerializeOrderedContainerAsElement(
        dynamic_cast<
          const types::IOrderedContainer&
        >(that),
        writer
      );
    case types::ModelType::kUnorderedContainer:
      return SerializeUnorderedContainerAsElement(
        dynamic_cast<
          const types::IUnorderedContainer&
        >(that),
        writer
      );
    default:
      throw std::invalid_argument(
        common::Concat(
          "Invalid model type: ",
          stringification::to_string(that.model_type())
        )
      );
  };
}

common::optional<xml_common::SerializationError> SerializeContainerPtrAsElement(
  const std::shared_ptr<types::IContainer>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeContainerAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeOrderedContainerAsSequence(
  const types::IOrderedContainer& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "identifier",
    that.identifier(),
    writer,
    iteration::Property::kIdentifier,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteListOfInstancesProperty(
    "children",
    that.children(),
    writer,
    iteration::Property::kChildren,
    SerializeElementPtrAsElement
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteProperty(
    "isSorted",
    that.is_sorted(),
    writer,
    iteration::Property::kIsSorted,
    WriteBool
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeOrderedContainerAsElement(
  const types::IOrderedContainer& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "orderedContainer",
    that,
    writer,
    SerializeOrderedContainerAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeOrderedContainerPtrAsElement(
  const std::shared_ptr<types::IOrderedContainer>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeOrderedContainerAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeUnorderedContainerAsSequence(
  const types::IUnorderedContainer& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "identifier",
    that.identifier(),
    writer,
    iteration::Property::kIdentifier,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteListOfInstancesProperty(
    "children",
    that.children(),
    writer,
    iteration::Property::kChildren,
    SerializeElementPtrAsElement
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeUnorderedContainerAsElement(
  const types::IUnorderedContainer& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "unorderedContainer",
    that,
    writer,
    SerializeUnorderedContainerAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeUnorderedContainerPtrAsElement(
  const std::shared_ptr<types::IUnorderedContainer>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeUnorderedContainerAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeGlobalAttributeAsSequence(
  const types::IGlobalAttribute& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "kind",
    that.kind(),
    writer,
    iteration::Property::kKind,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeGlobalAttributeAsElement(
  const types::IGlobalAttribute& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "globalAttribute",
    that,
    writer,
    SerializeGlobalAttributeAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeGlobalAttributePtrAsElement(
  const std::shared_ptr<types::IGlobalAttribute>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeGlobalAttributeAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeLocalAttributeAsSequence(
  const types::ILocalAttribute& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "name",
    that.name(),
    writer,
    iteration::Property::kName,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeLocalAttributeAsElement(
  const types::ILocalAttribute& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "localAttribute",
    that,
    writer,
    SerializeLocalAttributeAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeLocalAttributePtrAsElement(
  const std::shared_ptr<types::ILocalAttribute>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeLocalAttributeAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeAttributeOperandAsSequence(
  const types::IAttributeOperand& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "attribute",
    that.attribute(),
    writer,
    iteration::Property::kAttribute,
    SerializeAttributeItemAsElement
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeAttributeOperandAsElement(
  const types::IAttributeOperand& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "attributeOperand",
    that,
    writer,
    SerializeAttributeOperandAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeAttributeOperandPtrAsElement(
  const std::shared_ptr<types::IAttributeOperand>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeAttributeOperandAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeStringLiteralAsSequence(
  const types::IStringLiteral& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "text",
    that.text(),
    writer,
    iteration::Property::kText,
    WriteWstring
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeStringLiteralAsElement(
  const types::IStringLiteral& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "stringLiteral",
    that,
    writer,
    SerializeStringLiteralAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeStringLiteralPtrAsElement(
  const std::shared_ptr<types::IStringLiteral>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeStringLiteralAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeNumberLiteralAsSequence(
  const types::INumberLiteral& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "number",
    that.number(),
    writer,
    iteration::Property::kNumber,
    WriteDouble
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeNumberLiteralAsElement(
  const types::INumberLiteral& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "numberLiteral",
    that,
    writer,
    SerializeNumberLiteralAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeNumberLiteralPtrAsElement(
  const std::shared_ptr<types::INumberLiteral>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeNumberLiteralAsElement(*that, writer);
}

/**
 * \brief Serialize \p that instance as a sequence of XML elements.
 *
 * Each XML element corresponds to a property.
 *
 * \param that instance to be serialized
 * \param writer to write to
 * \return error, if any
 */
common::optional<xml_common::SerializationError> SerializeSomethingAsSequence(
  const types::ISomething& that,
  xml_common::SelfClosingWriter& writer
) {
  common::optional<xml_common::SerializationError> error;

  error = WriteProperty(
    "root",
    that.root(),
    writer,
    iteration::Property::kRoot,
    SerializeElementAsElement
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteProperty(
    "optionalElement",
    that.optional_element(),
    writer,
    iteration::Property::kOptionalElement,
    SerializeElementAsElement
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteProperty(
    "value",
    that.value(),
    writer,
    iteration::Property::kValue,
    SerializeValueAsElement
  );
  if (error.has_value()) {
    return error;
  }

  error = WriteListOfInstancesProperty(
    "values",
    that.values(),
    writer,
    iteration::Property::kValues,
    SerializeValueAsElement
  );
  if (error.has_value()) {
    return error;
  }

  return common::nullopt;
}

common::optional<xml_common::SerializationError> SerializeSomethingAsElement(
  const types::ISomething& that,
  xml_common::SelfClosingWriter& writer
) {
  return WriteElement(
    "something",
    that,
    writer,
    SerializeSomethingAsSequence
  );
}

common::optional<xml_common::SerializationError> SerializeSomethingPtrAsElement(
  const std::shared_ptr<types::ISomething>& that,
  xml_common::SelfClosingWriter& writer
) {
  return SerializeSomethingAsElement(*that, writer);
}

common::optional<xml_common::SerializationError> SerializeAttributeItemAsElement(
  const types::AttributeItem& that,
  xml_common::SelfClosingWriter& writer
) {
  switch (that.index()) {
    case 0:
      return SerializeGlobalAttributePtrAsElement(
        common::get<0>(that),
        writer
      );
    case 1:
      return SerializeLocalAttributePtrAsElement(
        common::get<1>(that),
        writer
      );
    default:
      throw std::logic_error(
        common::Concat(
          "Invalid variant index for AttributeItem: ",
          std::to_string(that.index())
        )
      );
  };
}

common::optional<xml_common::SerializationError> SerializeStringValueAsElement(
  const types::StringValue& that,
  xml_common::SelfClosingWriter& writer
) {
  switch (that.index()) {
    case 0:
      return SerializeStringLiteralPtrAsElement(
        common::get<0>(that),
        writer
      );
    case 1:
      return SerializeAttributeOperandPtrAsElement(
        common::get<1>(that),
        writer
      );
    default:
      throw std::logic_error(
        common::Concat(
          "Invalid variant index for StringValue: ",
          std::to_string(that.index())
        )
      );
  };
}

common::optional<xml_common::SerializationError> SerializeValueAsElement(
  const types::Value& that,
  xml_common::SelfClosingWriter& writer
) {
  switch (that.index()) {
    case 0:
      return SerializeStringLiteralPtrAsElement(
        common::get<0>(that),
        writer
      );
    case 1:
      return SerializeAttributeOperandPtrAsElement(
        common::get<1>(that),
        writer
      );
    case 2:
      return SerializeNumberLiteralPtrAsElement(
        common::get<2>(that),
        writer
      );
    default:
      throw std::logic_error(
        common::Concat(
          "Invalid variant index for Value: ",
          std::to_string(that.index())
        )
      );
  };
}

common::optional<xml_common::SerializationError> WriteClass(
  const types::IClass& that,
  xml_common::SelfClosingWriter& writer
) {
  // NOTE (mristin):
  // The dynamic casts are necessary due to virtual inheritance. Otherwise,
  // we would have used static casts.

  switch (that.model_type()) {
    case types::ModelType::kLeaf:
      return SerializeLeafAsElement(
        dynamic_cast<
          const types::ILeaf&
        >(that),
        writer
      );
    case types::ModelType::kOrderedContainer:
      return SerializeOrderedContainerAsElement(
        dynamic_cast<
          const types::IOrderedContainer&
        >(that),
        writer
      );
    case types::ModelType::kUnorderedContainer:
      return SerializeUnorderedContainerAsElement(
        dynamic_cast<
          const types::IUnorderedContainer&
        >(that),
        writer
      );
    case types::ModelType::kGlobalAttribute:
      return SerializeGlobalAttributeAsElement(
        dynamic_cast<
          const types::IGlobalAttribute&
        >(that),
        writer
      );
    case types::ModelType::kLocalAttribute:
      return SerializeLocalAttributeAsElement(
        dynamic_cast<
          const types::ILocalAttribute&
        >(that),
        writer
      );
    case types::ModelType::kAttributeOperand:
      return SerializeAttributeOperandAsElement(
        dynamic_cast<
          const types::IAttributeOperand&
        >(that),
        writer
      );
    case types::ModelType::kStringLiteral:
      return SerializeStringLiteralAsElement(
        dynamic_cast<
          const types::IStringLiteral&
        >(that),
        writer
      );
    case types::ModelType::kNumberLiteral:
      return SerializeNumberLiteralAsElement(
        dynamic_cast<
          const types::INumberLiteral&
        >(that),
        writer
      );
    case types::ModelType::kSomething:
      return SerializeSomethingAsElement(
        dynamic_cast<
          const types::ISomething&
        >(that),
        writer
      );
    default:
      throw std::invalid_argument(
        common::Concat(
          "Invalid model type: ",
          stringification::to_string(that.model_type())
        )
      );
  };
}

void Serialize(
  const types::IClass& that,
  const WritingOptions& options,
  std::ostream& os
) {
  if (options.write_declaration) {
    os << "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n";
    if (os.bad()) {
      throw SerializationException(
        xml_common::kTheOutputStreamIsInABadState
      );
    }
  }

  // NOTE (mristin):
  // The namespace is declared on the root element and on no other one, so we
  // hand it to the writer instead of asking at every single element whether it
  // is the root. The writer takes the attributes at the very first element and
  // leaves nothing behind.
  xml_common::SelfClosingWriter writer(
    os,
    options.write_namespace
      ? " xmlns=\"https://dummy.com\""
      : ""
  );

  common::optional<xml_common::SerializationError> error(
    WriteClass(that, writer)
  );

  if (error.has_value()) {
    throw SerializationException(
      std::move(error->cause),
      std::move(error->path)
    );
  }
}

// endregion Serialization

}  // namespace xmlization
}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
