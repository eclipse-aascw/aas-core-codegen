// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/types.hpp"
#include "dummy/stringification.hpp"
#include "dummy/visitation.hpp"

#pragma warning(push, 0)
#include <sstream>
#pragma warning(pop)

namespace dummy {
namespace visitation {

// region AbstractVisitor

void AbstractVisitor::Visit(
  const std::shared_ptr<types::IClass>& that
) {
  // NOTE (mristin):
  // We have to dynamically cast the pointers due to the virtual multiple
  // inheritance, and also because we used shared pointers for references.
  // If we used constant references instead of shared pointers, we could use
  // a pattern such as double dispatch. However, this has the limitation that
  // it would prevent us from collecting the instances in the visitor, such that
  // they outlive the original structures.

  switch (that->model_type()) {
    case types::ModelType::kLeaf:
      VisitLeaf(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that)
      );
      break;
    case types::ModelType::kOrderedContainer:
      VisitOrderedContainer(
        std::dynamic_pointer_cast<
          types::IOrderedContainer
        >(that)
      );
      break;
    case types::ModelType::kUnorderedContainer:
      VisitUnorderedContainer(
        std::dynamic_pointer_cast<
          types::IUnorderedContainer
        >(that)
      );
      break;
    case types::ModelType::kGlobalAttribute:
      VisitGlobalAttribute(
        std::dynamic_pointer_cast<
          types::IGlobalAttribute
        >(that)
      );
      break;
    case types::ModelType::kLocalAttribute:
      VisitLocalAttribute(
        std::dynamic_pointer_cast<
          types::ILocalAttribute
        >(that)
      );
      break;
    case types::ModelType::kAttributeOperand:
      VisitAttributeOperand(
        std::dynamic_pointer_cast<
          types::IAttributeOperand
        >(that)
      );
      break;
    case types::ModelType::kStringLiteral:
      VisitStringLiteral(
        std::dynamic_pointer_cast<
          types::IStringLiteral
        >(that)
      );
      break;
    case types::ModelType::kNumberLiteral:
      VisitNumberLiteral(
        std::dynamic_pointer_cast<
          types::INumberLiteral
        >(that)
      );
      break;
    case types::ModelType::kSomething:
      VisitSomething(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that)
      );
      break;
    default:
      throw std::logic_error(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(that->model_type())
          )
        )
      );
  }
}

// endregion

// region PassThroughVisitor

void PassThroughVisitor::VisitLeaf(
  const std::shared_ptr<types::ILeaf>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitOrderedContainer(
  const std::shared_ptr<types::IOrderedContainer>& that
) {
  // mutable_children
  for (
    const std::shared_ptr<types::IElement>& item :
    that->mutable_children()
  ) {
    Visit(item);
  }
}

void PassThroughVisitor::VisitUnorderedContainer(
  const std::shared_ptr<types::IUnorderedContainer>& that
) {
  // mutable_children
  for (
    const std::shared_ptr<types::IElement>& item :
    that->mutable_children()
  ) {
    Visit(item);
  }
}

void PassThroughVisitor::VisitGlobalAttribute(
  const std::shared_ptr<types::IGlobalAttribute>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitLocalAttribute(
  const std::shared_ptr<types::ILocalAttribute>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitAttributeOperand(
  const std::shared_ptr<types::IAttributeOperand>& that
) {
  // mutable_attribute
  switch (
    (that->mutable_attribute()).index()
  ) {
    case 0:
      Visit(
        common::get<0>(
          that->mutable_attribute()
        )
      );
      break;
    case 1:
      Visit(
        common::get<1>(
          that->mutable_attribute()
        )
      );
      break;
    default:
      throw std::logic_error("Invalid variant index");
  }
}

void PassThroughVisitor::VisitStringLiteral(
  const std::shared_ptr<types::IStringLiteral>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitNumberLiteral(
  const std::shared_ptr<types::INumberLiteral>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitSomething(
  const std::shared_ptr<types::ISomething>& that
) {
  // mutable_root
  Visit(
    that->mutable_root()
  );

  // region mutable_optional_element
  const common::optional<
    std::shared_ptr<types::IElement>
  >& maybe_optional_element(
    that->mutable_optional_element()
  );
  if (maybe_optional_element.has_value()) {
    Visit(
      maybe_optional_element.value()
    );
  }
  // endregion

  // mutable_value
  switch (
    (that->mutable_value()).index()
  ) {
    case 0:
      Visit(
        common::get<0>(
          that->mutable_value()
        )
      );
      break;
    case 1:
      Visit(
        common::get<1>(
          that->mutable_value()
        )
      );
      break;
    case 2:
      Visit(
        common::get<2>(
          that->mutable_value()
        )
      );
      break;
    default:
      throw std::logic_error("Invalid variant index");
  }

  // mutable_values
  for (
    const types::Value& item :
    that->mutable_values()
  ) {
    switch (
      (item).index()
    ) {
      case 0:
        Visit(
          common::get<0>(
            item
          )
        );
        break;
      case 1:
        Visit(
          common::get<1>(
            item
          )
        );
        break;
      case 2:
        Visit(
          common::get<2>(
            item
          )
        );
        break;
      default:
        throw std::logic_error("Invalid variant index");
    }
  }
}

// endregion

}  // namespace visitation
}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
