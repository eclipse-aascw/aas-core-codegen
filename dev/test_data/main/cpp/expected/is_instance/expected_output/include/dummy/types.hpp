#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kLeaf = 0,
  kOrderedContainer = 1,
  kUnorderedContainer = 2,
  kGlobalAttribute = 3,
  kLocalAttribute = 4,
  kAttributeOperand = 5,
  kStringLiteral = 6,
  kNumberLiteral = 7,
  kSomething = 8
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IElement;

class ILeaf;

class IContainer;

class IOrderedContainer;

class IUnorderedContainer;

class IGlobalAttribute;

class ILocalAttribute;

class IAttributeOperand;

class IStringLiteral;

class INumberLiteral;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

// region Named unions

/**
 * Represent a union of classes.
 */
using AttributeItem = common::variant<
    std::shared_ptr<IGlobalAttribute>,
    std::shared_ptr<ILocalAttribute>
  >;

/**
 * Represent a union of classes.
 */
using StringValue = common::variant<
    std::shared_ptr<IStringLiteral>,
    std::shared_ptr<IAttributeOperand>
  >;

/**
 * Represent a union of classes.
 */
using Value = common::variant<
    std::shared_ptr<IStringLiteral>,
    std::shared_ptr<IAttributeOperand>,
    std::shared_ptr<INumberLiteral>
  >;

// endregion

class IElement
    : virtual public IClass {
 public:
  virtual const std::wstring& identifier() const = 0;

  virtual std::wstring& mutable_identifier() = 0;

  virtual void set_identifier(
    std::wstring value
  ) = 0;

  virtual ~IElement() = default;
};

class ILeaf
    : virtual public IElement {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~ILeaf() = default;
};

class IContainer
    : virtual public IElement {
 public:
  virtual const std::vector<
    std::shared_ptr<IElement>
  >& children() const = 0;

  virtual std::vector<
    std::shared_ptr<IElement>
  >& mutable_children() = 0;

  virtual void set_children(
    std::vector<
      std::shared_ptr<IElement>
    > value
  ) = 0;

  virtual ~IContainer() = default;
};

class IOrderedContainer
    : virtual public IContainer {
 public:
  virtual bool is_sorted() const = 0;

  virtual bool& mutable_is_sorted() = 0;

  virtual void set_is_sorted(
    bool value
  ) = 0;

  virtual ~IOrderedContainer() = default;
};

class IUnorderedContainer
    : virtual public IContainer {
 public:
  virtual ~IUnorderedContainer() = default;
};

class IGlobalAttribute
    : virtual public IClass {
 public:
  virtual const std::wstring& kind() const = 0;

  virtual std::wstring& mutable_kind() = 0;

  virtual void set_kind(
    std::wstring value
  ) = 0;

  virtual ~IGlobalAttribute() = default;
};

class ILocalAttribute
    : virtual public IClass {
 public:
  virtual const std::wstring& name() const = 0;

  virtual std::wstring& mutable_name() = 0;

  virtual void set_name(
    std::wstring value
  ) = 0;

  virtual ~ILocalAttribute() = default;
};

class IAttributeOperand
    : virtual public IClass {
 public:
  virtual const AttributeItem& attribute() const = 0;

  virtual AttributeItem& mutable_attribute() = 0;

  virtual void set_attribute(
    AttributeItem value
  ) = 0;

  virtual ~IAttributeOperand() = default;
};

class IStringLiteral
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IStringLiteral() = default;
};

class INumberLiteral
    : virtual public IClass {
 public:
  virtual double number() const = 0;

  virtual double& mutable_number() = 0;

  virtual void set_number(
    double value
  ) = 0;

  virtual ~INumberLiteral() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::shared_ptr<IElement>& root() const = 0;

  virtual std::shared_ptr<IElement>& mutable_root() = 0;

  virtual void set_root(
    std::shared_ptr<IElement> value
  ) = 0;

  virtual const common::optional<
    std::shared_ptr<IElement>
  >& optional_element() const = 0;

  virtual common::optional<
    std::shared_ptr<IElement>
  >& mutable_optional_element() = 0;

  virtual void set_optional_element(
    common::optional<
      std::shared_ptr<IElement>
    > value
  ) = 0;

  virtual const Value& value() const = 0;

  virtual Value& mutable_value() = 0;

  virtual void set_value(
    Value value
  ) = 0;

  virtual const std::vector<Value>& values() const = 0;

  virtual std::vector<Value>& mutable_values() = 0;

  virtual void set_values(
    std::vector<Value> value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Leaf
    : public ILeaf {
 public:
  Leaf(
    std::wstring identifier,
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~Leaf() override = default;

 private:
  std::wstring identifier_;

  std::wstring text_;
};

class OrderedContainer
    : public IOrderedContainer {
 public:
  OrderedContainer(
    std::wstring identifier,
    std::vector<
      std::shared_ptr<IElement>
    > children,
    bool is_sorted
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set children_

  const std::vector<
    std::shared_ptr<IElement>
  >& children() const override;

  std::vector<
    std::shared_ptr<IElement>
  >& mutable_children() override;

  void set_children(
    std::vector<
      std::shared_ptr<IElement>
    > value
  ) override;

  // endregion

  // region Get and set is_sorted_

  bool is_sorted() const override;

  bool& mutable_is_sorted() override;

  void set_is_sorted(
    bool value
  ) override;

  // endregion

  ~OrderedContainer() override = default;

 private:
  std::wstring identifier_;

  std::vector<
    std::shared_ptr<IElement>
  > children_;

  bool is_sorted_;
};

class UnorderedContainer
    : public IUnorderedContainer {
 public:
  UnorderedContainer(
    std::wstring identifier,
    std::vector<
      std::shared_ptr<IElement>
    > children
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set children_

  const std::vector<
    std::shared_ptr<IElement>
  >& children() const override;

  std::vector<
    std::shared_ptr<IElement>
  >& mutable_children() override;

  void set_children(
    std::vector<
      std::shared_ptr<IElement>
    > value
  ) override;

  // endregion

  ~UnorderedContainer() override = default;

 private:
  std::wstring identifier_;

  std::vector<
    std::shared_ptr<IElement>
  > children_;
};

class GlobalAttribute
    : public IGlobalAttribute {
 public:
  explicit GlobalAttribute(
    std::wstring kind
  );

  ModelType model_type() const override;

  // region Get and set kind_

  const std::wstring& kind() const override;

  std::wstring& mutable_kind() override;

  void set_kind(
    std::wstring value
  ) override;

  // endregion

  ~GlobalAttribute() override = default;

 private:
  std::wstring kind_;
};

class LocalAttribute
    : public ILocalAttribute {
 public:
  explicit LocalAttribute(
    std::wstring name
  );

  ModelType model_type() const override;

  // region Get and set name_

  const std::wstring& name() const override;

  std::wstring& mutable_name() override;

  void set_name(
    std::wstring value
  ) override;

  // endregion

  ~LocalAttribute() override = default;

 private:
  std::wstring name_;
};

class AttributeOperand
    : public IAttributeOperand {
 public:
  explicit AttributeOperand(
    AttributeItem attribute
  );

  ModelType model_type() const override;

  // region Get and set attribute_

  const AttributeItem& attribute() const override;

  AttributeItem& mutable_attribute() override;

  void set_attribute(
    AttributeItem value
  ) override;

  // endregion

  ~AttributeOperand() override = default;

 private:
  AttributeItem attribute_;
};

class StringLiteral
    : public IStringLiteral {
 public:
  explicit StringLiteral(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~StringLiteral() override = default;

 private:
  std::wstring text_;
};

class NumberLiteral
    : public INumberLiteral {
 public:
  explicit NumberLiteral(
    double number
  );

  ModelType model_type() const override;

  // region Get and set number_

  double number() const override;

  double& mutable_number() override;

  void set_number(
    double value
  ) override;

  // endregion

  ~NumberLiteral() override = default;

 private:
  double number_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::shared_ptr<IElement> root,
    Value value,
    std::vector<Value> values,
    common::optional<
      std::shared_ptr<IElement>
    > optional_element = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set root_

  const std::shared_ptr<IElement>& root() const override;

  std::shared_ptr<IElement>& mutable_root() override;

  void set_root(
    std::shared_ptr<IElement> value
  ) override;

  // endregion

  // region Get and set optional_element_

  const common::optional<
    std::shared_ptr<IElement>
  >& optional_element() const override;

  common::optional<
    std::shared_ptr<IElement>
  >& mutable_optional_element() override;

  void set_optional_element(
    common::optional<
      std::shared_ptr<IElement>
    > value
  ) override;

  // endregion

  // region Get and set value_

  const Value& value() const override;

  Value& mutable_value() override;

  void set_value(
    Value value
  ) override;

  // endregion

  // region Get and set values_

  const std::vector<Value>& values() const override;

  std::vector<Value>& mutable_values() override;

  void set_values(
    std::vector<Value> value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::shared_ptr<IElement> root_;

  common::optional<
    std::shared_ptr<IElement>
  > optional_element_;

  Value value_;

  std::vector<Value> values_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IElement.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IElement
 */
bool IsElement(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ILeaf.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ILeaf
 */
bool IsLeaf(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IContainer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IContainer
 */
bool IsContainer(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IOrderedContainer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IOrderedContainer
 */
bool IsOrderedContainer(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IUnorderedContainer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IUnorderedContainer
 */
bool IsUnorderedContainer(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IGlobalAttribute.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IGlobalAttribute
 */
bool IsGlobalAttribute(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ILocalAttribute.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ILocalAttribute
 */
bool IsLocalAttribute(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IAttributeOperand.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IAttributeOperand
 */
bool IsAttributeOperand(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IStringLiteral.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IStringLiteral
 */
bool IsStringLiteral(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref INumberLiteral.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref INumberLiteral
 */
bool IsNumberLiteral(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

// region Underlying instances of named unions

/**
 * \brief Extract the instance held in \p that named union.
 *
 * The named union is a variant over its roots. The instance is returned as
 * \ref IClass, so that it can be checked with the is-a functions, and
 * down-cast with `std::dynamic_pointer_cast`.
 *
 * \param that named union holding the instance
 * \return the instance held in \p that named union
 */
std::shared_ptr<IClass> UnderlyingOfAttributeItem(
  const AttributeItem& that
);

/**
 * \brief Extract the instance held in \p that named union.
 *
 * The named union is a variant over its roots. The instance is returned as
 * \ref IClass, so that it can be checked with the is-a functions, and
 * down-cast with `std::dynamic_pointer_cast`.
 *
 * \param that named union holding the instance
 * \return the instance held in \p that named union
 */
std::shared_ptr<IClass> UnderlyingOfStringValue(
  const StringValue& that
);

/**
 * \brief Extract the instance held in \p that named union.
 *
 * The named union is a variant over its roots. The instance is returned as
 * \ref IClass, so that it can be checked with the is-a functions, and
 * down-cast with `std::dynamic_pointer_cast`.
 *
 * \param that named union holding the instance
 * \return the instance held in \p that named union
 */
std::shared_ptr<IClass> UnderlyingOfValue(
  const Value& that
);

// endregion Underlying instances of named unions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
