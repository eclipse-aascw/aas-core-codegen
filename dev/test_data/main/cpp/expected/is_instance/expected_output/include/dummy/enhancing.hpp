#ifndef DUMMY_ENHANCING_GUARD_
#define DUMMY_ENHANCING_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"
#include "dummy/stringification.hpp"
#include "dummy/types.hpp"

#pragma warning(push, 0)
#include <sstream>
#include <stdexcept>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup enhancing Enhance instances of the model with your custom enhancements.
 * @{
 */
namespace enhancing {

// region Forward declarations

template <typename E>
std::shared_ptr<
  types::IElement
> Wrap(
  const std::shared_ptr<
    types::IElement
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ILeaf
> Wrap(
  const std::shared_ptr<
    types::ILeaf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IContainer
> Wrap(
  const std::shared_ptr<
    types::IContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IOrderedContainer
> Wrap(
  const std::shared_ptr<
    types::IOrderedContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IUnorderedContainer
> Wrap(
  const std::shared_ptr<
    types::IUnorderedContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IGlobalAttribute
> Wrap(
  const std::shared_ptr<
    types::IGlobalAttribute
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ILocalAttribute
> Wrap(
  const std::shared_ptr<
    types::ILocalAttribute
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IAttributeOperand
> Wrap(
  const std::shared_ptr<
    types::IAttributeOperand
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IStringLiteral
> Wrap(
  const std::shared_ptr<
    types::IStringLiteral
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::INumberLiteral
> Wrap(
  const std::shared_ptr<
    types::INumberLiteral
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
types::AttributeItem Wrap(
  const types::AttributeItem& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
types::StringValue Wrap(
  const types::StringValue& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
types::Value Wrap(
  const types::Value& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

// endregion Forward declarations

/// \cond HIDDEN
namespace impl {

template<typename E>
class IEnhanced {
 public:
  virtual const std::shared_ptr<E>& enhancement() const = 0;

  virtual std::shared_ptr<E>& mutable_enhancement() = 0;

  virtual void set_enhancement(
    std::shared_ptr<E> value
  ) = 0;

  virtual ~IEnhanced() = default;
};

template<class E>
class EnhancedLeaf
    : virtual public types::ILeaf,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kLeaf;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedLeaf(
    std::shared_ptr<types::ILeaf> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedLeaf() = default;

 private:
  std::shared_ptr<types::ILeaf> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedOrderedContainer
    : virtual public types::IOrderedContainer,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kOrderedContainer;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::vector<
    std::shared_ptr<types::IElement>
  >& children() const override {
    return instance_->children();
  }

  std::vector<
    std::shared_ptr<types::IElement>
  >& mutable_children() override {
    return instance_->mutable_children();
  }

  void set_children(
    std::vector<
      std::shared_ptr<types::IElement>
    > value
  ) override {
    instance_->set_children(value);
  }

  bool is_sorted() const override {
    return instance_->is_sorted();
  }

  bool& mutable_is_sorted() override {
    return instance_->mutable_is_sorted();
  }

  void set_is_sorted(
    bool value
  ) override {
    instance_->set_is_sorted(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedOrderedContainer(
    std::shared_ptr<types::IOrderedContainer> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedOrderedContainer() = default;

 private:
  std::shared_ptr<types::IOrderedContainer> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedUnorderedContainer
    : virtual public types::IUnorderedContainer,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kUnorderedContainer;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::vector<
    std::shared_ptr<types::IElement>
  >& children() const override {
    return instance_->children();
  }

  std::vector<
    std::shared_ptr<types::IElement>
  >& mutable_children() override {
    return instance_->mutable_children();
  }

  void set_children(
    std::vector<
      std::shared_ptr<types::IElement>
    > value
  ) override {
    instance_->set_children(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedUnorderedContainer(
    std::shared_ptr<types::IUnorderedContainer> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedUnorderedContainer() = default;

 private:
  std::shared_ptr<types::IUnorderedContainer> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedGlobalAttribute
    : virtual public types::IGlobalAttribute,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kGlobalAttribute;
  }

  const std::wstring& kind() const override {
    return instance_->kind();
  }

  std::wstring& mutable_kind() override {
    return instance_->mutable_kind();
  }

  void set_kind(
    std::wstring value
  ) override {
    instance_->set_kind(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedGlobalAttribute(
    std::shared_ptr<types::IGlobalAttribute> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedGlobalAttribute() = default;

 private:
  std::shared_ptr<types::IGlobalAttribute> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedLocalAttribute
    : virtual public types::ILocalAttribute,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kLocalAttribute;
  }

  const std::wstring& name() const override {
    return instance_->name();
  }

  std::wstring& mutable_name() override {
    return instance_->mutable_name();
  }

  void set_name(
    std::wstring value
  ) override {
    instance_->set_name(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedLocalAttribute(
    std::shared_ptr<types::ILocalAttribute> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedLocalAttribute() = default;

 private:
  std::shared_ptr<types::ILocalAttribute> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedAttributeOperand
    : virtual public types::IAttributeOperand,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kAttributeOperand;
  }

  const types::AttributeItem& attribute() const override {
    return instance_->attribute();
  }

  types::AttributeItem& mutable_attribute() override {
    return instance_->mutable_attribute();
  }

  void set_attribute(
    types::AttributeItem value
  ) override {
    instance_->set_attribute(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedAttributeOperand(
    std::shared_ptr<types::IAttributeOperand> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedAttributeOperand() = default;

 private:
  std::shared_ptr<types::IAttributeOperand> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedStringLiteral
    : virtual public types::IStringLiteral,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kStringLiteral;
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedStringLiteral(
    std::shared_ptr<types::IStringLiteral> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedStringLiteral() = default;

 private:
  std::shared_ptr<types::IStringLiteral> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedNumberLiteral
    : virtual public types::INumberLiteral,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kNumberLiteral;
  }

  double number() const override {
    return instance_->number();
  }

  double& mutable_number() override {
    return instance_->mutable_number();
  }

  void set_number(
    double value
  ) override {
    instance_->set_number(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedNumberLiteral(
    std::shared_ptr<types::INumberLiteral> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedNumberLiteral() = default;

 private:
  std::shared_ptr<types::INumberLiteral> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSomething
    : virtual public types::ISomething,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomething;
  }

  const std::shared_ptr<types::IElement>& root() const override {
    return instance_->root();
  }

  std::shared_ptr<types::IElement>& mutable_root() override {
    return instance_->mutable_root();
  }

  void set_root(
    std::shared_ptr<types::IElement> value
  ) override {
    instance_->set_root(value);
  }

  const common::optional<
    std::shared_ptr<types::IElement>
  >& optional_element() const override {
    return instance_->optional_element();
  }

  common::optional<
    std::shared_ptr<types::IElement>
  >& mutable_optional_element() override {
    return instance_->mutable_optional_element();
  }

  void set_optional_element(
    common::optional<
      std::shared_ptr<types::IElement>
    > value
  ) override {
    instance_->set_optional_element(value);
  }

  const types::Value& value() const override {
    return instance_->value();
  }

  types::Value& mutable_value() override {
    return instance_->mutable_value();
  }

  void set_value(
    types::Value value
  ) override {
    instance_->set_value(value);
  }

  const std::vector<types::Value>& values() const override {
    return instance_->values();
  }

  std::vector<types::Value>& mutable_values() override {
    return instance_->mutable_values();
  }

  void set_values(
    std::vector<types::Value> value
  ) override {
    instance_->set_values(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomething(
    std::shared_ptr<types::ISomething> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomething() = default;

 private:
  std::shared_ptr<types::ISomething> instance_;
  std::shared_ptr<E> enhancement_;
};

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ILeaf> WrapLeaf(
  const std::shared_ptr<types::ILeaf>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ILeaf>(
      new EnhancedLeaf<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IOrderedContainer> WrapOrderedContainer(
  const std::shared_ptr<types::IOrderedContainer>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  {
    const std::vector<
      std::shared_ptr<types::IElement>
    >& value(
      that->children()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IElement>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IElement>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_children(
      std::move(wrapped)
    );
  }

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IOrderedContainer>(
      new EnhancedOrderedContainer<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IUnorderedContainer> WrapUnorderedContainer(
  const std::shared_ptr<types::IUnorderedContainer>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  {
    const std::vector<
      std::shared_ptr<types::IElement>
    >& value(
      that->children()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IElement>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IElement>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_children(
      std::move(wrapped)
    );
  }

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IUnorderedContainer>(
      new EnhancedUnorderedContainer<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IGlobalAttribute> WrapGlobalAttribute(
  const std::shared_ptr<types::IGlobalAttribute>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IGlobalAttribute>(
      new EnhancedGlobalAttribute<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ILocalAttribute> WrapLocalAttribute(
  const std::shared_ptr<types::ILocalAttribute>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ILocalAttribute>(
      new EnhancedLocalAttribute<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IAttributeOperand> WrapAttributeOperand(
  const std::shared_ptr<types::IAttributeOperand>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  that->set_attribute(
    Wrap<E>(
      that->attribute(),
      factory
    )
  );

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IAttributeOperand>(
      new EnhancedAttributeOperand<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IStringLiteral> WrapStringLiteral(
  const std::shared_ptr<types::IStringLiteral>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IStringLiteral>(
      new EnhancedStringLiteral<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::INumberLiteral> WrapNumberLiteral(
  const std::shared_ptr<types::INumberLiteral>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::INumberLiteral>(
      new EnhancedNumberLiteral<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomething> WrapSomething(
  const std::shared_ptr<types::ISomething>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  that->set_root(
    Wrap<E>(
      that->root(),
      factory
    )
  );

  if (that->optional_element().has_value()) {
    const std::shared_ptr<types::IElement>& value(
      that->optional_element().value()
    );

    std::shared_ptr<
      types::IElement
    > wrapped(
      Wrap<E>(
        value,
        factory
      )
    );

    that->set_optional_element(
      common::make_optional(
        std::move(wrapped)
      )
    );
  }

  that->set_value(
    Wrap<E>(
      that->value(),
      factory
    )
  );

  {
    const std::vector<types::Value>& value(
      that->values()
    );
    const std::size_t size = value.size();

    std::vector<types::Value> wrapped;
    wrapped.reserve(size);

    for (
      const types::Value& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_values(
      std::move(wrapped)
    );
  }

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomething>(
      new EnhancedSomething<E>(
        that,
        enh
      )
    );
}

/**
 * Assert that the \p that instance has not been already enhanced.
 *
 * \param that instance to be checked
 * \tparam E type of the enhancement
 * \tparam T interface type of \p that instance
 * \throw std::logic_error if \p that already enhanced
 */
template<
  typename E,
  typename T,
  typename std::enable_if<
    std::is_base_of<types::IClass, T>::value
  >::type* = nullptr
>
void AssertNotEnhanced(
  const std::shared_ptr<T>& that
) {
  std::shared_ptr<impl::IEnhanced<E> > enhanced(
    std::dynamic_pointer_cast<
      impl::IEnhanced<E>
    >(that)
  );
  if (enhanced != nullptr) {
    throw std::logic_error(
      common::Concat(
        "An instance of ",
        stringification::to_string(that->model_type()),
        " has been already wrapped."
      )
    );
  }
}

}  // namespace impl
/// \endcond

/**
 * Wrap \p that instance recursively with the enhancement produced by the \p factory.
 *
 * The factory decides itself whether it will produce an enhancement for
 * \p that instance, or not. Even if no enhancement has been produced for \p that
 * instance, we will still continue to enhance the instances referenced
 * by \p that instance recursively.
 *
 * \param that instance to wrap
 * \param factory to selectively produce an enhancement
 * \return enhanced \p that instance
 * \throw std::logic_error if \p that instance has been already wrapped.
 * \tparam E type of the enhancement
 */
///@{

template <typename E>
std::shared_ptr<
  types::IClass
> Wrap(
  const std::shared_ptr<
    types::IClass
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IClass
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that),
        factory
      );
      break;
    case types::ModelType::kOrderedContainer:
      return impl::WrapOrderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IOrderedContainer
        >(that),
        factory
      );
      break;
    case types::ModelType::kUnorderedContainer:
      return impl::WrapUnorderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IUnorderedContainer
        >(that),
        factory
      );
      break;
    case types::ModelType::kGlobalAttribute:
      return impl::WrapGlobalAttribute<E>(
        std::dynamic_pointer_cast<
          types::IGlobalAttribute
        >(that),
        factory
      );
      break;
    case types::ModelType::kLocalAttribute:
      return impl::WrapLocalAttribute<E>(
        std::dynamic_pointer_cast<
          types::ILocalAttribute
        >(that),
        factory
      );
      break;
    case types::ModelType::kAttributeOperand:
      return impl::WrapAttributeOperand<E>(
        std::dynamic_pointer_cast<
          types::IAttributeOperand
        >(that),
        factory
      );
      break;
    case types::ModelType::kStringLiteral:
      return impl::WrapStringLiteral<E>(
        std::dynamic_pointer_cast<
          types::IStringLiteral
        >(that),
        factory
      );
      break;
    case types::ModelType::kNumberLiteral:
      return impl::WrapNumberLiteral<E>(
        std::dynamic_pointer_cast<
          types::INumberLiteral
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IElement
> Wrap(
  const std::shared_ptr<
    types::IElement
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IElement
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that),
        factory
      );
      break;
    case types::ModelType::kOrderedContainer:
      return impl::WrapOrderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IOrderedContainer
        >(that),
        factory
      );
      break;
    case types::ModelType::kUnorderedContainer:
      return impl::WrapUnorderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IUnorderedContainer
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ILeaf
> Wrap(
  const std::shared_ptr<
    types::ILeaf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ILeaf
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IContainer
> Wrap(
  const std::shared_ptr<
    types::IContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IContainer
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kOrderedContainer:
      return impl::WrapOrderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IOrderedContainer
        >(that),
        factory
      );
      break;
    case types::ModelType::kUnorderedContainer:
      return impl::WrapUnorderedContainer<E>(
        std::dynamic_pointer_cast<
          types::IUnorderedContainer
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IOrderedContainer
> Wrap(
  const std::shared_ptr<
    types::IOrderedContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IOrderedContainer
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kOrderedContainer:
      return impl::WrapOrderedContainer<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IUnorderedContainer
> Wrap(
  const std::shared_ptr<
    types::IUnorderedContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IUnorderedContainer
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kUnorderedContainer:
      return impl::WrapUnorderedContainer<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IGlobalAttribute
> Wrap(
  const std::shared_ptr<
    types::IGlobalAttribute
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IGlobalAttribute
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kGlobalAttribute:
      return impl::WrapGlobalAttribute<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ILocalAttribute
> Wrap(
  const std::shared_ptr<
    types::ILocalAttribute
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ILocalAttribute
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kLocalAttribute:
      return impl::WrapLocalAttribute<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IAttributeOperand
> Wrap(
  const std::shared_ptr<
    types::IAttributeOperand
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IAttributeOperand
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAttributeOperand:
      return impl::WrapAttributeOperand<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IStringLiteral
> Wrap(
  const std::shared_ptr<
    types::IStringLiteral
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IStringLiteral
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kStringLiteral:
      return impl::WrapStringLiteral<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::INumberLiteral
> Wrap(
  const std::shared_ptr<
    types::INumberLiteral
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::INumberLiteral
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kNumberLiteral:
      return impl::WrapNumberLiteral<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomething
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
types::AttributeItem Wrap(
  const types::AttributeItem& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  switch (that.index()) {
    case 0:
      return types::AttributeItem(
        common::in_place_index_t<0>(),
        Wrap<E>(
          common::get<0>(that),
          factory
        )
      );
    case 1:
      return types::AttributeItem(
        common::in_place_index_t<1>(),
        Wrap<E>(
          common::get<1>(that),
          factory
        )
      );
    default:
      throw std::logic_error("Invalid variant index");
  }
}

template <typename E>
types::StringValue Wrap(
  const types::StringValue& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  switch (that.index()) {
    case 0:
      return types::StringValue(
        common::in_place_index_t<0>(),
        Wrap<E>(
          common::get<0>(that),
          factory
        )
      );
    case 1:
      return types::StringValue(
        common::in_place_index_t<1>(),
        Wrap<E>(
          common::get<1>(that),
          factory
        )
      );
    default:
      throw std::logic_error("Invalid variant index");
  }
}

template <typename E>
types::Value Wrap(
  const types::Value& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  switch (that.index()) {
    case 0:
      return types::Value(
        common::in_place_index_t<0>(),
        Wrap<E>(
          common::get<0>(that),
          factory
        )
      );
    case 1:
      return types::Value(
        common::in_place_index_t<1>(),
        Wrap<E>(
          common::get<1>(that),
          factory
        )
      );
    case 2:
      return types::Value(
        common::in_place_index_t<2>(),
        Wrap<E>(
          common::get<2>(that),
          factory
        )
      );
    default:
      throw std::logic_error("Invalid variant index");
  }
}

///@}}

/**
 * Try to unwrap the enhancement from \p that instance.
 *
 * \param that instance possibly wrapped with an enhancement
 * \return the enhancement, or `nullptr` if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> Unwrap(
  const std::shared_ptr<types::IClass>& that
) {
  const std::shared_ptr<impl::IEnhanced<E> >& maybe_enhanced(
    std::dynamic_pointer_cast<impl::IEnhanced<E> >(that)
  );

  if (!maybe_enhanced) {
    return nullptr;
  }

  return maybe_enhanced->enhancement();
}

/**
 * Unwrap the enhancement from \p that instance.
 *
 * \remark \p that instance must have been wrapped before.
 *
 * \param that instance expected to be wrapped with an enhancement
 * \return the enhancement
 * \throw std::invalid_argument if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> MustUnwrap(
  const std::shared_ptr<types::IClass>& that
) {
  std::shared_ptr<E> enhancement(
    Unwrap<E>(that)
  );
  if (!enhancement) {
    throw std::invalid_argument(
      common::Concat(
        "Expected an instance of ",
        stringification::to_string(that->model_type()),
        " to have been already wrapped with an enhancement, "
        "but it has been not."
      )
    );
  }
  return enhancement;
}

}  // namespace enhancing
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_ENHANCING_GUARD_
