#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kItem = 0,
  kSomething = 1
};

enum class Kind : std::uint32_t {
  kAlpha = 0,
  kBeta = 1,
};  // enum class Kind

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IItem;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class IItem
    : virtual public IClass {
 public:
  virtual const std::vector<std::wstring>& texts() const = 0;

  virtual std::vector<std::wstring>& mutable_texts() = 0;

  virtual void set_texts(
    std::vector<std::wstring> value
  ) = 0;

  virtual ~IItem() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual Kind kind() const = 0;

  virtual Kind& mutable_kind() = 0;

  virtual void set_kind(
    Kind value
  ) = 0;

  virtual const std::vector<int64_t>& numbers() const = 0;

  virtual std::vector<int64_t>& mutable_numbers() = 0;

  virtual void set_numbers(
    std::vector<int64_t> value
  ) = 0;

  virtual const std::vector<std::wstring>& texts() const = 0;

  virtual std::vector<std::wstring>& mutable_texts() = 0;

  virtual void set_texts(
    std::vector<std::wstring> value
  ) = 0;

  virtual const std::vector<
    std::shared_ptr<IItem>
  >& items() const = 0;

  virtual std::vector<
    std::shared_ptr<IItem>
  >& mutable_items() = 0;

  virtual void set_items(
    std::vector<
      std::shared_ptr<IItem>
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Item
    : public IItem {
 public:
  explicit Item(
    std::vector<std::wstring> texts
  );

  ModelType model_type() const override;

  // region Get and set texts_

  const std::vector<std::wstring>& texts() const override;

  std::vector<std::wstring>& mutable_texts() override;

  void set_texts(
    std::vector<std::wstring> value
  ) override;

  // endregion

  ~Item() override = default;

 private:
  std::vector<std::wstring> texts_;
};

class Something
    : public ISomething {
 public:
  Something(
    Kind kind,
    std::vector<int64_t> numbers,
    std::vector<std::wstring> texts,
    std::vector<
      std::shared_ptr<IItem>
    > items
  );

  ModelType model_type() const override;

  // region Get and set kind_

  Kind kind() const override;

  Kind& mutable_kind() override;

  void set_kind(
    Kind value
  ) override;

  // endregion

  // region Get and set numbers_

  const std::vector<int64_t>& numbers() const override;

  std::vector<int64_t>& mutable_numbers() override;

  void set_numbers(
    std::vector<int64_t> value
  ) override;

  // endregion

  // region Get and set texts_

  const std::vector<std::wstring>& texts() const override;

  std::vector<std::wstring>& mutable_texts() override;

  void set_texts(
    std::vector<std::wstring> value
  ) override;

  // endregion

  // region Get and set items_

  const std::vector<
    std::shared_ptr<IItem>
  >& items() const override;

  std::vector<
    std::shared_ptr<IItem>
  >& mutable_items() override;

  void set_items(
    std::vector<
      std::shared_ptr<IItem>
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  Kind kind_;

  std::vector<int64_t> numbers_;

  std::vector<std::wstring> texts_;

  std::vector<
    std::shared_ptr<IItem>
  > items_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IItem
 */
bool IsItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
