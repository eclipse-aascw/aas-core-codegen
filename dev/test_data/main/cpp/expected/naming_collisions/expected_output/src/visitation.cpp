// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/types.hpp"
#include "dummy/stringification.hpp"
#include "dummy/visitation.hpp"

#pragma warning(push, 0)
#include <sstream>
#pragma warning(pop)

namespace dummy {
namespace visitation {

// region AbstractVisitor

void AbstractVisitor::Visit(
  const std::shared_ptr<types::IClass>& that
) {
  // NOTE (mristin):
  // We have to dynamically cast the pointers due to the virtual multiple
  // inheritance, and also because we used shared pointers for references.
  // If we used constant references instead of shared pointers, we could use
  // a pattern such as double dispatch. However, this has the limitation that
  // it would prevent us from collecting the instances in the visitor, such that
  // they outlive the original structures.

  switch (that->model_type()) {
    case types::ModelType::kInstance:
      VisitInstance(
        std::dynamic_pointer_cast<
          types::IInstance
        >(that)
      );
      break;
    case types::ModelType::kPointer:
      VisitPointer(
        std::dynamic_pointer_cast<
          types::IPointer
        >(that)
      );
      break;
    case types::ModelType::kJsonObjectOfName:
      VisitJsonObjectOfName(
        std::dynamic_pointer_cast<
          types::IJsonObjectOfName
        >(that)
      );
      break;
    case types::ModelType::kListOf:
      VisitListOf(
        std::dynamic_pointer_cast<
          types::IListOf
        >(that)
      );
      break;
    case types::ModelType::kSomething:
      VisitSomething(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that)
      );
      break;
    default:
      throw std::logic_error(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(that->model_type())
          )
        )
      );
  }
}

// endregion

// region PassThroughVisitor

void PassThroughVisitor::VisitInstance(
  const std::shared_ptr<types::IInstance>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitPointer(
  const std::shared_ptr<types::IPointer>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitJsonObjectOfName(
  const std::shared_ptr<types::IJsonObjectOfName>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitListOf(
  const std::shared_ptr<types::IListOf>&
) {
  // No properties to be passed through.
}

void PassThroughVisitor::VisitSomething(
  const std::shared_ptr<types::ISomething>& that
) {
  // mutable_instances
  for (
    const std::shared_ptr<types::IInstance>& item :
    that->mutable_instances()
  ) {
    Visit(item);
  }

  // mutable_pointer
  Visit(
    that->mutable_pointer()
  );

  // mutable_json_object_of_name
  Visit(
    that->mutable_json_object_of_name()
  );

  // mutable_lists
  for (
    const std::shared_ptr<types::IListOf>& item :
    that->mutable_lists()
  ) {
    Visit(item);
  }

  // mutable_pair
  Visit(
    std::get<0>(
      that->mutable_pair()
    )
  );
}

// endregion

}  // namespace visitation
}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
