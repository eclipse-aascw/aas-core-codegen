#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <nlohmann/json.hpp>

#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kInstance = 0,
  kPointer = 1,
  kJsonObjectOfName = 2,
  kListOf = 3,
  kSomething = 4
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IInstance;

class IPointer;

class IJsonObjectOfName;

class IListOf;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

/// \brief Test that the function over this class does not clash with the helpers.
class IInstance
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IInstance() = default;
};

/// \brief Test that the function over this class does not clash with the helpers.
class IPointer
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IPointer() = default;
};

/// \brief Test that the function over this class does not clash with the items.
class IJsonObjectOfName
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IJsonObjectOfName() = default;
};

/// \brief Test that the leaf of this class does not clash with the head of a list.
class IListOf
    : virtual public IClass {
 public:
  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~IListOf() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::vector<
    std::shared_ptr<IInstance>
  >& instances() const = 0;

  virtual std::vector<
    std::shared_ptr<IInstance>
  >& mutable_instances() = 0;

  virtual void set_instances(
    std::vector<
      std::shared_ptr<IInstance>
    > value
  ) = 0;

  virtual const std::shared_ptr<IPointer>& pointer() const = 0;

  virtual std::shared_ptr<IPointer>& mutable_pointer() = 0;

  virtual void set_pointer(
    std::shared_ptr<IPointer> value
  ) = 0;

  virtual const std::shared_ptr<IJsonObjectOfName>& json_object_of_name() const = 0;

  virtual std::shared_ptr<IJsonObjectOfName>& mutable_json_object_of_name() = 0;

  virtual void set_json_object_of_name(
    std::shared_ptr<IJsonObjectOfName> value
  ) = 0;

  virtual const std::vector<nlohmann::json>& mappings() const = 0;

  virtual std::vector<nlohmann::json>& mutable_mappings() = 0;

  virtual void set_mappings(
    std::vector<nlohmann::json> value
  ) = 0;

  virtual const std::vector<
    std::shared_ptr<IListOf>
  >& lists() const = 0;

  virtual std::vector<
    std::shared_ptr<IListOf>
  >& mutable_lists() = 0;

  virtual void set_lists(
    std::vector<
      std::shared_ptr<IListOf>
    > value
  ) = 0;

  virtual const std::tuple<
    std::shared_ptr<IListOf>,
    std::wstring
  >& pair() const = 0;

  virtual std::tuple<
    std::shared_ptr<IListOf>,
    std::wstring
  >& mutable_pair() = 0;

  virtual void set_pair(
    std::tuple<
      std::shared_ptr<IListOf>,
      std::wstring
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Instance
    : public IInstance {
 public:
  explicit Instance(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~Instance() override = default;

 private:
  std::wstring text_;
};

class Pointer
    : public IPointer {
 public:
  explicit Pointer(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~Pointer() override = default;

 private:
  std::wstring text_;
};

class JsonObjectOfName
    : public IJsonObjectOfName {
 public:
  explicit JsonObjectOfName(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~JsonObjectOfName() override = default;

 private:
  std::wstring text_;
};

class ListOf
    : public IListOf {
 public:
  explicit ListOf(
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~ListOf() override = default;

 private:
  std::wstring text_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::vector<
      std::shared_ptr<IInstance>
    > instances,
    std::shared_ptr<IPointer> pointer,
    std::shared_ptr<IJsonObjectOfName> json_object_of_name,
    std::vector<nlohmann::json> mappings,
    std::vector<
      std::shared_ptr<IListOf>
    > lists,
    std::tuple<
      std::shared_ptr<IListOf>,
      std::wstring
    > pair
  );

  ModelType model_type() const override;

  // region Get and set instances_

  const std::vector<
    std::shared_ptr<IInstance>
  >& instances() const override;

  std::vector<
    std::shared_ptr<IInstance>
  >& mutable_instances() override;

  void set_instances(
    std::vector<
      std::shared_ptr<IInstance>
    > value
  ) override;

  // endregion

  // region Get and set pointer_

  const std::shared_ptr<IPointer>& pointer() const override;

  std::shared_ptr<IPointer>& mutable_pointer() override;

  void set_pointer(
    std::shared_ptr<IPointer> value
  ) override;

  // endregion

  // region Get and set json_object_of_name_

  const std::shared_ptr<IJsonObjectOfName>& json_object_of_name() const override;

  std::shared_ptr<IJsonObjectOfName>& mutable_json_object_of_name() override;

  void set_json_object_of_name(
    std::shared_ptr<IJsonObjectOfName> value
  ) override;

  // endregion

  // region Get and set mappings_

  const std::vector<nlohmann::json>& mappings() const override;

  std::vector<nlohmann::json>& mutable_mappings() override;

  void set_mappings(
    std::vector<nlohmann::json> value
  ) override;

  // endregion

  // region Get and set lists_

  const std::vector<
    std::shared_ptr<IListOf>
  >& lists() const override;

  std::vector<
    std::shared_ptr<IListOf>
  >& mutable_lists() override;

  void set_lists(
    std::vector<
      std::shared_ptr<IListOf>
    > value
  ) override;

  // endregion

  // region Get and set pair_

  const std::tuple<
    std::shared_ptr<IListOf>,
    std::wstring
  >& pair() const override;

  std::tuple<
    std::shared_ptr<IListOf>,
    std::wstring
  >& mutable_pair() override;

  void set_pair(
    std::tuple<
      std::shared_ptr<IListOf>,
      std::wstring
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::vector<
    std::shared_ptr<IInstance>
  > instances_;

  std::shared_ptr<IPointer> pointer_;

  std::shared_ptr<IJsonObjectOfName> json_object_of_name_;

  std::vector<nlohmann::json> mappings_;

  std::vector<
    std::shared_ptr<IListOf>
  > lists_;

  std::tuple<
    std::shared_ptr<IListOf>,
    std::wstring
  > pair_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IInstance.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IInstance
 */
bool IsInstance(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IPointer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IPointer
 */
bool IsPointer(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IJsonObjectOfName.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IJsonObjectOfName
 */
bool IsJsonObjectOfName(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IListOf.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IListOf
 */
bool IsListOf(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
