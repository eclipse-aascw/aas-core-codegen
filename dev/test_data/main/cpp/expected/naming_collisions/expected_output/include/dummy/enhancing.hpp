#ifndef DUMMY_ENHANCING_GUARD_
#define DUMMY_ENHANCING_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"
#include "dummy/stringification.hpp"
#include "dummy/types.hpp"

#pragma warning(push, 0)
#include <sstream>
#include <stdexcept>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup enhancing Enhance instances of the model with your custom enhancements.
 * @{
 */
namespace enhancing {

// region Forward declarations

template <typename E>
std::shared_ptr<
  types::IInstance
> Wrap(
  const std::shared_ptr<
    types::IInstance
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IPointer
> Wrap(
  const std::shared_ptr<
    types::IPointer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IJsonObjectOfName
> Wrap(
  const std::shared_ptr<
    types::IJsonObjectOfName
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IListOf
> Wrap(
  const std::shared_ptr<
    types::IListOf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

// endregion Forward declarations

/// \cond HIDDEN
namespace impl {

template<typename E>
class IEnhanced {
 public:
  virtual const std::shared_ptr<E>& enhancement() const = 0;

  virtual std::shared_ptr<E>& mutable_enhancement() = 0;

  virtual void set_enhancement(
    std::shared_ptr<E> value
  ) = 0;

  virtual ~IEnhanced() = default;
};

template<class E>
class EnhancedInstance
    : virtual public types::IInstance,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kInstance;
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedInstance(
    std::shared_ptr<types::IInstance> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedInstance() = default;

 private:
  std::shared_ptr<types::IInstance> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedPointer
    : virtual public types::IPointer,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kPointer;
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedPointer(
    std::shared_ptr<types::IPointer> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedPointer() = default;

 private:
  std::shared_ptr<types::IPointer> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedJsonObjectOfName
    : virtual public types::IJsonObjectOfName,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kJsonObjectOfName;
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedJsonObjectOfName(
    std::shared_ptr<types::IJsonObjectOfName> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedJsonObjectOfName() = default;

 private:
  std::shared_ptr<types::IJsonObjectOfName> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedListOf
    : virtual public types::IListOf,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kListOf;
  }

  const std::wstring& text() const override {
    return instance_->text();
  }

  std::wstring& mutable_text() override {
    return instance_->mutable_text();
  }

  void set_text(
    std::wstring value
  ) override {
    instance_->set_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedListOf(
    std::shared_ptr<types::IListOf> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedListOf() = default;

 private:
  std::shared_ptr<types::IListOf> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSomething
    : virtual public types::ISomething,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomething;
  }

  const std::vector<
    std::shared_ptr<types::IInstance>
  >& instances() const override {
    return instance_->instances();
  }

  std::vector<
    std::shared_ptr<types::IInstance>
  >& mutable_instances() override {
    return instance_->mutable_instances();
  }

  void set_instances(
    std::vector<
      std::shared_ptr<types::IInstance>
    > value
  ) override {
    instance_->set_instances(value);
  }

  const std::shared_ptr<types::IPointer>& pointer() const override {
    return instance_->pointer();
  }

  std::shared_ptr<types::IPointer>& mutable_pointer() override {
    return instance_->mutable_pointer();
  }

  void set_pointer(
    std::shared_ptr<types::IPointer> value
  ) override {
    instance_->set_pointer(value);
  }

  const std::shared_ptr<types::IJsonObjectOfName>& json_object_of_name() const override {
    return instance_->json_object_of_name();
  }

  std::shared_ptr<types::IJsonObjectOfName>& mutable_json_object_of_name() override {
    return instance_->mutable_json_object_of_name();
  }

  void set_json_object_of_name(
    std::shared_ptr<types::IJsonObjectOfName> value
  ) override {
    instance_->set_json_object_of_name(value);
  }

  const std::vector<nlohmann::json>& mappings() const override {
    return instance_->mappings();
  }

  std::vector<nlohmann::json>& mutable_mappings() override {
    return instance_->mutable_mappings();
  }

  void set_mappings(
    std::vector<nlohmann::json> value
  ) override {
    instance_->set_mappings(value);
  }

  const std::vector<
    std::shared_ptr<types::IListOf>
  >& lists() const override {
    return instance_->lists();
  }

  std::vector<
    std::shared_ptr<types::IListOf>
  >& mutable_lists() override {
    return instance_->mutable_lists();
  }

  void set_lists(
    std::vector<
      std::shared_ptr<types::IListOf>
    > value
  ) override {
    instance_->set_lists(value);
  }

  const std::tuple<
    std::shared_ptr<types::IListOf>,
    std::wstring
  >& pair() const override {
    return instance_->pair();
  }

  std::tuple<
    std::shared_ptr<types::IListOf>,
    std::wstring
  >& mutable_pair() override {
    return instance_->mutable_pair();
  }

  void set_pair(
    std::tuple<
      std::shared_ptr<types::IListOf>,
      std::wstring
    > value
  ) override {
    instance_->set_pair(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomething(
    std::shared_ptr<types::ISomething> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomething() = default;

 private:
  std::shared_ptr<types::ISomething> instance_;
  std::shared_ptr<E> enhancement_;
};

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IInstance> WrapInstance(
  const std::shared_ptr<types::IInstance>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IInstance>(
      new EnhancedInstance<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IPointer> WrapPointer(
  const std::shared_ptr<types::IPointer>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IPointer>(
      new EnhancedPointer<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IJsonObjectOfName> WrapJsonObjectOfName(
  const std::shared_ptr<types::IJsonObjectOfName>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IJsonObjectOfName>(
      new EnhancedJsonObjectOfName<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IListOf> WrapListOf(
  const std::shared_ptr<types::IListOf>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IListOf>(
      new EnhancedListOf<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomething> WrapSomething(
  const std::shared_ptr<types::ISomething>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  {
    const std::vector<
      std::shared_ptr<types::IInstance>
    >& value(
      that->instances()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IInstance>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IInstance>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_instances(
      std::move(wrapped)
    );
  }

  that->set_pointer(
    Wrap<E>(
      that->pointer(),
      factory
    )
  );

  that->set_json_object_of_name(
    Wrap<E>(
      that->json_object_of_name(),
      factory
    )
  );

  {
    const std::vector<
      std::shared_ptr<types::IListOf>
    >& value(
      that->lists()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IListOf>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IListOf>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_lists(
      std::move(wrapped)
    );
  }

  {
    const std::tuple<
      std::shared_ptr<types::IListOf>,
      std::wstring
    >& value(
      that->pair()
    );

    std::tuple<
      std::shared_ptr<types::IListOf>,
      std::wstring
    > wrapped(value);

    std::get<0>(wrapped) = Wrap<E>(
      std::get<0>(value),
      factory
    );

    that->set_pair(
      std::move(wrapped)
    );
  }

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomething>(
      new EnhancedSomething<E>(
        that,
        enh
      )
    );
}

/**
 * Assert that the \p that instance has not been already enhanced.
 *
 * \param that instance to be checked
 * \tparam E type of the enhancement
 * \tparam T interface type of \p that instance
 * \throw std::logic_error if \p that already enhanced
 */
template<
  typename E,
  typename T,
  typename std::enable_if<
    std::is_base_of<types::IClass, T>::value
  >::type* = nullptr
>
void AssertNotEnhanced(
  const std::shared_ptr<T>& that
) {
  std::shared_ptr<impl::IEnhanced<E> > enhanced(
    std::dynamic_pointer_cast<
      impl::IEnhanced<E>
    >(that)
  );
  if (enhanced != nullptr) {
    throw std::logic_error(
      common::Concat(
        "An instance of ",
        stringification::to_string(that->model_type()),
        " has been already wrapped."
      )
    );
  }
}

}  // namespace impl
/// \endcond

/**
 * Wrap \p that instance recursively with the enhancement produced by the \p factory.
 *
 * The factory decides itself whether it will produce an enhancement for
 * \p that instance, or not. Even if no enhancement has been produced for \p that
 * instance, we will still continue to enhance the instances referenced
 * by \p that instance recursively.
 *
 * \param that instance to wrap
 * \param factory to selectively produce an enhancement
 * \return enhanced \p that instance
 * \throw std::logic_error if \p that instance has been already wrapped.
 * \tparam E type of the enhancement
 */
///@{

template <typename E>
std::shared_ptr<
  types::IClass
> Wrap(
  const std::shared_ptr<
    types::IClass
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IClass
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kInstance:
      return impl::WrapInstance<E>(
        std::dynamic_pointer_cast<
          types::IInstance
        >(that),
        factory
      );
      break;
    case types::ModelType::kPointer:
      return impl::WrapPointer<E>(
        std::dynamic_pointer_cast<
          types::IPointer
        >(that),
        factory
      );
      break;
    case types::ModelType::kJsonObjectOfName:
      return impl::WrapJsonObjectOfName<E>(
        std::dynamic_pointer_cast<
          types::IJsonObjectOfName
        >(that),
        factory
      );
      break;
    case types::ModelType::kListOf:
      return impl::WrapListOf<E>(
        std::dynamic_pointer_cast<
          types::IListOf
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IInstance
> Wrap(
  const std::shared_ptr<
    types::IInstance
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IInstance
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kInstance:
      return impl::WrapInstance<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IPointer
> Wrap(
  const std::shared_ptr<
    types::IPointer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IPointer
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kPointer:
      return impl::WrapPointer<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IJsonObjectOfName
> Wrap(
  const std::shared_ptr<
    types::IJsonObjectOfName
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IJsonObjectOfName
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kJsonObjectOfName:
      return impl::WrapJsonObjectOfName<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IListOf
> Wrap(
  const std::shared_ptr<
    types::IListOf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IListOf
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kListOf:
      return impl::WrapListOf<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomething
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

///@}}

/**
 * Try to unwrap the enhancement from \p that instance.
 *
 * \param that instance possibly wrapped with an enhancement
 * \return the enhancement, or `nullptr` if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> Unwrap(
  const std::shared_ptr<types::IClass>& that
) {
  const std::shared_ptr<impl::IEnhanced<E> >& maybe_enhanced(
    std::dynamic_pointer_cast<impl::IEnhanced<E> >(that)
  );

  if (!maybe_enhanced) {
    return nullptr;
  }

  return maybe_enhanced->enhancement();
}

/**
 * Unwrap the enhancement from \p that instance.
 *
 * \remark \p that instance must have been wrapped before.
 *
 * \param that instance expected to be wrapped with an enhancement
 * \return the enhancement
 * \throw std::invalid_argument if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> MustUnwrap(
  const std::shared_ptr<types::IClass>& that
) {
  std::shared_ptr<E> enhancement(
    Unwrap<E>(that)
  );
  if (!enhancement) {
    throw std::invalid_argument(
      common::Concat(
        "Expected an instance of ",
        stringification::to_string(that->model_type()),
        " to have been already wrapped with an enhancement, "
        "but it has been not."
      )
    );
  }
  return enhancement;
}

}  // namespace enhancing
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_ENHANCING_GUARD_
