#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kLangString = 0,
  kLangStringSet = 1,
  kIecContent = 2,
  kOtherContent = 3,
  kSpecification = 4,
  kSomething = 5
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class ILangString;

class ILangStringSet;

class IContent;

class IIecContent;

class IOtherContent;

class ISpecification;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class ILangString
    : virtual public IClass {
 public:
  virtual const std::wstring& language() const = 0;

  virtual std::wstring& mutable_language() = 0;

  virtual void set_language(
    std::wstring value
  ) = 0;

  virtual const std::wstring& text() const = 0;

  virtual std::wstring& mutable_text() = 0;

  virtual void set_text(
    std::wstring value
  ) = 0;

  virtual ~ILangString() = default;
};

class ILangStringSet
    : virtual public IClass {
 public:
  virtual const std::vector<
    std::shared_ptr<ILangString>
  >& lang_strings() const = 0;

  virtual std::vector<
    std::shared_ptr<ILangString>
  >& mutable_lang_strings() = 0;

  virtual void set_lang_strings(
    std::vector<
      std::shared_ptr<ILangString>
    > value
  ) = 0;

  virtual ~ILangStringSet() = default;
};

class IContent
    : virtual public IClass {
 public:
  virtual ~IContent() = default;
};

class IIecContent
    : virtual public IContent {
 public:
  virtual const common::optional<
    std::vector<
      std::shared_ptr<ILangString>
    >
  >& definition() const = 0;

  virtual common::optional<
    std::vector<
      std::shared_ptr<ILangString>
    >
  >& mutable_definition() = 0;

  virtual void set_definition(
    common::optional<
      std::vector<
        std::shared_ptr<ILangString>
      >
    > value
  ) = 0;

  virtual ~IIecContent() = default;
};

class IOtherContent
    : virtual public IContent {
 public:
  virtual ~IOtherContent() = default;
};

class ISpecification
    : virtual public IClass {
 public:
  virtual const std::shared_ptr<IContent>& content() const = 0;

  virtual std::shared_ptr<IContent>& mutable_content() = 0;

  virtual void set_content(
    std::shared_ptr<IContent> value
  ) = 0;

  virtual ~ISpecification() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::wstring& default_language() const = 0;

  virtual std::wstring& mutable_default_language() = 0;

  virtual void set_default_language(
    std::wstring value
  ) = 0;

  virtual const std::vector<
    std::shared_ptr<ILangStringSet>
  >& lang_string_sets() const = 0;

  virtual std::vector<
    std::shared_ptr<ILangStringSet>
  >& mutable_lang_string_sets() = 0;

  virtual void set_lang_string_sets(
    std::vector<
      std::shared_ptr<ILangStringSet>
    > value
  ) = 0;

  virtual const common::optional<
    std::vector<
      std::shared_ptr<ISpecification>
    >
  >& specifications() const = 0;

  virtual common::optional<
    std::vector<
      std::shared_ptr<ISpecification>
    >
  >& mutable_specifications() = 0;

  virtual void set_specifications(
    common::optional<
      std::vector<
        std::shared_ptr<ISpecification>
      >
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class LangString
    : public ILangString {
 public:
  LangString(
    std::wstring language,
    std::wstring text
  );

  ModelType model_type() const override;

  // region Get and set language_

  const std::wstring& language() const override;

  std::wstring& mutable_language() override;

  void set_language(
    std::wstring value
  ) override;

  // endregion

  // region Get and set text_

  const std::wstring& text() const override;

  std::wstring& mutable_text() override;

  void set_text(
    std::wstring value
  ) override;

  // endregion

  ~LangString() override = default;

 private:
  std::wstring language_;

  std::wstring text_;
};

class LangStringSet
    : public ILangStringSet {
 public:
  explicit LangStringSet(
    std::vector<
      std::shared_ptr<ILangString>
    > lang_strings
  );

  ModelType model_type() const override;

  // region Get and set lang_strings_

  const std::vector<
    std::shared_ptr<ILangString>
  >& lang_strings() const override;

  std::vector<
    std::shared_ptr<ILangString>
  >& mutable_lang_strings() override;

  void set_lang_strings(
    std::vector<
      std::shared_ptr<ILangString>
    > value
  ) override;

  // endregion

  ~LangStringSet() override = default;

 private:
  std::vector<
    std::shared_ptr<ILangString>
  > lang_strings_;
};

class IecContent
    : public IIecContent {
 public:
  IecContent(
    common::optional<
      std::vector<
        std::shared_ptr<ILangString>
      >
    > definition = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set definition_

  const common::optional<
    std::vector<
      std::shared_ptr<ILangString>
    >
  >& definition() const override;

  common::optional<
    std::vector<
      std::shared_ptr<ILangString>
    >
  >& mutable_definition() override;

  void set_definition(
    common::optional<
      std::vector<
        std::shared_ptr<ILangString>
      >
    > value
  ) override;

  // endregion

  ~IecContent() override = default;

 private:
  common::optional<
    std::vector<
      std::shared_ptr<ILangString>
    >
  > definition_;
};

class OtherContent
    : public IOtherContent {
 public:
  OtherContent();

  ModelType model_type() const override;

  ~OtherContent() override = default;
};

class Specification
    : public ISpecification {
 public:
  explicit Specification(
    std::shared_ptr<IContent> content
  );

  ModelType model_type() const override;

  // region Get and set content_

  const std::shared_ptr<IContent>& content() const override;

  std::shared_ptr<IContent>& mutable_content() override;

  void set_content(
    std::shared_ptr<IContent> value
  ) override;

  // endregion

  ~Specification() override = default;

 private:
  std::shared_ptr<IContent> content_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::wstring default_language,
    std::vector<
      std::shared_ptr<ILangStringSet>
    > lang_string_sets,
    common::optional<
      std::vector<
        std::shared_ptr<ISpecification>
      >
    > specifications = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set default_language_

  const std::wstring& default_language() const override;

  std::wstring& mutable_default_language() override;

  void set_default_language(
    std::wstring value
  ) override;

  // endregion

  // region Get and set lang_string_sets_

  const std::vector<
    std::shared_ptr<ILangStringSet>
  >& lang_string_sets() const override;

  std::vector<
    std::shared_ptr<ILangStringSet>
  >& mutable_lang_string_sets() override;

  void set_lang_string_sets(
    std::vector<
      std::shared_ptr<ILangStringSet>
    > value
  ) override;

  // endregion

  // region Get and set specifications_

  const common::optional<
    std::vector<
      std::shared_ptr<ISpecification>
    >
  >& specifications() const override;

  common::optional<
    std::vector<
      std::shared_ptr<ISpecification>
    >
  >& mutable_specifications() override;

  void set_specifications(
    common::optional<
      std::vector<
        std::shared_ptr<ISpecification>
      >
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::wstring default_language_;

  std::vector<
    std::shared_ptr<ILangStringSet>
  > lang_string_sets_;

  common::optional<
    std::vector<
      std::shared_ptr<ISpecification>
    >
  > specifications_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ILangString.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ILangString
 */
bool IsLangString(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ILangStringSet.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ILangStringSet
 */
bool IsLangStringSet(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IContent.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IContent
 */
bool IsContent(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IIecContent.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IIecContent
 */
bool IsIecContent(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IOtherContent.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IOtherContent
 */
bool IsOtherContent(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISpecification.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISpecification
 */
bool IsSpecification(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
