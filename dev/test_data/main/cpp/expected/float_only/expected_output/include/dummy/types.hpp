#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kSomething = 0
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

/// \brief Represent a class whose only refusable content is a floating-point number.
///
/// JSON knows neither an infinity nor a not-a-number, so a float is one of
/// the few values a serialization may have to refuse. An integer is another,
/// and a target which decides what it has to guard by walking the type of
/// a property can confuse the two -- so we give it a class which reaches
/// a float and no integer at all, in each of the shapes a float can come in.
class ISomething
    : virtual public IClass {
 public:
  virtual double some_float() const = 0;

  virtual double& mutable_some_float() = 0;

  virtual void set_some_float(
    double value
  ) = 0;

  virtual const common::optional<double>& some_optional_float() const = 0;

  virtual common::optional<double>& mutable_some_optional_float() = 0;

  virtual void set_some_optional_float(
    common::optional<double> value
  ) = 0;

  virtual const std::vector<double>& some_floats() const = 0;

  virtual std::vector<double>& mutable_some_floats() = 0;

  virtual void set_some_floats(
    std::vector<double> value
  ) = 0;

  virtual const std::tuple<
    std::wstring,
    double
  >& some_pair() const = 0;

  virtual std::tuple<
    std::wstring,
    double
  >& mutable_some_pair() = 0;

  virtual void set_some_pair(
    std::tuple<
      std::wstring,
      double
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Something
    : public ISomething {
 public:
  Something(
    double some_float,
    std::vector<double> some_floats,
    std::tuple<
      std::wstring,
      double
    > some_pair,
    common::optional<double> some_optional_float = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set some_float_

  double some_float() const override;

  double& mutable_some_float() override;

  void set_some_float(
    double value
  ) override;

  // endregion

  // region Get and set some_optional_float_

  const common::optional<double>& some_optional_float() const override;

  common::optional<double>& mutable_some_optional_float() override;

  void set_some_optional_float(
    common::optional<double> value
  ) override;

  // endregion

  // region Get and set some_floats_

  const std::vector<double>& some_floats() const override;

  std::vector<double>& mutable_some_floats() override;

  void set_some_floats(
    std::vector<double> value
  ) override;

  // endregion

  // region Get and set some_pair_

  const std::tuple<
    std::wstring,
    double
  >& some_pair() const override;

  std::tuple<
    std::wstring,
    double
  >& mutable_some_pair() override;

  void set_some_pair(
    std::tuple<
      std::wstring,
      double
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  double some_float_;

  common::optional<double> some_optional_float_;

  std::vector<double> some_floats_;

  std::tuple<
    std::wstring,
    double
  > some_pair_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
