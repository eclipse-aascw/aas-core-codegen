#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kBox = 0,
  kBag = 1,
  kContainer = 2
};

enum class Color : std::uint32_t {
  kRed = 0,
  kGreen = 1,
};  // enum class Color

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IItem;

class IBox;

class IBag;

class IContainer;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class IItem
    : virtual public IClass {
 public:
  virtual const std::wstring& label() const = 0;

  virtual std::wstring& mutable_label() = 0;

  virtual void set_label(
    std::wstring value
  ) = 0;

  /// \brief Render a human-readable description of the item.
  virtual std::wstring Describe() const = 0;

  virtual ~IItem() = default;
};

class IBox
    : virtual public IItem {
 public:
  virtual const common::optional<Color>& color() const = 0;

  virtual common::optional<Color>& mutable_color() = 0;

  virtual void set_color(
    common::optional<Color> value
  ) = 0;

  /// \brief Return the IBox::color if set, or the default otherwise.
  virtual Color ColorOrDefault() const = 0;

  /// \brief Return the IBox::color, or the \p fallback if not set.
  virtual Color ResolveColor(
    const common::optional<Color>& fallback
  ) const = 0;

  /// \brief Set the IBox::label to \p new_label.
  virtual void Relabel(
    const std::wstring& new_label
  ) = 0;

  virtual ~IBox() = default;
};

class IBag
    : virtual public IItem {
 public:
  virtual const std::vector<std::wstring>& tags() const = 0;

  virtual std::vector<std::wstring>& mutable_tags() = 0;

  virtual void set_tags(
    std::vector<std::wstring> value
  ) = 0;

  virtual ~IBag() = default;
};

class IContainer
    : virtual public IClass {
 public:
  virtual const std::vector<std::wstring>& names() const = 0;

  virtual std::vector<std::wstring>& mutable_names() = 0;

  virtual void set_names(
    std::vector<std::wstring> value
  ) = 0;

  virtual const std::vector<
    std::shared_ptr<IItem>
  >& items() const = 0;

  virtual std::vector<
    std::shared_ptr<IItem>
  >& mutable_items() = 0;

  virtual void set_items(
    std::vector<
      std::shared_ptr<IItem>
    > value
  ) = 0;

  virtual ~IContainer() = default;
};

// endregion

// region Definitions of concrete classes

class Box
    : public IBox {
 public:
  explicit Box(
    std::wstring label,
    common::optional<Color> color = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set label_

  const std::wstring& label() const override;

  std::wstring& mutable_label() override;

  void set_label(
    std::wstring value
  ) override;

  // endregion

  // region Get and set color_

  const common::optional<Color>& color() const override;

  common::optional<Color>& mutable_color() override;

  void set_color(
    common::optional<Color> value
  ) override;

  // endregion

  std::wstring Describe() const override;

  Color ColorOrDefault() const override;

  Color ResolveColor(
    const common::optional<Color>& fallback
  ) const override;

  void Relabel(
    const std::wstring& new_label
  ) override;

  ~Box() override = default;

 private:
  std::wstring label_;

  common::optional<Color> color_;
};

class Bag
    : public IBag {
 public:
  Bag(
    std::wstring label,
    std::vector<std::wstring> tags
  );

  ModelType model_type() const override;

  // region Get and set label_

  const std::wstring& label() const override;

  std::wstring& mutable_label() override;

  void set_label(
    std::wstring value
  ) override;

  // endregion

  // region Get and set tags_

  const std::vector<std::wstring>& tags() const override;

  std::vector<std::wstring>& mutable_tags() override;

  void set_tags(
    std::vector<std::wstring> value
  ) override;

  // endregion

  std::wstring Describe() const override;

  ~Bag() override = default;

 private:
  std::wstring label_;

  std::vector<std::wstring> tags_;
};

class Container
    : public IContainer {
 public:
  Container(
    std::vector<std::wstring> names,
    std::vector<
      std::shared_ptr<IItem>
    > items
  );

  ModelType model_type() const override;

  // region Get and set names_

  const std::vector<std::wstring>& names() const override;

  std::vector<std::wstring>& mutable_names() override;

  void set_names(
    std::vector<std::wstring> value
  ) override;

  // endregion

  // region Get and set items_

  const std::vector<
    std::shared_ptr<IItem>
  >& items() const override;

  std::vector<
    std::shared_ptr<IItem>
  >& mutable_items() override;

  void set_items(
    std::vector<
      std::shared_ptr<IItem>
    > value
  ) override;

  // endregion

  ~Container() override = default;

 private:
  std::vector<std::wstring> names_;

  std::vector<
    std::shared_ptr<IItem>
  > items_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IItem
 */
bool IsItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IBox.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IBox
 */
bool IsBox(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IBag.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IBag
 */
bool IsBag(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IContainer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IContainer
 */
bool IsContainer(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
