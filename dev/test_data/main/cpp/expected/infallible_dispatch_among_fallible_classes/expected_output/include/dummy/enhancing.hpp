#ifndef DUMMY_ENHANCING_GUARD_
#define DUMMY_ENHANCING_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"
#include "dummy/stringification.hpp"
#include "dummy/types.hpp"

#pragma warning(push, 0)
#include <sstream>
#include <stdexcept>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup enhancing Enhance instances of the model with your custom enhancements.
 * @{
 */
namespace enhancing {

// region Forward declarations

template <typename E>
std::shared_ptr<
  types::IAbstractWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IAbstractWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IAbstractDescendantWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IAbstractDescendantWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IParentWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IParentWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IChildWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IChildWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IWithNumber
> Wrap(
  const std::shared_ptr<
    types::IWithNumber
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
types::UnionWithoutNumbers Wrap(
  const types::UnionWithoutNumbers& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
types::UnionWithNumbers Wrap(
  const types::UnionWithNumbers& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

// endregion Forward declarations

/// \cond HIDDEN
namespace impl {

template<typename E>
class IEnhanced {
 public:
  virtual const std::shared_ptr<E>& enhancement() const = 0;

  virtual std::shared_ptr<E>& mutable_enhancement() = 0;

  virtual void set_enhancement(
    std::shared_ptr<E> value
  ) = 0;

  virtual ~IEnhanced() = default;
};

template<class E>
class EnhancedAbstractDescendantWithoutNumbers
    : virtual public types::IAbstractDescendantWithoutNumbers,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kAbstractDescendantWithoutNumbers;
  }

  const std::wstring& some_text() const override {
    return instance_->some_text();
  }

  std::wstring& mutable_some_text() override {
    return instance_->mutable_some_text();
  }

  void set_some_text(
    std::wstring value
  ) override {
    instance_->set_some_text(value);
  }

  const std::wstring& another_text() const override {
    return instance_->another_text();
  }

  std::wstring& mutable_another_text() override {
    return instance_->mutable_another_text();
  }

  void set_another_text(
    std::wstring value
  ) override {
    instance_->set_another_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedAbstractDescendantWithoutNumbers(
    std::shared_ptr<types::IAbstractDescendantWithoutNumbers> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedAbstractDescendantWithoutNumbers() = default;

 private:
  std::shared_ptr<types::IAbstractDescendantWithoutNumbers> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedParentWithoutNumbers
    : virtual public types::IParentWithoutNumbers,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kParentWithoutNumbers;
  }

  const std::wstring& some_text() const override {
    return instance_->some_text();
  }

  std::wstring& mutable_some_text() override {
    return instance_->mutable_some_text();
  }

  void set_some_text(
    std::wstring value
  ) override {
    instance_->set_some_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedParentWithoutNumbers(
    std::shared_ptr<types::IParentWithoutNumbers> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedParentWithoutNumbers() = default;

 private:
  std::shared_ptr<types::IParentWithoutNumbers> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedChildWithoutNumbers
    : virtual public types::IChildWithoutNumbers,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kChildWithoutNumbers;
  }

  const std::wstring& some_text() const override {
    return instance_->some_text();
  }

  std::wstring& mutable_some_text() override {
    return instance_->mutable_some_text();
  }

  void set_some_text(
    std::wstring value
  ) override {
    instance_->set_some_text(value);
  }

  const std::wstring& another_text() const override {
    return instance_->another_text();
  }

  std::wstring& mutable_another_text() override {
    return instance_->mutable_another_text();
  }

  void set_another_text(
    std::wstring value
  ) override {
    instance_->set_another_text(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedChildWithoutNumbers(
    std::shared_ptr<types::IChildWithoutNumbers> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedChildWithoutNumbers() = default;

 private:
  std::shared_ptr<types::IChildWithoutNumbers> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedWithNumber
    : virtual public types::IWithNumber,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kWithNumber;
  }

  int64_t some_int() const override {
    return instance_->some_int();
  }

  int64_t& mutable_some_int() override {
    return instance_->mutable_some_int();
  }

  void set_some_int(
    int64_t value
  ) override {
    instance_->set_some_int(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedWithNumber(
    std::shared_ptr<types::IWithNumber> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedWithNumber() = default;

 private:
  std::shared_ptr<types::IWithNumber> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSomething
    : virtual public types::ISomething,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomething;
  }

  const std::shared_ptr<types::IAbstractWithoutNumbers>& abstract_property() const override {
    return instance_->abstract_property();
  }

  std::shared_ptr<types::IAbstractWithoutNumbers>& mutable_abstract_property() override {
    return instance_->mutable_abstract_property();
  }

  void set_abstract_property(
    std::shared_ptr<types::IAbstractWithoutNumbers> value
  ) override {
    instance_->set_abstract_property(value);
  }

  const std::shared_ptr<types::IParentWithoutNumbers>& parent_property() const override {
    return instance_->parent_property();
  }

  std::shared_ptr<types::IParentWithoutNumbers>& mutable_parent_property() override {
    return instance_->mutable_parent_property();
  }

  void set_parent_property(
    std::shared_ptr<types::IParentWithoutNumbers> value
  ) override {
    instance_->set_parent_property(value);
  }

  const std::vector<
    std::shared_ptr<types::IAbstractWithoutNumbers>
  >& list_abstract_property() const override {
    return instance_->list_abstract_property();
  }

  std::vector<
    std::shared_ptr<types::IAbstractWithoutNumbers>
  >& mutable_list_abstract_property() override {
    return instance_->mutable_list_abstract_property();
  }

  void set_list_abstract_property(
    std::vector<
      std::shared_ptr<types::IAbstractWithoutNumbers>
    > value
  ) override {
    instance_->set_list_abstract_property(value);
  }

  const common::optional<
    std::shared_ptr<types::IParentWithoutNumbers>
  >& optional_parent_property() const override {
    return instance_->optional_parent_property();
  }

  common::optional<
    std::shared_ptr<types::IParentWithoutNumbers>
  >& mutable_optional_parent_property() override {
    return instance_->mutable_optional_parent_property();
  }

  void set_optional_parent_property(
    common::optional<
      std::shared_ptr<types::IParentWithoutNumbers>
    > value
  ) override {
    instance_->set_optional_parent_property(value);
  }

  const types::UnionWithoutNumbers& union_without_numbers_property() const override {
    return instance_->union_without_numbers_property();
  }

  types::UnionWithoutNumbers& mutable_union_without_numbers_property() override {
    return instance_->mutable_union_without_numbers_property();
  }

  void set_union_without_numbers_property(
    types::UnionWithoutNumbers value
  ) override {
    instance_->set_union_without_numbers_property(value);
  }

  const types::UnionWithNumbers& union_with_numbers_property() const override {
    return instance_->union_with_numbers_property();
  }

  types::UnionWithNumbers& mutable_union_with_numbers_property() override {
    return instance_->mutable_union_with_numbers_property();
  }

  void set_union_with_numbers_property(
    types::UnionWithNumbers value
  ) override {
    instance_->set_union_with_numbers_property(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomething(
    std::shared_ptr<types::ISomething> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomething() = default;

 private:
  std::shared_ptr<types::ISomething> instance_;
  std::shared_ptr<E> enhancement_;
};

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IAbstractDescendantWithoutNumbers> WrapAbstractDescendantWithoutNumbers(
  const std::shared_ptr<types::IAbstractDescendantWithoutNumbers>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IAbstractDescendantWithoutNumbers>(
      new EnhancedAbstractDescendantWithoutNumbers<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IParentWithoutNumbers> WrapParentWithoutNumbers(
  const std::shared_ptr<types::IParentWithoutNumbers>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IParentWithoutNumbers>(
      new EnhancedParentWithoutNumbers<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IChildWithoutNumbers> WrapChildWithoutNumbers(
  const std::shared_ptr<types::IChildWithoutNumbers>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IChildWithoutNumbers>(
      new EnhancedChildWithoutNumbers<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IWithNumber> WrapWithNumber(
  const std::shared_ptr<types::IWithNumber>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IWithNumber>(
      new EnhancedWithNumber<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomething> WrapSomething(
  const std::shared_ptr<types::ISomething>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  that->set_abstract_property(
    Wrap<E>(
      that->abstract_property(),
      factory
    )
  );

  that->set_parent_property(
    Wrap<E>(
      that->parent_property(),
      factory
    )
  );

  {
    const std::vector<
      std::shared_ptr<types::IAbstractWithoutNumbers>
    >& value(
      that->list_abstract_property()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IAbstractWithoutNumbers>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IAbstractWithoutNumbers>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_list_abstract_property(
      std::move(wrapped)
    );
  }

  if (that->optional_parent_property().has_value()) {
    const std::shared_ptr<types::IParentWithoutNumbers>& value(
      that->optional_parent_property().value()
    );

    std::shared_ptr<
      types::IParentWithoutNumbers
    > wrapped(
      Wrap<E>(
        value,
        factory
      )
    );

    that->set_optional_parent_property(
      common::make_optional(
        std::move(wrapped)
      )
    );
  }

  that->set_union_without_numbers_property(
    Wrap<E>(
      that->union_without_numbers_property(),
      factory
    )
  );

  that->set_union_with_numbers_property(
    Wrap<E>(
      that->union_with_numbers_property(),
      factory
    )
  );

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomething>(
      new EnhancedSomething<E>(
        that,
        enh
      )
    );
}

/**
 * Assert that the \p that instance has not been already enhanced.
 *
 * \param that instance to be checked
 * \tparam E type of the enhancement
 * \tparam T interface type of \p that instance
 * \throw std::logic_error if \p that already enhanced
 */
template<
  typename E,
  typename T,
  typename std::enable_if<
    std::is_base_of<types::IClass, T>::value
  >::type* = nullptr
>
void AssertNotEnhanced(
  const std::shared_ptr<T>& that
) {
  std::shared_ptr<impl::IEnhanced<E> > enhanced(
    std::dynamic_pointer_cast<
      impl::IEnhanced<E>
    >(that)
  );
  if (enhanced != nullptr) {
    throw std::logic_error(
      common::Concat(
        "An instance of ",
        stringification::to_string(that->model_type()),
        " has been already wrapped."
      )
    );
  }
}

}  // namespace impl
/// \endcond

/**
 * Wrap \p that instance recursively with the enhancement produced by the \p factory.
 *
 * The factory decides itself whether it will produce an enhancement for
 * \p that instance, or not. Even if no enhancement has been produced for \p that
 * instance, we will still continue to enhance the instances referenced
 * by \p that instance recursively.
 *
 * \param that instance to wrap
 * \param factory to selectively produce an enhancement
 * \return enhanced \p that instance
 * \throw std::logic_error if \p that instance has been already wrapped.
 * \tparam E type of the enhancement
 */
///@{

template <typename E>
std::shared_ptr<
  types::IClass
> Wrap(
  const std::shared_ptr<
    types::IClass
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IClass
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAbstractDescendantWithoutNumbers:
      return impl::WrapAbstractDescendantWithoutNumbers<E>(
        std::dynamic_pointer_cast<
          types::IAbstractDescendantWithoutNumbers
        >(that),
        factory
      );
      break;
    case types::ModelType::kParentWithoutNumbers:
      return impl::WrapParentWithoutNumbers<E>(
        std::dynamic_pointer_cast<
          types::IParentWithoutNumbers
        >(that),
        factory
      );
      break;
    case types::ModelType::kChildWithoutNumbers:
      return impl::WrapChildWithoutNumbers<E>(
        std::dynamic_pointer_cast<
          types::IChildWithoutNumbers
        >(that),
        factory
      );
      break;
    case types::ModelType::kWithNumber:
      return impl::WrapWithNumber<E>(
        std::dynamic_pointer_cast<
          types::IWithNumber
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IAbstractWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IAbstractWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IAbstractWithoutNumbers
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAbstractDescendantWithoutNumbers:
      return impl::WrapAbstractDescendantWithoutNumbers<E>(
        std::dynamic_pointer_cast<
          types::IAbstractDescendantWithoutNumbers
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IAbstractDescendantWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IAbstractDescendantWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IAbstractDescendantWithoutNumbers
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAbstractDescendantWithoutNumbers:
      return impl::WrapAbstractDescendantWithoutNumbers<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IParentWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IParentWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IParentWithoutNumbers
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kParentWithoutNumbers:
      return impl::WrapParentWithoutNumbers<E>(
        that,
        factory
      );
      break;
    case types::ModelType::kChildWithoutNumbers:
      return impl::WrapChildWithoutNumbers<E>(
        std::dynamic_pointer_cast<
          types::IChildWithoutNumbers
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IChildWithoutNumbers
> Wrap(
  const std::shared_ptr<
    types::IChildWithoutNumbers
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IChildWithoutNumbers
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kChildWithoutNumbers:
      return impl::WrapChildWithoutNumbers<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IWithNumber
> Wrap(
  const std::shared_ptr<
    types::IWithNumber
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IWithNumber
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kWithNumber:
      return impl::WrapWithNumber<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomething
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
types::UnionWithoutNumbers Wrap(
  const types::UnionWithoutNumbers& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  switch (that.index()) {
    case 0:
      return types::UnionWithoutNumbers(
        common::in_place_index_t<0>(),
        Wrap<E>(
          common::get<0>(that),
          factory
        )
      );
    case 1:
      return types::UnionWithoutNumbers(
        common::in_place_index_t<1>(),
        Wrap<E>(
          common::get<1>(that),
          factory
        )
      );
    default:
      throw std::logic_error("Invalid variant index");
  }
}

template <typename E>
types::UnionWithNumbers Wrap(
  const types::UnionWithNumbers& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  switch (that.index()) {
    case 0:
      return types::UnionWithNumbers(
        common::in_place_index_t<0>(),
        Wrap<E>(
          common::get<0>(that),
          factory
        )
      );
    case 1:
      return types::UnionWithNumbers(
        common::in_place_index_t<1>(),
        Wrap<E>(
          common::get<1>(that),
          factory
        )
      );
    default:
      throw std::logic_error("Invalid variant index");
  }
}

///@}}

/**
 * Try to unwrap the enhancement from \p that instance.
 *
 * \param that instance possibly wrapped with an enhancement
 * \return the enhancement, or `nullptr` if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> Unwrap(
  const std::shared_ptr<types::IClass>& that
) {
  const std::shared_ptr<impl::IEnhanced<E> >& maybe_enhanced(
    std::dynamic_pointer_cast<impl::IEnhanced<E> >(that)
  );

  if (!maybe_enhanced) {
    return nullptr;
  }

  return maybe_enhanced->enhancement();
}

/**
 * Unwrap the enhancement from \p that instance.
 *
 * \remark \p that instance must have been wrapped before.
 *
 * \param that instance expected to be wrapped with an enhancement
 * \return the enhancement
 * \throw std::invalid_argument if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> MustUnwrap(
  const std::shared_ptr<types::IClass>& that
) {
  std::shared_ptr<E> enhancement(
    Unwrap<E>(that)
  );
  if (!enhancement) {
    throw std::invalid_argument(
      common::Concat(
        "Expected an instance of ",
        stringification::to_string(that->model_type()),
        " to have been already wrapped with an enhancement, "
        "but it has been not."
      )
    );
  }
  return enhancement;
}

}  // namespace enhancing
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_ENHANCING_GUARD_
