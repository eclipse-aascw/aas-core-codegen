#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kAbstractDescendantWithoutNumbers = 0,
  kParentWithoutNumbers = 1,
  kChildWithoutNumbers = 2,
  kWithNumber = 3,
  kSomething = 4
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IAbstractWithoutNumbers;

class IAbstractDescendantWithoutNumbers;

class IParentWithoutNumbers;

class IChildWithoutNumbers;

class IWithNumber;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

// region Named unions

/**
 * Represent a union of classes.
 */
using UnionWithoutNumbers = common::variant<
    std::shared_ptr<IAbstractWithoutNumbers>,
    std::shared_ptr<IParentWithoutNumbers>
  >;

/**
 * Represent a union of classes.
 */
using UnionWithNumbers = common::variant<
    std::shared_ptr<IAbstractWithoutNumbers>,
    std::shared_ptr<IWithNumber>
  >;

// endregion

/// \brief Represent an abstract class whose serialization can not fail.
class IAbstractWithoutNumbers
    : virtual public IClass {
 public:
  virtual const std::wstring& some_text() const = 0;

  virtual std::wstring& mutable_some_text() = 0;

  virtual void set_some_text(
    std::wstring value
  ) = 0;

  virtual ~IAbstractWithoutNumbers() = default;
};

class IAbstractDescendantWithoutNumbers
    : virtual public IAbstractWithoutNumbers {
 public:
  virtual const std::wstring& another_text() const = 0;

  virtual std::wstring& mutable_another_text() = 0;

  virtual void set_another_text(
    std::wstring value
  ) = 0;

  virtual ~IAbstractDescendantWithoutNumbers() = default;
};

/// \brief Represent a concrete class with descendants which can not fail.
class IParentWithoutNumbers
    : virtual public IClass {
 public:
  virtual const std::wstring& some_text() const = 0;

  virtual std::wstring& mutable_some_text() = 0;

  virtual void set_some_text(
    std::wstring value
  ) = 0;

  virtual ~IParentWithoutNumbers() = default;
};

class IChildWithoutNumbers
    : virtual public IParentWithoutNumbers {
 public:
  virtual const std::wstring& another_text() const = 0;

  virtual std::wstring& mutable_another_text() = 0;

  virtual void set_another_text(
    std::wstring value
  ) = 0;

  virtual ~IChildWithoutNumbers() = default;
};

/// \brief Represent a class whose serialization can fail.
class IWithNumber
    : virtual public IClass {
 public:
  virtual int64_t some_int() const = 0;

  virtual int64_t& mutable_some_int() = 0;

  virtual void set_some_int(
    int64_t value
  ) = 0;

  virtual ~IWithNumber() = default;
};

/// \brief Hold values whose declared type leaves the run-time type open.
///
/// The serialization of IWithNumber can fail, while
/// the serialization of the classes and the union without numbers can not.
/// The latter must stay infallible even though the dispatch over all
/// the classes can fail.
class ISomething
    : virtual public IClass {
 public:
  virtual const std::shared_ptr<IAbstractWithoutNumbers>& abstract_property() const = 0;

  virtual std::shared_ptr<IAbstractWithoutNumbers>& mutable_abstract_property() = 0;

  virtual void set_abstract_property(
    std::shared_ptr<IAbstractWithoutNumbers> value
  ) = 0;

  virtual const std::shared_ptr<IParentWithoutNumbers>& parent_property() const = 0;

  virtual std::shared_ptr<IParentWithoutNumbers>& mutable_parent_property() = 0;

  virtual void set_parent_property(
    std::shared_ptr<IParentWithoutNumbers> value
  ) = 0;

  virtual const std::vector<
    std::shared_ptr<IAbstractWithoutNumbers>
  >& list_abstract_property() const = 0;

  virtual std::vector<
    std::shared_ptr<IAbstractWithoutNumbers>
  >& mutable_list_abstract_property() = 0;

  virtual void set_list_abstract_property(
    std::vector<
      std::shared_ptr<IAbstractWithoutNumbers>
    > value
  ) = 0;

  virtual const common::optional<
    std::shared_ptr<IParentWithoutNumbers>
  >& optional_parent_property() const = 0;

  virtual common::optional<
    std::shared_ptr<IParentWithoutNumbers>
  >& mutable_optional_parent_property() = 0;

  virtual void set_optional_parent_property(
    common::optional<
      std::shared_ptr<IParentWithoutNumbers>
    > value
  ) = 0;

  virtual const UnionWithoutNumbers& union_without_numbers_property() const = 0;

  virtual UnionWithoutNumbers& mutable_union_without_numbers_property() = 0;

  virtual void set_union_without_numbers_property(
    UnionWithoutNumbers value
  ) = 0;

  virtual const UnionWithNumbers& union_with_numbers_property() const = 0;

  virtual UnionWithNumbers& mutable_union_with_numbers_property() = 0;

  virtual void set_union_with_numbers_property(
    UnionWithNumbers value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class AbstractDescendantWithoutNumbers
    : public IAbstractDescendantWithoutNumbers {
 public:
  AbstractDescendantWithoutNumbers(
    std::wstring some_text,
    std::wstring another_text
  );

  ModelType model_type() const override;

  // region Get and set some_text_

  const std::wstring& some_text() const override;

  std::wstring& mutable_some_text() override;

  void set_some_text(
    std::wstring value
  ) override;

  // endregion

  // region Get and set another_text_

  const std::wstring& another_text() const override;

  std::wstring& mutable_another_text() override;

  void set_another_text(
    std::wstring value
  ) override;

  // endregion

  ~AbstractDescendantWithoutNumbers() override = default;

 private:
  std::wstring some_text_;

  std::wstring another_text_;
};

class ParentWithoutNumbers
    : public IParentWithoutNumbers {
 public:
  explicit ParentWithoutNumbers(
    std::wstring some_text
  );

  ModelType model_type() const override;

  // region Get and set some_text_

  const std::wstring& some_text() const override;

  std::wstring& mutable_some_text() override;

  void set_some_text(
    std::wstring value
  ) override;

  // endregion

  ~ParentWithoutNumbers() override = default;

 private:
  std::wstring some_text_;
};

class ChildWithoutNumbers
    : public IChildWithoutNumbers {
 public:
  ChildWithoutNumbers(
    std::wstring some_text,
    std::wstring another_text
  );

  ModelType model_type() const override;

  // region Get and set some_text_

  const std::wstring& some_text() const override;

  std::wstring& mutable_some_text() override;

  void set_some_text(
    std::wstring value
  ) override;

  // endregion

  // region Get and set another_text_

  const std::wstring& another_text() const override;

  std::wstring& mutable_another_text() override;

  void set_another_text(
    std::wstring value
  ) override;

  // endregion

  ~ChildWithoutNumbers() override = default;

 private:
  std::wstring some_text_;

  std::wstring another_text_;
};

class WithNumber
    : public IWithNumber {
 public:
  explicit WithNumber(
    int64_t some_int
  );

  ModelType model_type() const override;

  // region Get and set some_int_

  int64_t some_int() const override;

  int64_t& mutable_some_int() override;

  void set_some_int(
    int64_t value
  ) override;

  // endregion

  ~WithNumber() override = default;

 private:
  int64_t some_int_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::shared_ptr<IAbstractWithoutNumbers> abstract_property,
    std::shared_ptr<IParentWithoutNumbers> parent_property,
    std::vector<
      std::shared_ptr<IAbstractWithoutNumbers>
    > list_abstract_property,
    UnionWithoutNumbers union_without_numbers_property,
    UnionWithNumbers union_with_numbers_property,
    common::optional<
      std::shared_ptr<IParentWithoutNumbers>
    > optional_parent_property = common::nullopt
  );

  ModelType model_type() const override;

  // region Get and set abstract_property_

  const std::shared_ptr<IAbstractWithoutNumbers>& abstract_property() const override;

  std::shared_ptr<IAbstractWithoutNumbers>& mutable_abstract_property() override;

  void set_abstract_property(
    std::shared_ptr<IAbstractWithoutNumbers> value
  ) override;

  // endregion

  // region Get and set parent_property_

  const std::shared_ptr<IParentWithoutNumbers>& parent_property() const override;

  std::shared_ptr<IParentWithoutNumbers>& mutable_parent_property() override;

  void set_parent_property(
    std::shared_ptr<IParentWithoutNumbers> value
  ) override;

  // endregion

  // region Get and set list_abstract_property_

  const std::vector<
    std::shared_ptr<IAbstractWithoutNumbers>
  >& list_abstract_property() const override;

  std::vector<
    std::shared_ptr<IAbstractWithoutNumbers>
  >& mutable_list_abstract_property() override;

  void set_list_abstract_property(
    std::vector<
      std::shared_ptr<IAbstractWithoutNumbers>
    > value
  ) override;

  // endregion

  // region Get and set optional_parent_property_

  const common::optional<
    std::shared_ptr<IParentWithoutNumbers>
  >& optional_parent_property() const override;

  common::optional<
    std::shared_ptr<IParentWithoutNumbers>
  >& mutable_optional_parent_property() override;

  void set_optional_parent_property(
    common::optional<
      std::shared_ptr<IParentWithoutNumbers>
    > value
  ) override;

  // endregion

  // region Get and set union_without_numbers_property_

  const UnionWithoutNumbers& union_without_numbers_property() const override;

  UnionWithoutNumbers& mutable_union_without_numbers_property() override;

  void set_union_without_numbers_property(
    UnionWithoutNumbers value
  ) override;

  // endregion

  // region Get and set union_with_numbers_property_

  const UnionWithNumbers& union_with_numbers_property() const override;

  UnionWithNumbers& mutable_union_with_numbers_property() override;

  void set_union_with_numbers_property(
    UnionWithNumbers value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::shared_ptr<IAbstractWithoutNumbers> abstract_property_;

  std::shared_ptr<IParentWithoutNumbers> parent_property_;

  std::vector<
    std::shared_ptr<IAbstractWithoutNumbers>
  > list_abstract_property_;

  common::optional<
    std::shared_ptr<IParentWithoutNumbers>
  > optional_parent_property_;

  UnionWithoutNumbers union_without_numbers_property_;

  UnionWithNumbers union_with_numbers_property_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IAbstractWithoutNumbers.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IAbstractWithoutNumbers
 */
bool IsAbstractWithoutNumbers(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IAbstractDescendantWithoutNumbers.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IAbstractDescendantWithoutNumbers
 */
bool IsAbstractDescendantWithoutNumbers(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IParentWithoutNumbers.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IParentWithoutNumbers
 */
bool IsParentWithoutNumbers(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IChildWithoutNumbers.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IChildWithoutNumbers
 */
bool IsChildWithoutNumbers(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IWithNumber.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IWithNumber
 */
bool IsWithNumber(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

// region Underlying instances of named unions

/**
 * \brief Extract the instance held in \p that named union.
 *
 * The named union is a variant over its roots. The instance is returned as
 * \ref IClass, so that it can be checked with the is-a functions, and
 * down-cast with `std::dynamic_pointer_cast`.
 *
 * \param that named union holding the instance
 * \return the instance held in \p that named union
 */
std::shared_ptr<IClass> UnderlyingOfUnionWithoutNumbers(
  const UnionWithoutNumbers& that
);

/**
 * \brief Extract the instance held in \p that named union.
 *
 * The named union is a variant over its roots. The instance is returned as
 * \ref IClass, so that it can be checked with the is-a functions, and
 * down-cast with `std::dynamic_pointer_cast`.
 *
 * \param that named union holding the instance
 * \return the instance held in \p that named union
 */
std::shared_ptr<IClass> UnderlyingOfUnionWithNumbers(
  const UnionWithNumbers& that
);

// endregion Underlying instances of named unions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
