/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

package dummy.types.impl;

import dummy.common.*;
import dummy.visitation.IVisitor;
import dummy.visitation.IVisitorWithContext;
import dummy.visitation.ITransformer;
import dummy.visitation.ITransformerWithContext;
import dummy.types.enums.*;
import dummy.types.impl.*;
import dummy.types.model.*;
import java.util.Collections;
import java.util.List;
import java.util.Optional;
import java.util.Objects;
import java.util.Iterator;
import java.util.Spliterator;
import java.util.function.Consumer;
import java.util.stream.Stream;
import java.util.stream.StreamSupport;
import dummy.types.model.ISomething;
import java.util.Collections;
import java.util.List;
import java.util.Objects;

public class Something implements ISomething {
  private String defaultLanguage;

  private List<ILangStringSet> langStringSets;

  private List<ISpecification> specifications;

  public Something(
    String defaultLanguage,
    List<ILangStringSet> langStringSets) {
    this.defaultLanguage = Objects.requireNonNull(
      defaultLanguage,
      "Argument \"defaultLanguage\" must be non-null.");
    this.langStringSets = Objects.requireNonNull(
      langStringSets,
      "Argument \"langStringSets\" must be non-null.");
  }

  public Something(
    String defaultLanguage,
    List<ILangStringSet> langStringSets,
    List<ISpecification> specifications) {
    this.defaultLanguage = Objects.requireNonNull(
      defaultLanguage,
      "Argument \"defaultLanguage\" must be non-null.");
    this.langStringSets = Objects.requireNonNull(
      langStringSets,
      "Argument \"langStringSets\" must be non-null.");
    this.specifications = specifications;
  }

  @Override
  public String getDefaultLanguage() {
    return defaultLanguage;
  }

  @Override
  public void setDefaultLanguage(String defaultLanguage) {
    this.defaultLanguage = Objects.requireNonNull(
      defaultLanguage,
      "Argument \"defaultLanguage\" must be non-null.");
  }

  @Override
  public List<ILangStringSet> getLangStringSets() {
    return langStringSets;
  }

  @Override
  public void setLangStringSets(List<ILangStringSet> langStringSets) {
    this.langStringSets = Objects.requireNonNull(
      langStringSets,
      "Argument \"langStringSets\" must be non-null.");
  }

  @Override
  public Optional<List<ISpecification>> getSpecifications() {
    return Optional.ofNullable(specifications);
  }

  @Override
  public void setSpecifications(List<ISpecification> specifications) {
    this.specifications = specifications;
  }

  /**
   * Iterate over {@link Something#specifications}, if set,
   * and otherwise return an empty iterator.
   */
  public Iterable<ISpecification> overSpecificationsOrEmpty() {
    return getSpecifications().orElseGet(Collections::emptyList);
  }

  /**
   * Iterate recursively over all the class instances referenced from this instance.
   */
  public Iterable<IClass> descend() {
    return new _SomethingRecursiveIterable();
  }

  /**
   * Iterate over all the class instances referenced from this instance.
   */
  public Iterable<IClass> descendOnce() {
    return new _SomethingIterable();
  }

  /**
   * Accept the {@code visitor} to visit this instance for double dispatch.
   **/
  @Override
  public void accept(IVisitor visitor) {
    visitor.visitSomething(this);
  }

  /**
   * Accept the {@code visitor} to visit this instance for double dispatch
   * with the {@code context}.
   **/
  @Override
  public <ContextT> void accept(
      IVisitorWithContext<ContextT> visitor,
      ContextT context) {
    visitor.visitSomething(this, context);
  }

  /**
   * Accept the {@code transformer} to visit this instance for double dispatch.
   **/
  @Override
  public <T> T transform(ITransformer<T> transformer) {
    return transformer.transformSomething(this);
  }

  /**
   * Accept the {@code transformer} to visit this instance for double dispatch
   * with the {@code context}.
   **/
  @Override
  public <ContextT, T> T transform(
      ITransformerWithContext<ContextT, T> transformer,
      ContextT context) {
    return transformer.transformSomething(this, context);
  }

  private class _SomethingIterable implements Iterable<IClass> {
    @Override
    public Iterator<IClass> iterator() {
      Stream<IClass> stream = stream();

      return stream.iterator();
    }

    @Override
    public void forEach(Consumer<? super IClass> action) {
      Stream<IClass> stream = stream();

      stream.forEach(action);
    }

    @Override
    public Spliterator<IClass> spliterator() {
      Stream<IClass> stream = stream();

      return stream.spliterator();
    }

    private Stream<IClass> stream() {
      Stream<IClass> memberStream = Stream.empty();

      if (langStringSets != null) {
        memberStream = Stream.concat(memberStream,
          Something.this.langStringSets.stream());
      }

      if (specifications != null) {
        memberStream = Stream.concat(memberStream,
          Something.this.specifications.stream());
      }

      return memberStream;
    }
  }

  private class _SomethingRecursiveIterable implements Iterable<IClass> {
    @Override
    public Iterator<IClass> iterator() {
      Stream<IClass> stream = stream();

      return stream.iterator();
    }

    @Override
    public void forEach(Consumer<? super IClass> action) {
      Stream<IClass> stream = stream();

      stream.forEach(action);
    }

    @Override
    public Spliterator<IClass> spliterator() {
      Stream<IClass> stream = stream();

      return stream.spliterator();
    }

    private Stream<IClass> stream() {
      Stream<IClass> memberStream = Stream.empty();

      if (langStringSets != null) {
        memberStream = Stream.concat(memberStream,
          Something.this.langStringSets.stream()
            .flatMap(item -> Stream.concat(Stream.<IClass>of(item),
              StreamSupport.stream(item.descend().spliterator(), false))));
      }

      if (specifications != null) {
        memberStream = Stream.concat(memberStream,
          Something.this.specifications.stream()
            .flatMap(item -> Stream.concat(Stream.<IClass>of(item),
              StreamSupport.stream(item.descend().spliterator(), false))));
      }

      return memberStream;
    }
  }
}

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
