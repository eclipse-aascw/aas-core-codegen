/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

package dummy.visitation;

import dummy.types.model.*;
import dummy.visitation.IVisitor;

/**
 * Just descend through the instances without any action.
 *
 * <p>This class is meaningless for itself. However, it is a good base if you
 * want to descend through instances and apply actions only on a subset of
 * classes.
 */
public class VisitorThrough implements IVisitor {
  public void visit(IClass that) {
    that.accept(this);
  }

  public void visitLangString(
    ILangString that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }

  public void visitLangStringSet(
    ILangStringSet that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }

  public void visitIecContent(
    IIecContent that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }

  public void visitOtherContent(
    IOtherContent that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }

  public void visitSpecification(
    ISpecification that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }

  public void visitSomething(
    ISomething that
  ) {
    // Just descend through, do nothing with {@code that}
    for (IClass something : that.descendOnce()) {
      visit(something);
    }
  }
}

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
