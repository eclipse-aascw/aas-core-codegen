/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

package dummy.types.impl;

import dummy.common.*;
import dummy.visitation.IVisitor;
import dummy.visitation.IVisitorWithContext;
import dummy.visitation.ITransformer;
import dummy.visitation.ITransformerWithContext;
import dummy.types.enums.*;
import dummy.types.impl.*;
import dummy.types.model.*;
import java.util.Collections;
import java.util.List;
import java.util.Optional;
import java.util.Objects;
import java.util.Iterator;
import java.util.Spliterator;
import java.util.function.Consumer;
import java.util.stream.Stream;
import java.util.stream.StreamSupport;
import dummy.types.model.IOrderedContainer;
import java.util.Collections;
import java.util.List;
import java.util.Objects;

public class OrderedContainer implements IOrderedContainer {
  private String identifier;

  private List<IElement> children;

  private Boolean isSorted;

  public OrderedContainer(
    String identifier,
    List<IElement> children,
    Boolean isSorted) {
    this.identifier = Objects.requireNonNull(
      identifier,
      "Argument \"identifier\" must be non-null.");
    this.children = Objects.requireNonNull(
      children,
      "Argument \"children\" must be non-null.");
    this.isSorted = Objects.requireNonNull(
      isSorted,
      "Argument \"isSorted\" must be non-null.");
  }

  @Override
  public String getIdentifier() {
    return identifier;
  }

  @Override
  public void setIdentifier(String identifier) {
    this.identifier = Objects.requireNonNull(
      identifier,
      "Argument \"identifier\" must be non-null.");
  }

  @Override
  public List<IElement> getChildren() {
    return children;
  }

  @Override
  public void setChildren(List<IElement> children) {
    this.children = Objects.requireNonNull(
      children,
      "Argument \"children\" must be non-null.");
  }

  @Override
  public Boolean getIsSorted() {
    return isSorted;
  }

  @Override
  public void setIsSorted(Boolean isSorted) {
    this.isSorted = Objects.requireNonNull(
      isSorted,
      "Argument \"isSorted\" must be non-null.");
  }

  /**
   * Iterate recursively over all the class instances referenced from this instance.
   */
  public Iterable<IClass> descend() {
    return new _OrderedContainerRecursiveIterable();
  }

  /**
   * Iterate over all the class instances referenced from this instance.
   */
  public Iterable<IClass> descendOnce() {
    return new _OrderedContainerIterable();
  }

  /**
   * Accept the {@code visitor} to visit this instance for double dispatch.
   **/
  @Override
  public void accept(IVisitor visitor) {
    visitor.visitOrderedContainer(this);
  }

  /**
   * Accept the {@code visitor} to visit this instance for double dispatch
   * with the {@code context}.
   **/
  @Override
  public <ContextT> void accept(
      IVisitorWithContext<ContextT> visitor,
      ContextT context) {
    visitor.visitOrderedContainer(this, context);
  }

  /**
   * Accept the {@code transformer} to visit this instance for double dispatch.
   **/
  @Override
  public <T> T transform(ITransformer<T> transformer) {
    return transformer.transformOrderedContainer(this);
  }

  /**
   * Accept the {@code transformer} to visit this instance for double dispatch
   * with the {@code context}.
   **/
  @Override
  public <ContextT, T> T transform(
      ITransformerWithContext<ContextT, T> transformer,
      ContextT context) {
    return transformer.transformOrderedContainer(this, context);
  }

  private class _OrderedContainerIterable implements Iterable<IClass> {
    @Override
    public Iterator<IClass> iterator() {
      Stream<IClass> stream = stream();

      return stream.iterator();
    }

    @Override
    public void forEach(Consumer<? super IClass> action) {
      Stream<IClass> stream = stream();

      stream.forEach(action);
    }

    @Override
    public Spliterator<IClass> spliterator() {
      Stream<IClass> stream = stream();

      return stream.spliterator();
    }

    private Stream<IClass> stream() {
      Stream<IClass> memberStream = Stream.empty();

      if (children != null) {
        memberStream = Stream.concat(memberStream,
          OrderedContainer.this.children.stream());
      }

      return memberStream;
    }
  }

  private class _OrderedContainerRecursiveIterable implements Iterable<IClass> {
    @Override
    public Iterator<IClass> iterator() {
      Stream<IClass> stream = stream();

      return stream.iterator();
    }

    @Override
    public void forEach(Consumer<? super IClass> action) {
      Stream<IClass> stream = stream();

      stream.forEach(action);
    }

    @Override
    public Spliterator<IClass> spliterator() {
      Stream<IClass> stream = stream();

      return stream.spliterator();
    }

    private Stream<IClass> stream() {
      Stream<IClass> memberStream = Stream.empty();

      if (children != null) {
        memberStream = Stream.concat(memberStream,
          OrderedContainer.this.children.stream()
            .flatMap(item -> Stream.concat(Stream.<IClass>of(item),
              StreamSupport.stream(item.descend().spliterator(), false))));
      }

      return memberStream;
    }
  }
}

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
