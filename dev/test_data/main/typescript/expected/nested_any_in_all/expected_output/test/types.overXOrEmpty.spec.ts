/**
 * Test `over*OrEmpty` methods.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as TestCommonJsonization from "./commonJsonization";

test("IecContent.overDefinitionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalIecContent();

  let count = 0;
  for (const _ of instance.overDefinitionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.definition?.length ?? 0,
  );
});

test("IecContent.overDefinitionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalIecContent();

  let count = 0;
  for (const _ of instance.overDefinitionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.definition?.length ?? 0,
  );
});

test("Something.overSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSomething();

  let count = 0;
  for (const _ of instance.overSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specifications?.length ?? 0,
  );
});

test("Something.overSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSomething();

  let count = 0;
  for (const _ of instance.overSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specifications?.length ?? 0,
  );
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
