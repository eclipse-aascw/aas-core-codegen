/**
 * Test XML de/serialization of interfaces.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";

import * as TestCommonXmlization from "./commonXmlization";

test(
  "IContent XML round-trip " +
  "starting from IecContent OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalIecContent();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asContent(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IContent XML round-trip via " +
  "contentFromXmlString starting from IecContent OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalIecContent();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.contentFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test(
  "IContent XML round-trip " +
  "starting from OtherContent OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOtherContent();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asContent(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IContent XML round-trip via " +
  "contentFromXmlString starting from OtherContent OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOtherContent();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.contentFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test("IContent XML deserialization fail", () => {
  const instanceOrError = AasXmlization.fromXmlString("This is not XML.");
  expect(instanceOrError.error).not.toBeNull();
});

test(
  "IContent XML deserialization fail via " +
  "contentFromXmlString",
  () => {
    const instanceOrError = AasXmlization.contentFromXmlString(
      "This is not XML."
    );
    expect(instanceOrError.error).not.toBeNull();
  }
);

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
