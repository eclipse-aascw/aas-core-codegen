/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.LangString} from
 * the test data directory.
 */
export function loadMaximalLangString(
): AasTypes.LangString {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangString",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangString(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangString in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangString} from
 * the test data directory.
 */
export function loadMinimalLangString(
): AasTypes.LangString {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangString",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangString(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangString in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringSet} from
 * the test data directory.
 */
export function loadMaximalLangStringSet(
): AasTypes.LangStringSet {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringSet",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringSetFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringSet(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringSet in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringSet} from
 * the test data directory.
 */
export function loadMinimalLangStringSet(
): AasTypes.LangStringSet {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringSet",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringSetFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringSet(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringSet in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.IecContent} from
 * the test data directory.
 */
export function loadMaximalIecContent(
): AasTypes.IecContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "IecContent",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.iecContentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asIecContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of IecContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.IecContent} from
 * the test data directory.
 */
export function loadMinimalIecContent(
): AasTypes.IecContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "IecContent",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.iecContentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asIecContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of IecContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.OtherContent} from
 * the test data directory.
 */
export function loadMaximalOtherContent(
): AasTypes.OtherContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OtherContent",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.otherContentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOtherContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OtherContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.OtherContent} from
 * the test data directory.
 */
export function loadMinimalOtherContent(
): AasTypes.OtherContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OtherContent",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.otherContentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOtherContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OtherContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Specification} from
 * the test data directory.
 */
export function loadMaximalSpecification(
): AasTypes.Specification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Specification",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.specificationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Specification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Specification} from
 * the test data directory.
 */
export function loadMinimalSpecification(
): AasTypes.Specification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Specification",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.specificationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Specification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
