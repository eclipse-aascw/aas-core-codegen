/**
 * Provide functions for loading generated XML examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasXmlization from "../src/xmlization";

import * as TestCommon from "./common";

/**
 * Load a maximal XML example of {@link types.LangString}
 * from the test data directory.
 */
export function loadMaximalLangString(
): AasTypes.LangString {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "langString",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangString(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangString in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.LangString}
 * from the test data directory.
 */
export function loadMinimalLangString(
): AasTypes.LangString {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "langString",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangString(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangString in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.LangStringSet}
 * from the test data directory.
 */
export function loadMaximalLangStringSet(
): AasTypes.LangStringSet {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "langStringSet",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringSet(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringSet in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.LangStringSet}
 * from the test data directory.
 */
export function loadMinimalLangStringSet(
): AasTypes.LangStringSet {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "langStringSet",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringSet(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringSet in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.IecContent}
 * from the test data directory.
 */
export function loadMaximalIecContent(
): AasTypes.IecContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "iecContent",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asIecContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of IecContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.IecContent}
 * from the test data directory.
 */
export function loadMinimalIecContent(
): AasTypes.IecContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "iecContent",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asIecContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of IecContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.OtherContent}
 * from the test data directory.
 */
export function loadMaximalOtherContent(
): AasTypes.OtherContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "otherContent",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOtherContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OtherContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.OtherContent}
 * from the test data directory.
 */
export function loadMinimalOtherContent(
): AasTypes.OtherContent {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "otherContent",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOtherContent(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OtherContent in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Specification}
 * from the test data directory.
 */
export function loadMaximalSpecification(
): AasTypes.Specification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "specification",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Specification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Specification}
 * from the test data directory.
 */
export function loadMinimalSpecification(
): AasTypes.Specification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "specification",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Specification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
