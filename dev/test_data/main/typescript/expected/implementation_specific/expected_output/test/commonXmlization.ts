/**
 * Provide functions for loading generated XML examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasXmlization from "../src/xmlization";

import * as TestCommon from "./common";

/**
 * Load a maximal XML example of {@link types.Box}
 * from the test data directory.
 */
export function loadMaximalBox(
): AasTypes.Box {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "box",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBox(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Box in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Box}
 * from the test data directory.
 */
export function loadMinimalBox(
): AasTypes.Box {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "box",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBox(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Box in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Bag}
 * from the test data directory.
 */
export function loadMaximalBag(
): AasTypes.Bag {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "bag",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBag(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Bag in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Bag}
 * from the test data directory.
 */
export function loadMinimalBag(
): AasTypes.Bag {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "bag",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBag(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Bag in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Container}
 * from the test data directory.
 */
export function loadMaximalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "container",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Container}
 * from the test data directory.
 */
export function loadMinimalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "container",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
