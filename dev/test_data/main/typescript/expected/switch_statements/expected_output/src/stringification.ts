/**
 * De/serialize enumerations from and to string representations.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as AasTypes from "./types";

const MODEL_TYPE_FROM_STRING = new Map<string, AasTypes.ModelType>([
  [
    "Something",
    AasTypes.ModelType.Something
  ]
]);

/**
 * Parse `text` as a string representation of {@link types!ModelType}.
 *
 * @param text - string representation of {@link types!ModelType}
 * @returns literal of {@link types!ModelType}, if valid, and `null` otherwise
 */
export function modelTypeFromString(
  text: string
): AasTypes.ModelType | null {
  const result = MODEL_TYPE_FROM_STRING.get(text);
  return result !== undefined ? result : null;
}

// NOTE (mristin):
// The literals of ModelType are consecutive integers starting at 0,
// so we index into an array instead of looking the text up in a map.
const MODEL_TYPE_TO_STRING: readonly string[] = [
  "Something"
];

/**
 * Translate {@link types!ModelType} to a string.
 *
 * @param value - to be stringified
 * @returns string representation of {@link types!ModelType},
 * if `value` valid, and `null` otherwise
 */
export function modelTypeToString(
  value: AasTypes.ModelType
): string | null {
  const result = MODEL_TYPE_TO_STRING[value];
  return result !== undefined ? result : null;
}

/**
 * Translate {@link types!ModelType} to a string.
 *
 * @param value - to be stringified
 * @returns string representation of {@link types!ModelType}
 * @throws
 * {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error|Error}
 * if the `value` is invalid
 */
export function mustModelTypeToString(
  value: AasTypes.ModelType
): string {
  const result = MODEL_TYPE_TO_STRING[value];
  if (result === undefined) {
    throw new Error(
      `Invalid literal of ModelType: ${value}`
    );
  }
  return result;
}

const KIND_FROM_STRING = new Map<string, AasTypes.Kind>([
  ["alpha", AasTypes.Kind.Alpha],
  ["beta", AasTypes.Kind.Beta],
  ["gamma", AasTypes.Kind.Gamma],
  ["delta", AasTypes.Kind.Delta]
]);

/**
 * Parse `text` as a string representation of {@link types!Kind}.
 *
 * @param text - string representation of {@link types!Kind}
 * @returns literal of {@link types!Kind}, if valid, and `null` otherwise
 */
export function kindFromString(
  text: string
): AasTypes.Kind | null {
  const result = KIND_FROM_STRING.get(text);
  return result !== undefined ? result : null;
}

// NOTE (mristin):
// The literals of Kind are consecutive integers starting at 0,
// so we index into an array instead of looking the text up in a map.
const KIND_TO_STRING: readonly string[] = [
  "alpha", // Alpha
  "beta", // Beta
  "gamma", // Gamma
  "delta" // Delta
];

/**
 * Translate {@link types!Kind} to a string.
 *
 * @param value - to be stringified
 * @returns string representation of {@link types!Kind}, if `value` valid, and `null` otherwise
 */
export function kindToString(
  value: AasTypes.Kind
): string | null {
  const result = KIND_TO_STRING[value];
  return result !== undefined ? result : null;
}

/**
 * Translate {@link types!Kind} to a string.
 *
 * @param value - to be stringified
 * @returns string representation of {@link types!Kind}
 * @throws
 * {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error|Error}
 * if the `value` is invalid
 */
export function mustKindToString(
  value: AasTypes.Kind
): string {
  const result = KIND_TO_STRING[value];
  if (result === undefined) {
    throw new Error(
      `Invalid literal of Kind: ${value}`
    );
  }
  return result;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
