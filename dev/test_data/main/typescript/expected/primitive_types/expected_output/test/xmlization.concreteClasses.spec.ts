/**
 * Test XML de/serialization of concrete classes.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";
import * as AasVerification from "../src/verification";

import * as TestCommon from "./common";

test("Something XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "something"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSomething(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Something in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Something XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "something"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

/**
 * Read the first recorded example of Something with the content
 * of the element `xmlName` replaced by `text`.
 */
function readWith(xmlName: string, text: string): AasTypes.Something {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "something"
      ),
      ".xml"
    )
  );
  pths.sort();

  expect(pths.length).toBeGreaterThan(0);

  const original = fs.readFileSync(pths[0], "utf-8");

  const start = original.indexOf(`<${xmlName}>`) + xmlName.length + 2;
  const end = original.indexOf(`</${xmlName}>`);

  const patched = original.slice(0, start) + text + original.slice(end);

  const instanceOrError = AasXmlization.fromXmlString(patched);
  expect(instanceOrError.error).toBeNull();

  const casted = AasTypes.asSomething(instanceOrError.mustValue());
  if (casted === null) {
    throw new Error(`Expected an instance of Something`);
  }

  return casted;
}

test("someFloat read from '1e400'", () => {
  const instance = readWith(
    "someFloat",
    "1e400"
  );
  expect(instance.someFloat).toBe(Infinity);
});

test("someFloat read from '-1e400'", () => {
  const instance = readWith(
    "someFloat",
    "-1e400"
  );
  expect(instance.someFloat).toBe(-Infinity);
});

test("someFloat read from '1e-400'", () => {
  const instance = readWith(
    "someFloat",
    "1e-400"
  );
  expect(instance.someFloat).toBe(0);
});

test("someFloat read from 'INF'", () => {
  const instance = readWith(
    "someFloat",
    "INF"
  );
  expect(instance.someFloat).toBe(Infinity);
});

test("someFloat read from '+INF'", () => {
  const instance = readWith(
    "someFloat",
    "+INF"
  );
  expect(instance.someFloat).toBe(Infinity);
});

test("someFloat read from '-INF'", () => {
  const instance = readWith(
    "someFloat",
    "-INF"
  );
  expect(instance.someFloat).toBe(-Infinity);
});

test("someBool read from '1'", () => {
  const instance = readWith(
    "someBool",
    "1"
  );
  expect(instance.someBool).toBe(true);
});

test("someBool read from '0'", () => {
  const instance = readWith(
    "someBool",
    "0"
  );
  expect(instance.someBool).toBe(false);
});

test("someBool read from 'true'", () => {
  const instance = readWith(
    "someBool",
    "true"
  );
  expect(instance.someBool).toBe(true);
});

test("someBool read from 'false'", () => {
  const instance = readWith(
    "someBool",
    "false"
  );
  expect(instance.someBool).toBe(false);
});

test("someBytes read from 'SGk='", () => {
  const instance = readWith(
    "someBytes",
    "SGk="
  );
  expect(instance.someBytes).toEqual(new Uint8Array([72, 105]));
});

test("someBytes read from 'SG k='", () => {
  const instance = readWith(
    "someBytes",
    "SG k="
  );
  expect(instance.someBytes).toEqual(new Uint8Array([72, 105]));
});

test("someBytes read from 'S G k ='", () => {
  const instance = readWith(
    "someBytes",
    "S G k ="
  );
  expect(instance.someBytes).toEqual(new Uint8Array([72, 105]));
});

test("someBytes read from ''", () => {
  const instance = readWith(
    "someBytes",
    ""
  );
  expect(instance.someBytes).toEqual(new Uint8Array([]));
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
