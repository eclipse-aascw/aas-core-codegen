/**
 * Test XML de/serialization of interfaces.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";

import * as TestCommonXmlization from "./commonXmlization";

test(
  "IElement XML round-trip " +
  "starting from Leaf OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalLeaf();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asElement(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IElement XML round-trip via " +
  "elementFromXmlString starting from Leaf OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalLeaf();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.elementFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test(
  "IElement XML round-trip " +
  "starting from OrderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOrderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asElement(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IElement XML round-trip via " +
  "elementFromXmlString starting from OrderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOrderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.elementFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test(
  "IElement XML round-trip " +
  "starting from UnorderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalUnorderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asElement(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IElement XML round-trip via " +
  "elementFromXmlString starting from UnorderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalUnorderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.elementFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test("IElement XML deserialization fail", () => {
  const instanceOrError = AasXmlization.fromXmlString("This is not XML.");
  expect(instanceOrError.error).not.toBeNull();
});

test(
  "IElement XML deserialization fail via " +
  "elementFromXmlString",
  () => {
    const instanceOrError = AasXmlization.elementFromXmlString(
      "This is not XML."
    );
    expect(instanceOrError.error).not.toBeNull();
  }
);

test(
  "IContainer XML round-trip " +
  "starting from OrderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOrderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asContainer(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IContainer XML round-trip via " +
  "containerFromXmlString starting from OrderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalOrderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.containerFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test(
  "IContainer XML round-trip " +
  "starting from UnorderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalUnorderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.fromXmlString(xmlText);
    expect(anotherInstanceOrError.error).toBeNull();
    const anotherInstance = anotherInstanceOrError.mustValue();

    const asInterface = AasTypes.asContainer(anotherInstance);
    expect(asInterface).not.toBeNull();
  }
);

test(
  "IContainer XML round-trip via " +
  "containerFromXmlString starting from UnorderedContainer OK",
  () => {
    const instance = TestCommonXmlization.loadMinimalUnorderedContainer();

    const xmlText = AasXmlization.toXmlString(instance);
    const anotherInstanceOrError = AasXmlization.containerFromXmlString(
      xmlText
    );
    expect(anotherInstanceOrError.error).toBeNull();
  }
);

test("IContainer XML deserialization fail", () => {
  const instanceOrError = AasXmlization.fromXmlString("This is not XML.");
  expect(instanceOrError.error).not.toBeNull();
});

test(
  "IContainer XML deserialization fail via " +
  "containerFromXmlString",
  () => {
    const instanceOrError = AasXmlization.containerFromXmlString(
      "This is not XML."
    );
    expect(instanceOrError.error).not.toBeNull();
  }
);

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
