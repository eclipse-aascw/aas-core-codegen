/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.Leaf} from
 * the test data directory.
 */
export function loadMaximalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Leaf",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.leafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Leaf} from
 * the test data directory.
 */
export function loadMinimalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Leaf",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.leafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.OrderedContainer} from
 * the test data directory.
 */
export function loadMaximalOrderedContainer(
): AasTypes.OrderedContainer {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OrderedContainer",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.orderedContainerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOrderedContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OrderedContainer in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.OrderedContainer} from
 * the test data directory.
 */
export function loadMinimalOrderedContainer(
): AasTypes.OrderedContainer {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OrderedContainer",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.orderedContainerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOrderedContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OrderedContainer in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.UnorderedContainer} from
 * the test data directory.
 */
export function loadMaximalUnorderedContainer(
): AasTypes.UnorderedContainer {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "UnorderedContainer",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.unorderedContainerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asUnorderedContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of UnorderedContainer in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.UnorderedContainer} from
 * the test data directory.
 */
export function loadMinimalUnorderedContainer(
): AasTypes.UnorderedContainer {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "UnorderedContainer",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.unorderedContainerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asUnorderedContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of UnorderedContainer in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.GlobalAttribute} from
 * the test data directory.
 */
export function loadMaximalGlobalAttribute(
): AasTypes.GlobalAttribute {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "GlobalAttribute",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.globalAttributeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asGlobalAttribute(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of GlobalAttribute in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.GlobalAttribute} from
 * the test data directory.
 */
export function loadMinimalGlobalAttribute(
): AasTypes.GlobalAttribute {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "GlobalAttribute",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.globalAttributeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asGlobalAttribute(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of GlobalAttribute in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LocalAttribute} from
 * the test data directory.
 */
export function loadMaximalLocalAttribute(
): AasTypes.LocalAttribute {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LocalAttribute",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.localAttributeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLocalAttribute(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LocalAttribute in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LocalAttribute} from
 * the test data directory.
 */
export function loadMinimalLocalAttribute(
): AasTypes.LocalAttribute {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LocalAttribute",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.localAttributeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLocalAttribute(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LocalAttribute in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.AttributeOperand} from
 * the test data directory.
 */
export function loadMaximalAttributeOperand(
): AasTypes.AttributeOperand {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AttributeOperand",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.attributeOperandFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAttributeOperand(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AttributeOperand in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.AttributeOperand} from
 * the test data directory.
 */
export function loadMinimalAttributeOperand(
): AasTypes.AttributeOperand {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AttributeOperand",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.attributeOperandFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAttributeOperand(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AttributeOperand in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.StringLiteral} from
 * the test data directory.
 */
export function loadMaximalStringLiteral(
): AasTypes.StringLiteral {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StringLiteral",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.stringLiteralFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStringLiteral(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StringLiteral in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.StringLiteral} from
 * the test data directory.
 */
export function loadMinimalStringLiteral(
): AasTypes.StringLiteral {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StringLiteral",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.stringLiteralFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStringLiteral(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StringLiteral in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.NumberLiteral} from
 * the test data directory.
 */
export function loadMaximalNumberLiteral(
): AasTypes.NumberLiteral {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "NumberLiteral",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.numberLiteralFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asNumberLiteral(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of NumberLiteral in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.NumberLiteral} from
 * the test data directory.
 */
export function loadMinimalNumberLiteral(
): AasTypes.NumberLiteral {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "NumberLiteral",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.numberLiteralFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asNumberLiteral(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of NumberLiteral in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
