/**
 * Test XML de/serialization of concrete classes.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";
import * as AasVerification from "../src/verification";

import * as TestCommon from "./common";

test("Leaf XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "leaf"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLeaf(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Leaf in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Leaf XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "leaf"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("OrderedContainer XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "orderedContainer"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asOrderedContainer(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of OrderedContainer in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("OrderedContainer XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "orderedContainer"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("UnorderedContainer XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "unorderedContainer"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asUnorderedContainer(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of UnorderedContainer in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("UnorderedContainer XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "unorderedContainer"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("GlobalAttribute XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "globalAttribute"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asGlobalAttribute(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of GlobalAttribute in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("GlobalAttribute XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "globalAttribute"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LocalAttribute XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "localAttribute"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLocalAttribute(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LocalAttribute in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LocalAttribute XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "localAttribute"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AttributeOperand XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "attributeOperand"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAttributeOperand(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AttributeOperand in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AttributeOperand XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "attributeOperand"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("StringLiteral XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "stringLiteral"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asStringLiteral(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of StringLiteral in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("StringLiteral XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "stringLiteral"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("NumberLiteral XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "numberLiteral"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asNumberLiteral(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of NumberLiteral in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("NumberLiteral XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "numberLiteral"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Something XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "something"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSomething(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Something in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Something XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "something"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
