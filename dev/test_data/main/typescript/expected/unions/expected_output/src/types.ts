// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

/**
 * Represent runtime model type of an instance.
 */
export enum ModelType {
  StructuralFirst = 0,
  StructuralSecond = 1,
  MixedAbstractDescendantOne = 2,
  MixedAbstractDescendantTwo = 3,
  MixedConcreteWithDescendants = 4,
  MixedConcreteWithDescendantsChild = 5,
  MixedConcreteLeaf = 6,
  ModelTypedFirst = 7,
  ModelTypedSecond = 8,
  Something = 9
}

/**
 * Iterate over the literals of {@link ModelType}.
 *
 * @remark
 * TypeScript does not provide an elegant way to iterate over the literals, so
 * this function helps you avoid common errors and pitfalls.
 *
 * @return iterator over the literals
 */
export function *overModelType (
): Iterable<ModelType> {
  // NOTE (mristin, 2022-12-03):
  // We yield numbers instead of literals to avoid name lookups on platforms
  // which do not provide JIT compilation of hot paths.
  yield <ModelType>0;  // StructuralFirst
  yield <ModelType>1;  // StructuralSecond
  yield <ModelType>2;  // MixedAbstractDescendantOne
  yield <ModelType>3;  // MixedAbstractDescendantTwo
  yield <ModelType>4;  // MixedConcreteWithDescendants
  yield <ModelType>5;  // MixedConcreteWithDescendantsChild
  yield <ModelType>6;  // MixedConcreteLeaf
  yield <ModelType>7;  // ModelTypedFirst
  yield <ModelType>8;  // ModelTypedSecond
  yield <ModelType>9;  // Something
}

/**
 * Represent the most general class of an AAS model.
 */
export abstract class Class {
  /**
   * Indicate the runtime model type of an instance.
   */
  abstract modelType(): ModelType;

  /**
   * Iterate over all the instances referenced from this one.
   */
  abstract descendOnce(): IterableIterator<Class>;

  /**
   * Iterate recursively over all the instances referenced from this one.
   */
  abstract descend(): IterableIterator<Class>;

  /**
   * Dispatch the `visitor` on this instance.
   *
   * @param visitor - to be dispatched
   */
  abstract accept(visitor: AbstractVisitor): void;

  /**
   * Dispatch the `visitor` on this instance with `context`.
   *
   * @param visitor - to be dispatched
   * @param context - of the visitation
   * @typeParam ContextT - type of the visitation context
   */
  abstract acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ): void;

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to be dispatched
   * @return this instance transformed
   * @typeParam T - type of the transformation result
   */
  abstract transform<T>(transformer: AbstractTransformer<T>): T;

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to be dispatched
   * @param context - of the transformation
   * @return this instance transformed
   * @typeParam T - type of the transformation result
   */
  abstract transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T;
}

export class StructuralFirst extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>0;  // StructuralFirst
  }

  uniqueToFirst: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitStructuralFirst(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitStructuralFirstWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformStructuralFirst(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformStructuralFirstWithContext(
      this, context
    );
  }

  constructor(uniqueToFirst: string) {
    super();
    this.uniqueToFirst = uniqueToFirst;
  }
}

export class StructuralSecond extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>1;  // StructuralSecond
  }

  uniqueToSecond: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitStructuralSecond(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitStructuralSecondWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformStructuralSecond(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformStructuralSecondWithContext(
      this, context
    );
  }

  constructor(uniqueToSecond: string) {
    super();
    this.uniqueToSecond = uniqueToSecond;
  }
}

export type StructuralUnion = StructuralFirst | StructuralSecond;

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface IMixedAbstractMember extends Class {
  // Intentionally empty.
}

export class MixedAbstractDescendantOne
  extends Class
  implements IMixedAbstractMember {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>2;  // MixedAbstractDescendantOne
  }

  uniqueToAbstractDescendantOne: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitMixedAbstractDescendantOne(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitMixedAbstractDescendantOneWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformMixedAbstractDescendantOne(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformMixedAbstractDescendantOneWithContext(
      this, context
    );
  }

  constructor(uniqueToAbstractDescendantOne: string) {
    super();
    this.uniqueToAbstractDescendantOne = uniqueToAbstractDescendantOne;
  }
}

export class MixedAbstractDescendantTwo
  extends Class
  implements IMixedAbstractMember {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>3;  // MixedAbstractDescendantTwo
  }

  uniqueToAbstractDescendantTwo: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitMixedAbstractDescendantTwo(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitMixedAbstractDescendantTwoWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformMixedAbstractDescendantTwo(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformMixedAbstractDescendantTwoWithContext(
      this, context
    );
  }

  constructor(uniqueToAbstractDescendantTwo: string) {
    super();
    this.uniqueToAbstractDescendantTwo = uniqueToAbstractDescendantTwo;
  }
}

export interface IMixedConcreteWithDescendants extends Class {
  someBaseProperty: string;
}

export class MixedConcreteWithDescendants
  extends Class
  implements IMixedConcreteWithDescendants {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>4;  // MixedConcreteWithDescendants
  }

  someBaseProperty: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitMixedConcreteWithDescendants(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitMixedConcreteWithDescendantsWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformMixedConcreteWithDescendants(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformMixedConcreteWithDescendantsWithContext(
      this, context
    );
  }

  constructor(someBaseProperty: string) {
    super();
    this.someBaseProperty = someBaseProperty;
  }
}

export class MixedConcreteWithDescendantsChild
  extends Class
  implements IMixedConcreteWithDescendants {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>5;  // MixedConcreteWithDescendantsChild
  }

  someBaseProperty: string;

  someChildProperty: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitMixedConcreteWithDescendantsChild(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitMixedConcreteWithDescendantsChildWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformMixedConcreteWithDescendantsChild(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformMixedConcreteWithDescendantsChildWithContext(
      this, context
    );
  }

  constructor(
    someBaseProperty: string,
    someChildProperty: string
  ) {
    super();
    this.someBaseProperty = someBaseProperty;
    this.someChildProperty = someChildProperty;
  }
}

export class MixedConcreteLeaf extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>6;  // MixedConcreteLeaf
  }

  uniqueToConcreteLeaf: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitMixedConcreteLeaf(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitMixedConcreteLeafWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformMixedConcreteLeaf(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformMixedConcreteLeafWithContext(
      this, context
    );
  }

  constructor(uniqueToConcreteLeaf: string) {
    super();
    this.uniqueToConcreteLeaf = uniqueToConcreteLeaf;
  }
}

export type MixedUnion =
  | MixedAbstractDescendantOne
  | MixedAbstractDescendantTwo
  | MixedConcreteWithDescendantsChild
  | MixedConcreteWithDescendants
  | MixedConcreteLeaf;

export class ModelTypedFirst extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>7;  // ModelTypedFirst
  }

  someProperty: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitModelTypedFirst(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitModelTypedFirstWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformModelTypedFirst(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformModelTypedFirstWithContext(
      this, context
    );
  }

  constructor(someProperty: string) {
    super();
    this.someProperty = someProperty;
  }
}

export class ModelTypedSecond extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>8;  // ModelTypedSecond
  }

  someProperty: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitModelTypedSecond(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitModelTypedSecondWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformModelTypedSecond(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformModelTypedSecondWithContext(
      this, context
    );
  }

  constructor(someProperty: string) {
    super();
    this.someProperty = someProperty;
  }
}

export type ModelTypedUnion = ModelTypedFirst | ModelTypedSecond;

export class Something extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>9;  // Something
  }

  structuralProperty: StructuralUnion;

  mixedProperty: MixedUnion;

  modelTypedProperty: ModelTypedUnion;

  listStructuralProperty: Array<StructuralUnion>;

  listMixedProperty: Array<MixedUnion>;

  listModelTypedProperty: Array<ModelTypedUnion>;

  tupleProperty: [StructuralUnion, MixedUnion, ModelTypedUnion];

  optionalStructuralProperty: StructuralUnion | null;

  optionalMixedProperty: MixedUnion | null;

  optionalModelTypedProperty: ModelTypedUnion | null;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    yield this.structuralProperty;

    yield this.mixedProperty;

    yield this.modelTypedProperty;

    yield * this.listStructuralProperty;

    yield * this.listMixedProperty;

    yield * this.listModelTypedProperty;

    yield this.tupleProperty[0];

    yield this.tupleProperty[1];

    yield this.tupleProperty[2];

    if (this.optionalStructuralProperty !== null) {
      yield this.optionalStructuralProperty;
    }

    if (this.optionalMixedProperty !== null) {
      yield this.optionalMixedProperty;
    }

    if (this.optionalModelTypedProperty !== null) {
      yield this.optionalModelTypedProperty;
    }
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    yield this.structuralProperty;

    yield * this.structuralProperty.descend();

    yield this.mixedProperty;

    yield * this.mixedProperty.descend();

    yield this.modelTypedProperty;

    yield * this.modelTypedProperty.descend();

    for (const anItem of this.listStructuralProperty) {
      yield anItem;

      yield * anItem.descend();
    }

    for (const anotherItem of this.listMixedProperty) {
      yield anotherItem;

      yield * anotherItem.descend();
    }

    for (const yetAnotherItem of this.listModelTypedProperty) {
      yield yetAnotherItem;

      yield * yetAnotherItem.descend();
    }

    yield this.tupleProperty[0];

    yield * this.tupleProperty[0].descend();

    yield this.tupleProperty[1];

    yield * this.tupleProperty[1].descend();

    yield this.tupleProperty[2];

    yield * this.tupleProperty[2].descend();

    if (this.optionalStructuralProperty !== null) {
      yield this.optionalStructuralProperty;

      yield * this.optionalStructuralProperty.descend();
    }

    if (this.optionalMixedProperty !== null) {
      yield this.optionalMixedProperty;

      yield * this.optionalMixedProperty.descend();
    }

    if (this.optionalModelTypedProperty !== null) {
      yield this.optionalModelTypedProperty;

      yield * this.optionalModelTypedProperty.descend();
    }
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitSomething(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitSomethingWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformSomething(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformSomethingWithContext(
      this, context
    );
  }

  constructor(
    structuralProperty: StructuralUnion,
    mixedProperty: MixedUnion,
    modelTypedProperty: ModelTypedUnion,
    listStructuralProperty: Array<StructuralUnion>,
    listMixedProperty: Array<MixedUnion>,
    listModelTypedProperty: Array<ModelTypedUnion>,
    tupleProperty: [StructuralUnion, MixedUnion, ModelTypedUnion],
    optionalStructuralProperty: StructuralUnion | null = null,
    optionalMixedProperty: MixedUnion | null = null,
    optionalModelTypedProperty: ModelTypedUnion | null = null
  ) {
    super();
    this.structuralProperty = structuralProperty;
    this.mixedProperty = mixedProperty;
    this.modelTypedProperty = modelTypedProperty;
    this.listStructuralProperty = listStructuralProperty;
    this.listMixedProperty = listMixedProperty;
    this.listModelTypedProperty = listModelTypedProperty;
    this.tupleProperty = tupleProperty;
    this.optionalStructuralProperty = optionalStructuralProperty;
    this.optionalMixedProperty = optionalMixedProperty;
    this.optionalModelTypedProperty = optionalModelTypedProperty;
  }
}

/**
 * Visit the instances of the model.
 */
export abstract class AbstractVisitor {
  /**
   * Double-dispatch on `that`.
   */
  visit(that: Class): void {
    that.accept(this);
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitStructuralFirst(
    that: StructuralFirst
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitStructuralSecond(
    that: StructuralSecond
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitModelTypedFirst(
    that: ModelTypedFirst
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitModelTypedSecond(
    that: ModelTypedSecond
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitSomething(
    that: Something
  ): void;
}

/**
 * Visit the instances of the model with context.
 *
 * @typeParam ContextT - type of the visitation context
 */
export abstract class AbstractVisitorWithContext<ContextT> {
  /**
   * Double-dispatch on `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitWithContext(
    that: Class,
    context: ContextT
  ): void {
    that.acceptWithContext(this, context);
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitStructuralFirstWithContext(
    that: StructuralFirst,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitStructuralSecondWithContext(
    that: StructuralSecond,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitMixedAbstractDescendantOneWithContext(
    that: MixedAbstractDescendantOne,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitMixedAbstractDescendantTwoWithContext(
    that: MixedAbstractDescendantTwo,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitMixedConcreteWithDescendantsWithContext(
    that: MixedConcreteWithDescendants,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitMixedConcreteWithDescendantsChildWithContext(
    that: MixedConcreteWithDescendantsChild,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitMixedConcreteLeafWithContext(
    that: MixedConcreteLeaf,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitModelTypedFirstWithContext(
    that: ModelTypedFirst,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitModelTypedSecondWithContext(
    that: ModelTypedSecond,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitSomethingWithContext(
    that: Something,
    context: ContextT
  ): void;
}

/**
 * Visit the instances of the model without action.
 *
 * @remarks
 * This visitor is not meant to be directly used. Instead, you usually
 * inherit from it, and implement only the relevant visit methods.
 */
export class PassThroughVisitor extends AbstractVisitor {
  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitStructuralFirst(
    that: StructuralFirst
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitStructuralSecond(
    that: StructuralSecond
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitModelTypedFirst(
    that: ModelTypedFirst
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitModelTypedSecond(
    that: ModelTypedSecond
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitSomething(
    that: Something
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }
}

/**
 * Visit the instances of the model without action and in context.
 *
 * @remarks
 * This visitor is not meant to be directly used. Instead, you usually
 * inherit from it, and implement only the relevant visit methods.
 */
export class PassThroughVisitorWithContext<ContextT>
    extends AbstractVisitorWithContext<ContextT> {
  /**
   * Double-dispatch on `that` in `context`.
   */
  visitWithContext(
    that: Class,
    context: ContextT
  ): void {
    that.acceptWithContext(this, context);
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitStructuralFirstWithContext(
    that: StructuralFirst,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitStructuralSecondWithContext(
    that: StructuralSecond,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitMixedAbstractDescendantOneWithContext(
    that: MixedAbstractDescendantOne,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitMixedAbstractDescendantTwoWithContext(
    that: MixedAbstractDescendantTwo,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitMixedConcreteWithDescendantsWithContext(
    that: MixedConcreteWithDescendants,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitMixedConcreteWithDescendantsChildWithContext(
    that: MixedConcreteWithDescendantsChild,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitMixedConcreteLeafWithContext(
    that: MixedConcreteLeaf,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitModelTypedFirstWithContext(
    that: ModelTypedFirst,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitModelTypedSecondWithContext(
    that: ModelTypedSecond,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitSomethingWithContext(
    that: Something,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }
}

/**
 * Transform the instance of the model.
 *
 * @typeParam T - type of the transformation result
 */
export abstract class AbstractTransformer<T> {
  /**
   * Double-dispatch on `that`.
   */
  transform(that: Class): T {
    return that.transform(this);
  }

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformStructuralFirst(
    that: StructuralFirst
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformStructuralSecond(
    that: StructuralSecond
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformModelTypedFirst(
    that: ModelTypedFirst
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformModelTypedSecond(
    that: ModelTypedSecond
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformSomething(
    that: Something
  ): T;
}

/**
 * Transform the instances of the model in context.
 *
 * @typeParam ContextT - type of the transformation context
 * @typeParam T - type of the transformation result
 */
export abstract class AbstractTransformerWithContext<ContextT, T> {
  /**
   * Double-dispatch on `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  transformWithContext(
    that: Class,
    context: ContextT
  ): T {
    return that.transformWithContext(this, context);
  }

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformStructuralFirstWithContext(
    that: StructuralFirst,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformStructuralSecondWithContext(
    that: StructuralSecond,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformMixedAbstractDescendantOneWithContext(
    that: MixedAbstractDescendantOne,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformMixedAbstractDescendantTwoWithContext(
    that: MixedAbstractDescendantTwo,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformMixedConcreteWithDescendantsWithContext(
    that: MixedConcreteWithDescendants,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformMixedConcreteWithDescendantsChildWithContext(
    that: MixedConcreteWithDescendantsChild,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformMixedConcreteLeafWithContext(
    that: MixedConcreteLeaf,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformModelTypedFirstWithContext(
    that: ModelTypedFirst,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformModelTypedSecondWithContext(
    that: ModelTypedSecond,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformSomethingWithContext(
    that: Something,
    context: ContextT
  ): T;
}

/**
 * Transform the instances of the model.
 *
 * @remarks
 * If you do not override the transformation methods, they simply
 * return {@link defaultResult}.
 *
 * @typeParam T - type of the transformation result
 */
export class TransformerWithDefault<T> extends AbstractTransformer<T> {
  /**
   * Default value which is returned if no override of the transformation
   */
  defaultResult: T

  /**
   * Initialize with the given `default` value.
   *
   * @param defaultResult - returned if no override of the transformation
   */
  constructor(defaultResult: T) {
    super();
    this.defaultResult = defaultResult;
  }

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralFirst(
    that: StructuralFirst
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralSecond(
    that: StructuralSecond
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedFirst(
    that: ModelTypedFirst
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedSecond(
    that: ModelTypedSecond
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

/**
 * Transform the instances of the model in context.
 *
 * @remarks
 * If you do not override the transformation methods, they simply
 * return {@link defaultResult}.
 *
 * @typeParam ContextT - type of the visitation context
 * @typeParam T - type of the transformation result
 */
export class TransformerWithDefaultAndContext<ContextT, T>
    extends AbstractTransformerWithContext<ContextT, T> {
  /**
   * Default value which is returned if no override of the transformation
   */
  defaultResult: T

  /**
   * Initialize with the given `default` value.
   *
   * @param defaultResult - returned if no override of the transformation
   */
  constructor(defaultResult: T) {
    super();
    this.defaultResult = defaultResult;
  }

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralFirstWithContext(
    that: StructuralFirst,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralSecondWithContext(
    that: StructuralSecond,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantOneWithContext(
    that: MixedAbstractDescendantOne,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantTwoWithContext(
    that: MixedAbstractDescendantTwo,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsWithContext(
    that: MixedConcreteWithDescendants,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsChildWithContext(
    that: MixedConcreteWithDescendantsChild,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteLeafWithContext(
    that: MixedConcreteLeaf,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedFirstWithContext(
    that: ModelTypedFirst,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedSecondWithContext(
    that: ModelTypedSecond,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomethingWithContext(
    that: Something,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

/**
 * Try to cast `that` instance to
 * the class {@link StructuralFirst}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asStructuralFirst(
  that: Class
): StructuralFirst | null {
  return (that instanceof StructuralFirst)
    ? <StructuralFirst>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isStructuralFirst(
  that: Class
): that is StructuralFirst {
  return that instanceof StructuralFirst;
}

/**
 * Try to cast `that` instance to
 * the class {@link StructuralSecond}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asStructuralSecond(
  that: Class
): StructuralSecond | null {
  return (that instanceof StructuralSecond)
    ? <StructuralSecond>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isStructuralSecond(
  that: Class
): that is StructuralSecond {
  return that instanceof StructuralSecond;
}

/**
 * Try to cast an instance of the model to {@link IMixedAbstractMember}.
 */
class AsMixedAbstractMemberTransformer
    extends AbstractTransformer<IMixedAbstractMember | null> {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralFirst(
    that: StructuralFirst
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralSecond(
    that: StructuralSecond
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  transformMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): IMixedAbstractMember | null {
    return that as IMixedAbstractMember;
  }

  transformMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): IMixedAbstractMember | null {
    return that as IMixedAbstractMember;
  }

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedFirst(
    that: ModelTypedFirst
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedSecond(
    that: ModelTypedSecond
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): IMixedAbstractMember | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const AS_MIXED_ABSTRACT_MEMBER_TRANSFORMER =
  new AsMixedAbstractMemberTransformer();

/**
 * Try to cast `that` instance to
 * the interface {@link IMixedAbstractMember}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedAbstractMember(
  that: Class
): IMixedAbstractMember | null {
  return AS_MIXED_ABSTRACT_MEMBER_TRANSFORMER.transform(that);
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedAbstractMember(
  that: Class
): that is IMixedAbstractMember {
  return asMixedAbstractMember(that) !== null;
}

/**
 * Try to cast `that` instance to
 * the class {@link MixedAbstractDescendantOne}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedAbstractDescendantOne(
  that: Class
): MixedAbstractDescendantOne | null {
  return (that instanceof MixedAbstractDescendantOne)
    ? <MixedAbstractDescendantOne>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedAbstractDescendantOne(
  that: Class
): that is MixedAbstractDescendantOne {
  return that instanceof MixedAbstractDescendantOne;
}

/**
 * Try to cast `that` instance to
 * the class {@link MixedAbstractDescendantTwo}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedAbstractDescendantTwo(
  that: Class
): MixedAbstractDescendantTwo | null {
  return (that instanceof MixedAbstractDescendantTwo)
    ? <MixedAbstractDescendantTwo>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedAbstractDescendantTwo(
  that: Class
): that is MixedAbstractDescendantTwo {
  return that instanceof MixedAbstractDescendantTwo;
}

/**
 * Try to cast an instance of the model to {@link IMixedConcreteWithDescendants}.
 */
class AsMixedConcreteWithDescendantsTransformer
    extends AbstractTransformer<IMixedConcreteWithDescendants | null> {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralFirst(
    that: StructuralFirst
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralSecond(
    that: StructuralSecond
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantOne(
    that: MixedAbstractDescendantOne
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantTwo(
    that: MixedAbstractDescendantTwo
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  transformMixedConcreteWithDescendants(
    that: MixedConcreteWithDescendants
  ): IMixedConcreteWithDescendants | null {
    return that as IMixedConcreteWithDescendants;
  }

  transformMixedConcreteWithDescendantsChild(
    that: MixedConcreteWithDescendantsChild
  ): IMixedConcreteWithDescendants | null {
    return that as IMixedConcreteWithDescendants;
  }

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteLeaf(
    that: MixedConcreteLeaf
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedFirst(
    that: ModelTypedFirst
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedSecond(
    that: ModelTypedSecond
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): IMixedConcreteWithDescendants | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const AS_MIXED_CONCRETE_WITH_DESCENDANTS_TRANSFORMER =
  new AsMixedConcreteWithDescendantsTransformer();

/**
 * Try to cast `that` instance to
 * the interface {@link IMixedConcreteWithDescendants}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedConcreteWithDescendants(
  that: Class
): IMixedConcreteWithDescendants | null {
  return AS_MIXED_CONCRETE_WITH_DESCENDANTS_TRANSFORMER.transform(that);
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedConcreteWithDescendants(
  that: Class
): that is IMixedConcreteWithDescendants {
  return asMixedConcreteWithDescendants(that) !== null;
}

/**
 * Try to cast `that` instance to
 * the class {@link MixedConcreteWithDescendantsChild}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedConcreteWithDescendantsChild(
  that: Class
): MixedConcreteWithDescendantsChild | null {
  return (that instanceof MixedConcreteWithDescendantsChild)
    ? <MixedConcreteWithDescendantsChild>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedConcreteWithDescendantsChild(
  that: Class
): that is MixedConcreteWithDescendantsChild {
  return that instanceof MixedConcreteWithDescendantsChild;
}

/**
 * Try to cast `that` instance to
 * the class {@link MixedConcreteLeaf}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asMixedConcreteLeaf(
  that: Class
): MixedConcreteLeaf | null {
  return (that instanceof MixedConcreteLeaf)
    ? <MixedConcreteLeaf>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isMixedConcreteLeaf(
  that: Class
): that is MixedConcreteLeaf {
  return that instanceof MixedConcreteLeaf;
}

/**
 * Try to cast `that` instance to
 * the class {@link ModelTypedFirst}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asModelTypedFirst(
  that: Class
): ModelTypedFirst | null {
  return (that instanceof ModelTypedFirst)
    ? <ModelTypedFirst>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isModelTypedFirst(
  that: Class
): that is ModelTypedFirst {
  return that instanceof ModelTypedFirst;
}

/**
 * Try to cast `that` instance to
 * the class {@link ModelTypedSecond}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asModelTypedSecond(
  that: Class
): ModelTypedSecond | null {
  return (that instanceof ModelTypedSecond)
    ? <ModelTypedSecond>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isModelTypedSecond(
  that: Class
): that is ModelTypedSecond {
  return that instanceof ModelTypedSecond;
}

/**
 * Try to cast `that` instance to
 * the class {@link Something}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asSomething(
  that: Class
): Something | null {
  return (that instanceof Something)
    ? <Something>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isSomething(
  that: Class
): that is Something {
  return that instanceof Something;
}

class TypeMatcher extends AbstractTransformerWithContext<
  Readonly<Class>,
  boolean
> {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralFirstWithContext(
    that: StructuralFirst,
    other: Class
  ): boolean {
    return isStructuralFirst(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformStructuralSecondWithContext(
    that: StructuralSecond,
    other: Class
  ): boolean {
    return isStructuralSecond(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantOneWithContext(
    that: MixedAbstractDescendantOne,
    other: Class
  ): boolean {
    return isMixedAbstractDescendantOne(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedAbstractDescendantTwoWithContext(
    that: MixedAbstractDescendantTwo,
    other: Class
  ): boolean {
    return isMixedAbstractDescendantTwo(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsWithContext(
    that: MixedConcreteWithDescendants,
    other: Class
  ): boolean {
    return isMixedConcreteWithDescendants(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteWithDescendantsChildWithContext(
    that: MixedConcreteWithDescendantsChild,
    other: Class
  ): boolean {
    return isMixedConcreteWithDescendantsChild(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformMixedConcreteLeafWithContext(
    that: MixedConcreteLeaf,
    other: Class
  ): boolean {
    return isMixedConcreteLeaf(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedFirstWithContext(
    that: ModelTypedFirst,
    other: Class
  ): boolean {
    return isModelTypedFirst(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformModelTypedSecondWithContext(
    that: ModelTypedSecond,
    other: Class
  ): boolean {
    return isModelTypedSecond(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomethingWithContext(
    that: Something,
    other: Class
  ): boolean {
    return isSomething(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const TYPE_MATCHER = new TypeMatcher();

/**
 * Check whether the type of `that` matches the type of `other` instance.
 *
 * @remarks
 * We check with `is*` function. Hence, if the class of `other` is a subclass of
 * the class of `that`, we confirm the match.
 *
 * @param that - standard instance
 * @param other - instance whose type is compared against `that`
 */
export function typesMatch<ClassT extends Class>(
  that: ClassT,
  other: Class
): other is ClassT {
  return TYPE_MATCHER.transformWithContext(that, other);
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
