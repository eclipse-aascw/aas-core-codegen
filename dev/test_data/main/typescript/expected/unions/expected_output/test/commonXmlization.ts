/**
 * Provide functions for loading generated XML examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasXmlization from "../src/xmlization";

import * as TestCommon from "./common";

/**
 * Load a maximal XML example of {@link types.StructuralFirst}
 * from the test data directory.
 */
export function loadMaximalStructuralFirst(
): AasTypes.StructuralFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "structuralFirst",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.StructuralFirst}
 * from the test data directory.
 */
export function loadMinimalStructuralFirst(
): AasTypes.StructuralFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "structuralFirst",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.StructuralSecond}
 * from the test data directory.
 */
export function loadMaximalStructuralSecond(
): AasTypes.StructuralSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "structuralSecond",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.StructuralSecond}
 * from the test data directory.
 */
export function loadMinimalStructuralSecond(
): AasTypes.StructuralSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "structuralSecond",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.MixedAbstractDescendantOne}
 * from the test data directory.
 */
export function loadMaximalMixedAbstractDescendantOne(
): AasTypes.MixedAbstractDescendantOne {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedAbstractDescendantOne",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantOne(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantOne in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.MixedAbstractDescendantOne}
 * from the test data directory.
 */
export function loadMinimalMixedAbstractDescendantOne(
): AasTypes.MixedAbstractDescendantOne {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedAbstractDescendantOne",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantOne(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantOne in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.MixedAbstractDescendantTwo}
 * from the test data directory.
 */
export function loadMaximalMixedAbstractDescendantTwo(
): AasTypes.MixedAbstractDescendantTwo {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedAbstractDescendantTwo",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantTwo(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantTwo in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.MixedAbstractDescendantTwo}
 * from the test data directory.
 */
export function loadMinimalMixedAbstractDescendantTwo(
): AasTypes.MixedAbstractDescendantTwo {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedAbstractDescendantTwo",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantTwo(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantTwo in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.MixedConcreteWithDescendants}
 * from the test data directory.
 */
export function loadMaximalMixedConcreteWithDescendants(
): AasTypes.MixedConcreteWithDescendants {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteWithDescendants",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendants(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendants in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.MixedConcreteWithDescendants}
 * from the test data directory.
 */
export function loadMinimalMixedConcreteWithDescendants(
): AasTypes.MixedConcreteWithDescendants {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteWithDescendants",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendants(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendants in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.MixedConcreteWithDescendantsChild}
 * from the test data directory.
 */
export function loadMaximalMixedConcreteWithDescendantsChild(
): AasTypes.MixedConcreteWithDescendantsChild {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteWithDescendantsChild",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendantsChild(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendantsChild in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.MixedConcreteWithDescendantsChild}
 * from the test data directory.
 */
export function loadMinimalMixedConcreteWithDescendantsChild(
): AasTypes.MixedConcreteWithDescendantsChild {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteWithDescendantsChild",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendantsChild(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendantsChild in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.MixedConcreteLeaf}
 * from the test data directory.
 */
export function loadMaximalMixedConcreteLeaf(
): AasTypes.MixedConcreteLeaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteLeaf",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteLeaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.MixedConcreteLeaf}
 * from the test data directory.
 */
export function loadMinimalMixedConcreteLeaf(
): AasTypes.MixedConcreteLeaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "mixedConcreteLeaf",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteLeaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.ModelTypedFirst}
 * from the test data directory.
 */
export function loadMaximalModelTypedFirst(
): AasTypes.ModelTypedFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "modelTypedFirst",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.ModelTypedFirst}
 * from the test data directory.
 */
export function loadMinimalModelTypedFirst(
): AasTypes.ModelTypedFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "modelTypedFirst",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.ModelTypedSecond}
 * from the test data directory.
 */
export function loadMaximalModelTypedSecond(
): AasTypes.ModelTypedSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "modelTypedSecond",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.ModelTypedSecond}
 * from the test data directory.
 */
export function loadMinimalModelTypedSecond(
): AasTypes.ModelTypedSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "modelTypedSecond",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
