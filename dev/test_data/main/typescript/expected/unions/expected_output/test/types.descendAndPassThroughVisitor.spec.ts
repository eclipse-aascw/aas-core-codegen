/**
 * Test `descend*` functions and `PassThroughVisitor`.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";
import * as fs from "fs";

import * as AasTypes from "../src/types";
import * as TestCommon from "./common";
import * as TestCommonJsonization from "./commonJsonization";

/**
 * Record a human-readable trace of a descent over an object tree.
 */
class TracingVisitor extends AasTypes.PassThroughVisitor {
  readonly log = new Array<string>();

  visit(that: AasTypes.Class) {
    this.log.push(TestCommon.traceMark(that));
    super.visit(that);
  }
}

/**
 * Expect that the trace from {@link types.Class.descend} and
 * {@link TracingVisitor} are equal.
 *
 * @param instance - to be descended through
 * @throws an {@link Error} if the two traces are not equal
 */
function expectDescendAndPassThroughVisitorSame(
  instance: AasTypes.Class
): void {
  const traceFromDescend = new Array<string>();
  for (const subInstance of instance.descend()) {
    traceFromDescend.push(TestCommon.traceMark(subInstance));
  }

  const visitor = new TracingVisitor();
  visitor.visit(instance);
  const traceFromVisitor = visitor.log;

  expect(traceFromVisitor.length).toBeGreaterThan(0);
  expect(TestCommon.traceMark(instance)).toStrictEqual(traceFromVisitor[0]);

  traceFromVisitor.shift();

  expect(traceFromVisitor).toStrictEqual(traceFromDescend);
}

/**
 * Compare the trace against the golden one from the test data,
 * or re-record the trace if {@link common.RECORD_MODE}.
 *
 * @param instance - to be traced
 * @param expectedPath - path to the golden trace
 */
function compareOrRecordTrace(
  instance: AasTypes.Class,
  expectedPath: string
) {
  const lines = new Array<string>();
  for (const descendant of instance.descend()) {
    lines.push(TestCommon.traceMark(descendant));
  }
  // NOTE (mristin, 2022-12-09):
  // We add a new line for POSIX systems which prefer a new line
  // at the end of the file.
  lines.push("");
  const got = lines.join("\n");

  if (TestCommon.RECORD_MODE) {
    const parent = path.dirname(expectedPath);
    if (!fs.existsSync(parent)) {
      fs.mkdirSync(parent, {recursive: true});
    }
    fs.writeFileSync(expectedPath, got, "utf-8");
  } else {
    if (!fs.existsSync(expectedPath)) {
      throw new Error(
        `The file with the recorded trace does not exist: ${expectedPath}; ` +
        `you probably want to set the environment ` +
        `variable ${TestCommon.RECORD_MODE_ENVIRONMENT_VARIABLE_NAME}?`
      );
    }

    const expected =
      fs.readFileSync(expectedPath, "utf-8")
      .replace(/\r\n/g, "\n");
    expect(got).toStrictEqual(expected);
  }
}

test("descend of StructuralFirst", () => {
  const instance = TestCommonJsonization.loadMaximalStructuralFirst();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "StructuralFirst",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalStructuralFirst();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of StructuralSecond", () => {
  const instance = TestCommonJsonization.loadMaximalStructuralSecond();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "StructuralSecond",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalStructuralSecond();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of MixedAbstractDescendantOne", () => {
  const instance = TestCommonJsonization.loadMaximalMixedAbstractDescendantOne();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "MixedAbstractDescendantOne",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalMixedAbstractDescendantOne();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of MixedAbstractDescendantTwo", () => {
  const instance = TestCommonJsonization.loadMaximalMixedAbstractDescendantTwo();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "MixedAbstractDescendantTwo",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalMixedAbstractDescendantTwo();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of MixedConcreteWithDescendants", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteWithDescendants();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "MixedConcreteWithDescendants",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteWithDescendants();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of MixedConcreteWithDescendantsChild", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteWithDescendantsChild();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "MixedConcreteWithDescendantsChild",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteWithDescendantsChild();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of MixedConcreteLeaf", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteLeaf();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "MixedConcreteLeaf",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalMixedConcreteLeaf();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of ModelTypedFirst", () => {
  const instance = TestCommonJsonization.loadMaximalModelTypedFirst();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "ModelTypedFirst",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalModelTypedFirst();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of ModelTypedSecond", () => {
  const instance = TestCommonJsonization.loadMaximalModelTypedSecond();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "ModelTypedSecond",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalModelTypedSecond();

  expectDescendAndPassThroughVisitorSame(instance);
});

test("descend of Something", () => {
  const instance = TestCommonJsonization.loadMaximalSomething();

  compareOrRecordTrace(
    instance,
    path.join(
      TestCommon.TEST_DATA_DIR,
      "descend",
      "Something",
      "maximal.json.trace"
    )
  );
});

test("descend against PassThroughVisitor", () => {
  const instance = TestCommonJsonization.loadMaximalSomething();

  expectDescendAndPassThroughVisitorSame(instance);
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
