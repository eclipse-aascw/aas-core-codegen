/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.StructuralFirst} from
 * the test data directory.
 */
export function loadMaximalStructuralFirst(
): AasTypes.StructuralFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StructuralFirst",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.structuralFirstFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.StructuralFirst} from
 * the test data directory.
 */
export function loadMinimalStructuralFirst(
): AasTypes.StructuralFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StructuralFirst",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.structuralFirstFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.StructuralSecond} from
 * the test data directory.
 */
export function loadMaximalStructuralSecond(
): AasTypes.StructuralSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StructuralSecond",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.structuralSecondFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.StructuralSecond} from
 * the test data directory.
 */
export function loadMinimalStructuralSecond(
): AasTypes.StructuralSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "StructuralSecond",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.structuralSecondFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asStructuralSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of StructuralSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MixedAbstractDescendantOne} from
 * the test data directory.
 */
export function loadMaximalMixedAbstractDescendantOne(
): AasTypes.MixedAbstractDescendantOne {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedAbstractDescendantOne",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedAbstractDescendantOneFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantOne(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantOne in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MixedAbstractDescendantOne} from
 * the test data directory.
 */
export function loadMinimalMixedAbstractDescendantOne(
): AasTypes.MixedAbstractDescendantOne {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedAbstractDescendantOne",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedAbstractDescendantOneFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantOne(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantOne in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MixedAbstractDescendantTwo} from
 * the test data directory.
 */
export function loadMaximalMixedAbstractDescendantTwo(
): AasTypes.MixedAbstractDescendantTwo {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedAbstractDescendantTwo",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedAbstractDescendantTwoFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantTwo(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantTwo in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MixedAbstractDescendantTwo} from
 * the test data directory.
 */
export function loadMinimalMixedAbstractDescendantTwo(
): AasTypes.MixedAbstractDescendantTwo {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedAbstractDescendantTwo",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedAbstractDescendantTwoFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedAbstractDescendantTwo(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedAbstractDescendantTwo in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MixedConcreteWithDescendants} from
 * the test data directory.
 */
export function loadMaximalMixedConcreteWithDescendants(
): AasTypes.MixedConcreteWithDescendants {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteWithDescendants",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteWithDescendantsFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendants(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendants in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MixedConcreteWithDescendants} from
 * the test data directory.
 */
export function loadMinimalMixedConcreteWithDescendants(
): AasTypes.MixedConcreteWithDescendants {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteWithDescendants",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteWithDescendantsFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendants(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendants in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MixedConcreteWithDescendantsChild} from
 * the test data directory.
 */
export function loadMaximalMixedConcreteWithDescendantsChild(
): AasTypes.MixedConcreteWithDescendantsChild {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteWithDescendantsChild",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteWithDescendantsChildFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendantsChild(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendantsChild in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MixedConcreteWithDescendantsChild} from
 * the test data directory.
 */
export function loadMinimalMixedConcreteWithDescendantsChild(
): AasTypes.MixedConcreteWithDescendantsChild {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteWithDescendantsChild",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteWithDescendantsChildFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteWithDescendantsChild(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteWithDescendantsChild in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MixedConcreteLeaf} from
 * the test data directory.
 */
export function loadMaximalMixedConcreteLeaf(
): AasTypes.MixedConcreteLeaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteLeaf",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteLeafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteLeaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MixedConcreteLeaf} from
 * the test data directory.
 */
export function loadMinimalMixedConcreteLeaf(
): AasTypes.MixedConcreteLeaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MixedConcreteLeaf",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.mixedConcreteLeafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMixedConcreteLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MixedConcreteLeaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ModelTypedFirst} from
 * the test data directory.
 */
export function loadMaximalModelTypedFirst(
): AasTypes.ModelTypedFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ModelTypedFirst",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.modelTypedFirstFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ModelTypedFirst} from
 * the test data directory.
 */
export function loadMinimalModelTypedFirst(
): AasTypes.ModelTypedFirst {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ModelTypedFirst",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.modelTypedFirstFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedFirst(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedFirst in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ModelTypedSecond} from
 * the test data directory.
 */
export function loadMaximalModelTypedSecond(
): AasTypes.ModelTypedSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ModelTypedSecond",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.modelTypedSecondFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ModelTypedSecond} from
 * the test data directory.
 */
export function loadMinimalModelTypedSecond(
): AasTypes.ModelTypedSecond {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ModelTypedSecond",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.modelTypedSecondFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asModelTypedSecond(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ModelTypedSecond in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
